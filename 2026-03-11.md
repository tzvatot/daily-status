---
layout: default
title: Daily Status - 2026-03-11
date: 2026-03-11
---

# Daily Status - 2026-03-11

## Accomplishments
- Diagnosed and resolved a permission denied issue in the operator's feedback controller on vmaas-dev. The root cause was a missing service account in the cluster's auth configuration — a pre-existing setup issue, not related to our code changes. Validated the fix against the installer's development overlay, which already includes the correct configuration.
- Deployed and validated the AAP direct provider changes on vmaas-dev for [osac-operator#110](https://github.com/osac-project/osac-operator/pull/110).
- Applied updated CRDs for cluster ordering and configured the operator with the correct AAP provision/deprovision templates.
- Created a test cluster order and verified the operator correctly triggers AAP jobs and tracks their state transitions, including populating the jobs array with status updates.
- Identified that AAP job template failure is due to a playbook naming mismatch — the osac-aap repo renamed playbooks but the AAP server templates still reference old names. This is an infrastructure config issue, not an operator bug.
- [osac-test-infra#14](https://github.com/osac-project/osac-test-infra/pull/14) — all review comments addressed and E2E validated, awaiting Akshay's re-review.

## Risks & Challenges
- Ongoing missile attacks in Israel — sleep-deprived, frequent shelter time, helping kids with remote schooling, cooking and childcare throughout the day. Reduced availability and focus expected.
- Cluster order provisioning flow is blocked on fixing the AAP job template playbook paths (need to run the config-as-code sync job or manually update AAP).
- vmaas-dev was set up manually and lacks a dedicated overlay in the installer, leading to configuration drift. Long-term, it should have its own overlay for consistent deployments.

## Key Effort
Deploying and validating the AAP direct provider cluster ordering flow on vmaas-dev, resolving environment configuration issues along the way.
