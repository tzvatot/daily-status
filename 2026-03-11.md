---
layout: default
title: Daily Status - 2026-03-11
date: 2026-03-11
---

# Daily Status - 2026-03-11

## Accomplishments
- Diagnosed and resolved a permission denied issue affecting the compute instance feedback controller on the vmaas-dev cluster. The root cause was a missing service account in the cluster's auth configuration — a pre-existing setup gap, not caused by recent code changes. Validated the fix against the installer's development overlay which already includes the correct configuration.
- Continued testing the cluster order flow with the AAP direct provider on vmaas-dev:
  - Applied updated custom resource definitions for cluster ordering
  - Configured AAP provision and deprovision templates on the deployment
  - Created a test cluster order and verified the operator correctly triggered an AAP job and tracked its status transitions
- Identified a blocker in the AAP job template configuration: playbook references still use old naming conventions after a rename in the [osac-aap](https://github.com/osac-project/osac-aap) repo, causing the provision job to fail. The operator behavior is correct — this is purely an AAP server-side config issue.
- [osac-test-infra#14](https://github.com/osac-project/osac-test-infra/pull/14) — all review comments addressed and E2E validated, awaiting re-review
- [osac-operator#110](https://github.com/osac-project/osac-operator/pull/110) — rebased and deployed to vmaas-dev for integration testing

## Risks & Challenges
- Ongoing missile attacks in Israel — sleep-deprived, frequent shelter time, helping kids with remote schooling, cooking and childcare throughout the day. Reduced availability and focus expected.
- AAP job template playbook paths need to be updated (from old naming to current naming) before cluster order provisioning can be fully validated. Fix requires running the config-as-code sync job or manually updating the template in AAP.

## Key Effort
Testing and validating the AAP direct provider cluster order flow on vmaas-dev, including diagnosing and fixing environment configuration issues blocking end-to-end testing.
