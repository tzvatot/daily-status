---
layout: default
title: Daily Status - 2026-03-11
date: 2026-03-11
---

# Daily Status - 2026-03-11

## Accomplishments
- Diagnosed and resolved a permission denied issue in the operator's feedback controller on vmaas-dev — root cause was a missing service account in the cluster's auth configuration (pre-existing setup issue, not related to our code changes). Validated the fix against the installer's development overlay which already includes both required service accounts. Confirmed the feedback controller is now successfully updating compute instance statuses.
- Progressed cluster order flow testing for the AAP direct provider: applied updated CRDs, configured provisioning and deprovisioning templates, created a test cluster order, and verified the operator correctly triggers AAP jobs and tracks their state transitions.
- Continued addressing review feedback on [osac-test-infra#14](https://github.com/osac-project/osac-test-infra/pull/14) (compute instance API tests) — all comments addressed and E2E validated, awaiting re-review.

## Risks & Challenges
- Ongoing missile attacks in Israel — sleep-deprived, frequent shelter time, helping kids with remote schooling, cooking and childcare throughout the day. Reduced availability and focus expected.
- Blocked on cluster order provisioning: AAP job template references outdated playbook names (renamed in the osac-aap repo but not yet synced to the AAP server). Need to run the config-as-code sync job or manually update the template before cluster provisioning can be retested.
- [osac-operator#110](https://github.com/osac-project/osac-operator/pull/110) (AAP direct provider) is deployed to vmaas-dev but cluster order end-to-end testing is incomplete due to the AAP playbook mismatch.
- vmaas-dev cluster was set up manually and lacks a proper installer overlay, leading to configuration drift (e.g., the missing service account). Long-term, it should have its own overlay for consistent deployment.

## Key Effort
Testing and validating the AAP direct provider cluster order flow on vmaas-dev, including diagnosing and fixing environment configuration issues discovered during deployment.
