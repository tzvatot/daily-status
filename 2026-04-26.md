---
layout: default
title: Daily Status - 2026-04-26
date: 2026-04-26
---

# Daily Status - 2026-04-26

## Accomplishments

- **Fixed template publishing on vmaas-dev**: Resolved multiple configuration issues (missing CA bundle, incorrect playbook paths, missing namespace variables, certificate validation, and OPA permissions) that were blocking AAP template publishing. Successfully re-published templates with correct node set definitions.
- **Validated CRD migration on vmaas-dev**: Deployed updated ClusterOrder CRD changes from [osac-operator#186](https://github.com/osac-project/osac-operator/pull/186), verified new cluster orders use explicit fields under the new API group, and cleaned up stale CRDs left over from the cloudkit-to-osac rename.
- **Restored fulfillment service on vmaas-dev**: Fixed a 3-day crash loop caused by a TLS certificate mismatch, re-registered the hub, and recreated missing host type entries in the fulfillment database.
- **Re-published templates with updated AAP code**: Pointed the AAP project to [osac-aap#247](https://github.com/osac-project/osac-aap/pull/247) branch for [MGMT-23939](https://issues.redhat.com/browse/MGMT-23939), synced and re-published templates with the new explicit cluster fields logic.
- **Reviewed [osac-operator#196](https://github.com/osac-project/osac-operator/pull/196)**: Posted review with 15 git-history-derived rules as suggestions for the modular CLAUDE.md proposal.

## Risks & Challenges

- **vmaas-dev environment is degraded**: The osac-operator informer caches fail to sync, blocking all controllers except computeinstance-feedback. Root cause is stale resources from old deployments and CRD migration leftovers. A stuck cluster order in "Deleting" state with an orphaned namespace needs cleanup, and there may be additional stale CRDs from the old API group that still need removal. All controller functionality (cluster ordering, tenant management, networking) is blocked until this is resolved.

## Key Effort

Restoring the vmaas-dev environment to a working state after CRD migration issues and configuration drift broke template publishing, fulfillment, and operator functionality.
