---
layout: default
title: Daily Status - 2026-04-26
date: 2026-04-26
---

# Daily Status - 2026-04-26

## Accomplishments

- **Restored template publishing on vmaas-dev**: Fixed a chain of configuration issues (missing CA bundle, incorrect playbook paths, missing namespace config, cert validation, and missing service account permissions) that were blocking all AAP automation jobs. Successfully re-published cluster templates with correct node set definitions.
- **Validated CRD migration on vmaas-dev**: Deployed updated ClusterOrder CRD from [osac-operator#186](https://github.com/osac-project/osac-operator/pull/186), verified new cluster orders use explicit fields as designed in [MGMT-23939](https://issues.redhat.com/browse/MGMT-23939), and cleaned up stale `cloudkit.openshift.io` CRDs left over from the rename migration.
- **Re-registered infrastructure in fulfillment DB**: Hub registration and host type entries were missing from the fulfillment database, blocking cluster provisioning — restored both.
- **Fixed fulfillment controller crash loop**: Controller had been in CrashLoopBackOff for 3 days due to a TLS certificate mismatch — resolved by correcting the CA bundle.
- **Rebased and synced [osac-aap#247](https://github.com/osac-project/osac-aap/pull/247)** on upstream main and re-published templates with the updated code.
- **Reviewed [osac-operator#196](https://github.com/osac-project/osac-operator/pull/196)**: Posted review with 15 rules derived from git history as suggestions for the modular CLAUDE.md approach.

## Risks & Challenges

- **vmaas-dev environment is degraded**: The osac-operator informer caches fail to sync, blocking all controllers except computeinstance-feedback. Root cause is stale resources from old deployments and CRD migration leftovers (e.g., a stuck deleting cluster order and its orphaned namespace). Cleanup of all stale resources is needed before the environment is fully functional.
- Pending: rename GitHub fork `tzvatot/cloudkit-aap` to `tzvatot/osac-aap`.

## Key Effort

Restoring the vmaas-dev environment to a working state after cascading configuration, CRD migration, and stale resource issues blocked template publishing, cluster provisioning, and operator startup.
