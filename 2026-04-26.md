---
layout: default
title: Daily Status - 2026-04-26
date: 2026-04-26
---

# Daily Status - 2026-04-26

## Accomplishments

- **Restored template publishing on vmaas-dev**: Fixed a chain of configuration issues (missing CA bundle, incorrect playbook paths, missing namespace config, certificate validation, and missing service account permissions) that were blocking all AAP jobs. Successfully re-published cluster templates with correct node set definitions.
- **Validated CRD migration on vmaas-dev**: Deployed updated ClusterOrder CRD changes from [osac-operator#186](https://github.com/osac-project/osac-operator/pull/186), verified new cluster orders use explicit fields under the new `osac.openshift.io` API group, and cleaned up stale `cloudkit.openshift.io` CRDs left over from the rename.
- **Re-registered infrastructure in fulfillment DB**: Restored missing hub registration and host type entries that were preventing cluster provisioning.
- **Fixed fulfillment controller crash loop**: Resolved a 3-day TLS certificate mismatch caused by the CA bundle pointing to the wrong certificate authority.
- **Reviewed [osac-operator#196](https://github.com/osac-project/osac-operator/pull/196)**: Posted review with 15 git-history-derived rules as suggestions for the modular CLAUDE.md contribution guidelines.
- **Rebased and updated [osac-aap#247](https://github.com/osac-project/osac-aap/pull/247)**: Rebased the explicit-cluster-fields branch on upstream main and force-pushed.

## Risks & Challenges

- **vmaas-dev environment is degraded**: The osac-operator informer caches fail to sync, blocking all controllers except computeinstance-feedback. Root cause is stale resources from old deployments and CRD migration leftovers (e.g., a stuck deleting cluster order referencing a stale namespace). Cleanup of all stale custom resources, namespaces, and any remaining old CRDs is needed before the environment is fully functional.
- GitHub fork `tzvatot/cloudkit-aap` still needs renaming to `tzvatot/osac-aap`.

## Key Effort

Restoring the vmaas-dev environment to a working state after the cloudkit-to-osac CRD migration, fixing a cascade of configuration and data issues blocking template publishing, cluster provisioning, and operator startup.
