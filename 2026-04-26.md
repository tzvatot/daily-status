---
layout: default
title: Daily Status - 2026-04-26
date: 2026-04-26
---

# Daily Status - 2026-04-26

## Accomplishments

- **Fixed template publishing on vmaas-dev**: Resolved multiple configuration issues (missing CA bundle, incorrect playbook paths, missing namespace parameters, certificate validation, and OPA authorization) that were blocking AAP template publishing. Successfully re-published templates with correct node set definitions.
- **Validated CRD migration on vmaas-dev**: Built and deployed a custom osac-operator image with updated CRD changes from [osac-operator#186](https://github.com/osac-project/osac-operator/pull/186), applied the new ClusterOrder CRD, and verified that new clusters correctly use explicit fields under the new API group.
- **Cleaned up legacy CRD conflicts**: Removed stale CRDs from the old API group that were coexisting with the new ones, causing controller confusion.
- **Restored missing environment data**: Re-registered the hub and created missing host type entries in the fulfillment database that were preventing cluster provisioning.
- **Fixed fulfillment controller crash loop**: Resolved a 3-day TLS certificate mismatch by correcting the CA bundle to use the proper certificate authority.
- **Rebased and updated AAP integration branch**: Rebased [osac-aap#247](https://github.com/osac-project/osac-aap/pull/247) on upstream main to incorporate latest changes.
- **Reviewed [osac-operator#196](https://github.com/osac-project/osac-operator/pull/196)**: Posted review with 15 git-history-derived rules as suggestions for the modular CLAUDE.md proposal.

## Risks & Challenges

- **vmaas-dev environment is degraded**: The osac-operator informer caches fail to sync, blocking all controllers except compute-instance feedback. Root cause is stale resources from old deployments and CRD migration remnants. Cleanup of stale custom resources, orphaned namespaces, and any remaining legacy CRDs is needed before the environment is fully functional.

## Key Effort

Restoring the vmaas-dev environment to a working state after CRD migration issues and accumulated configuration drift broke template publishing, cluster provisioning, and operator functionality.
