---
layout: default
title: Daily Status - 2026-04-26
date: 2026-04-26
---

# Daily Status - 2026-04-26

## Accomplishments

- **Fixed vmaas-dev environment end-to-end** — resolved multiple issues preventing the environment from functioning: fixed CA bundle configuration, corrected AAP playbook paths and parameters after the cloudkit→osac rename, re-registered the hub, recreated missing host types, cleaned up stale cluster orders and namespaces from the old CRD group, and updated RBAC for new resource types. All 13 operator controllers are now running successfully.
- **Validated explicit cluster fields on vmaas-dev** — deployed the operator with CRD changes from [osac-operator#186](https://github.com/osac-project/osac-operator/pull/186), created a new cluster using the updated template, and confirmed the ClusterOrder carries explicit fields as expected. Fixed multiple AAP job issues (conditional logic bug, pull secret double-encoding) to get provisioning progressing.
- **Re-published templates on vmaas-dev** — fixed the AAP publish-templates job configuration (playbook path, namespace, certs, service account permissions) and successfully published templates with correct node set definitions. Pointed AAP to the [osac-aap#247](https://github.com/osac-project/osac-aap/pull/247) branch for updated template code.
- **Reviewed [osac-operator#196](https://github.com/osac-project/osac-operator/pull/196)** — posted review with 15 suggested rules derived from git history for the modular CLAUDE.md proposal.
- **Built PR status notifier tool** ([osac-workspace#5](https://github.com/osac-project/osac-workspace/pull/5)) — created a tool that queries all OSAC repos for open PRs, classifies them by review state (needs review, changes requested, CI failing, etc.), and posts a formatted summary to Slack. Includes a systemd timer for weekday scheduling, 28 unit tests, and per-repo output capping. Made the companion [slack-creds-extractor](https://github.com/tzvatot/slack-creds-extractor) repo public.

## Risks & Challenges

- Waiting for operator backoff to expire and retry provisioning with the latest AAP code on vmaas-dev — cluster order reconciliation is in progress but not yet fully complete.
- GitHub fork `tzvatot/cloudkit-aap` still needs renaming to `tzvatot/osac-aap` to align with the project rename.
- Awaiting feedback from Eran on the PR notifier Slack message format before finalizing.

## Key Effort

Restoring the vmaas-dev environment to a fully operational state after the cloudkit-to-osac rename, while validating the explicit cluster fields feature and building a PR status notifier for the team.
