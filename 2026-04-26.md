---
layout: default
title: Daily Status - 2026-04-26
date: 2026-04-26
---

# Daily Status - 2026-04-26

## Accomplishments

- **Fixed template publishing on vmaas-dev**: Resolved multiple configuration issues (missing CA bundle, incorrect playbook paths, missing namespace config, certificate validation, and OPA permissions) that were blocking automated template publishing. Successfully re-published templates with correct node set definitions.
- **Validated cluster ordering with new CRD changes**: Created a new cluster using the updated template and verified that the cluster order correctly includes explicit fields from the updated CRD schema ([MGMT-23939](https://issues.redhat.com/browse/MGMT-23939)).
- **Cleaned up CRD migration artifacts on vmaas-dev**: Removed stale CRDs from the old API group that were coexisting with the new ones, re-registered the hub, and created missing host type entries in the fulfillment database.
- **Fixed fulfillment controller crash loop**: Resolved a 3-day crash loop caused by a TLS certificate mismatch in the CA bundle configuration.
- **Reviewed [osac-operator#196](https://github.com/osac-project/osac-operator/pull/196)**: Posted review with 15 git-history-derived rules as suggestions for the modular CLAUDE.md contribution guidelines.

## Risks & Challenges

- **vmaas-dev environment is degraded**: The osac-operator informer caches fail to sync, blocking all controllers except compute-instance feedback. Root cause is stale resources from old deployments and CRD migration leftovers. A stuck deleting cluster order references a namespace with stale resources that need cleanup. Full environment recovery requires identifying and removing all remaining stale custom resources and old API group CRDs.

## Key Effort

Restoring the vmaas-dev environment to a functional state after CRD migration issues and configuration drift broke template publishing, fulfillment, and operator startup.
