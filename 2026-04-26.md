---
layout: default
title: Daily Status - 2026-04-26
date: 2026-04-26
---

# Daily Status - 2026-04-26

## Accomplishments

- **Restored template publishing on vmaas-dev**: Fixed a chain of configuration issues (missing CA bundle, incorrect playbook paths, missing namespace config, OPA permissions) that were blocking all AAP automation jobs. Successfully re-published cluster templates with correct node set definitions.
- **Validated CRD migration on vmaas-dev**: Deployed updated ClusterOrder CRD changes from [osac-operator#186](https://github.com/osac-project/osac-operator/pull/186), verified new cluster orders use explicit fields, and cleaned up stale `cloudkit.openshift.io` CRDs left over from the rename to `osac.openshift.io`.
- **Re-bootstrapped vmaas-dev supporting services**: Re-registered the hub, created missing host types in the fulfillment DB, and fixed a 3-day fulfillment controller crash caused by TLS certificate mismatch.
- **Reviewed [osac-operator#196](https://github.com/osac-project/osac-operator/pull/196)**: Posted review with 15 git-history-derived rules as suggestions for the modular CLAUDE.md proposal.
- **Built PR status notifier tool** ([osac-workspace#5](https://github.com/osac-project/osac-workspace/pull/5)): Created a lightweight tool that queries all OSAC repos via GitHub GraphQL, classifies PRs by review state (needs review, changes requested, CI failing, etc.), and posts a daily summary to Slack. Includes 28 unit tests and runs on a weekday timer. Awaiting feedback on message format.

## Risks & Challenges

- **vmaas-dev environment is degraded**: The osac-operator informer caches fail to sync, blocking all controllers except computeinstance-feedback. Root cause is stale resources and namespaces from old deployments (e.g., a stuck deleting cluster order referencing a stale namespace) and leftover CRDs from the cloudkit-to-osac migration. Cleanup of stale custom resources, namespaces, and possibly additional old CRDs is needed before the environment is fully functional.

## Key Effort

Restoring the vmaas-dev environment to a working state after CRD migration issues and configuration drift broke template publishing, operator controllers, and supporting services.
