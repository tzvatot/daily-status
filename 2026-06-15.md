---
layout: default
title: Daily Status - 2026-06-15
date: 2026-06-15
---

# Daily Status - 2026-06-15

## Accomplishments
- Created a cluster provisioning script for the UI team using a dev-cluster catalog item, and identified that only one valid template exists on vmaas-dev — other catalog items reference non-existent templates
- Investigated why cluster provisioning is failing on vmaas-dev: all clusters are stuck in a progressing state because no hubs or host types are registered — root cause is that no osac-operator is deployed in the vmaas-dev namespace
- Discovered that the vmaas-dev API server TLS certificate (Let's Encrypt) expired on June 14, breaking all external cluster access; confirmed a partial workaround exists for CLI commands but pod-level access is also broken
- Notified the team via Slack about the certificate expiry and the missing operator deployment

## Risks & Challenges
- **vmaas-dev environment is fully broken** — two independent issues are blocking all cluster provisioning:
  1. Let's Encrypt API server certificate expired Jun 14, breaking all external access
  2. No osac-operator running in the vmaas-dev namespace, so no hubs or host types are registered
- Waiting for replies on certificate renewal and osac-operator deployment; no progress can be made on vmaas-dev testing until both are resolved

## Key Effort
Investigated and diagnosed multiple blocking issues preventing cluster provisioning on the vmaas-dev environment, and created a cluster provisioning script for the UI team.
