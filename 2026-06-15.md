---
layout: default
title: Daily Status - 2026-06-15
date: 2026-06-15
---

# Daily Status - 2026-06-15

## Accomplishments

- **Created a cluster provisioning script for the UI team** — built a helper script to create clusters from catalog items on the vmaas-dev environment. Identified that only one valid template exists; other catalog items reference non-existent templates. Cluster was created but is stuck due to environment issues (see Risks).

- **Investigated and root-caused vmaas-dev cluster provisioning failures** — all clusters on the environment are stuck because no hubs or host types are registered (no operator running in the namespace). Also discovered the API server TLS certificate expired on Jun 14. Notified the team via Slack; awaiting resolution on both issues.

- **Completed GraphQL rate-limit handling PR** — addressed all CodeRabbit review findings on [osac-workspace#59](https://github.com/osac-project/osac-workspace/pull/59) (null-safety guards, narrowed exception handling, sanitized error logging). All tests pass, received approval from Eran, and merged.

- **Set up and tested an alternative CaaS environment** — explored the test-infra-cluster-caas environment shared by Rastislav as a potential alternative to the broken vmaas-dev. Obtained credentials, verified CLI connectivity, and created two catalog items via the public REST API. Provisioned a test cluster, but it is also stuck due to empty hub registration. Awaiting guidance on routing and hub configuration.

- **Submitted draft PR for field definition validation ([OSAC-1400](https://redhat.atlassian.net/browse/OSAC-1400))** — rebased [fulfillment-service#691](https://github.com/osac-project/fulfillment-service/pull/691) on upstream main with full test suite passing (64 suites). Published as draft PR, awaiting review.

- **Reviewed IDP health check APIs PR** — reviewed [fulfillment-service#672](https://github.com/osac-project/fulfillment-service/pull/672) (author: CrystalChun). Flagged one unused message as dead code and made two suggestions around optional field semantics and missing test coverage. Posted as Request Changes.

- **Helped Alexander Chuzhoy with catalog item creation** — drafted and shared a YAML example for creating cluster catalog items via the OSAC CLI, since no documented examples existed.

- **Explored Fleet (GLPI) for bare metal provisioning** — investigated the Fleet system to assess its suitability for bare metal OpenShift provisioning. Concluded it is an inventory/asset tracker only and cannot provision machines or install OpenShift.

## Risks & Challenges

- **vmaas-dev environment is broken** — two issues are blocking all cluster provisioning: (1) the Let's Encrypt API server certificate expired on Jun 14, breaking all external access, and (2) no operator is running in the namespace so no hubs or host types are registered. Awaiting team response on cert renewal and operator deployment.
- **Alternative CaaS environment also cannot provision clusters** — the test-infra-cluster-caas environment has the same empty-hubs problem, and only limited routes are externally accessible. Awaiting clarification from Rastislav.

## Key Effort

Investigated and triaged cluster provisioning failures across two CaaS environments while progressing PR reviews and submitting new work for field definition validation.
