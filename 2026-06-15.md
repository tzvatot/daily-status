---
layout: default
title: Daily Status - 2026-06-15
date: 2026-06-15
---

# Daily Status - 2026-06-15

## Accomplishments
- **Investigated and reported vmaas-dev environment outage** — identified two root causes: expired Let's Encrypt API server certificate and missing osac-operator deployment (no hubs or host types registered). Sent Slack notification about the cert expiry; awaiting response on remediation.
- **Created a test cluster for the UI team** on vmaas-dev using a catalog item script, but cluster is stuck provisioning due to the environment issues above.
- **Merged PR for GraphQL rate-limit handling improvements** in [osac-workspace#59](https://github.com/osac-project/osac-workspace/pull/59) — addressed CodeRabbit review findings including null-safety guards, narrower exception handling, and sanitized error logging to prevent leaking sensitive details. All tests pass, approved by Eran, and merged.
- **Set up and validated an alternative CaaS environment** (test-infra-cluster-caas) shared by Rastislav — confirmed connectivity, obtained Keycloak credentials, created two catalog items via the public API, and attempted cluster provisioning. Cluster is stuck provisioning due to missing hub registration; awaiting guidance on routing and hub configuration.
- **Submitted draft PR for field definition validation** ([fulfillment-service#691](https://github.com/osac-project/fulfillment-service/pull/691)) for [OSAC-1400](https://redhat.atlassian.net/browse/OSAC-1400) — rebased and confirmed full test suite passes. Awaiting review.
- **Reviewed IDP health check API PR** ([fulfillment-service#672](https://github.com/osac-project/fulfillment-service/pull/672)) by CrystalChun — flagged unused protobuf message (dead code), suggested making a timestamp field optional, and noted a missing test. Posted as Request Changes.
- **Helped Alexander Chuzhoy with catalog item creation** — drafted and shared a working YAML example for creating cluster catalog items via the OSAC CLI, since no documented examples existed.
- **Explored Fleet (GLPI) for bare metal provisioning** — determined it is an IT asset inventory tracker, not a provisioning system, and cannot be used to create bare metal machines or install OpenShift.

## Risks & Challenges
- **vmaas-dev environment is broken** — the API server TLS certificate expired on Jun 14 and no osac-operator is deployed, so no clusters can be provisioned. All 8 existing clusters are stuck. Awaiting response on cert renewal and operator deployment.
- **Alternative CaaS environment (test-infra-cluster-caas) also cannot provision clusters** — hubs are not registered despite the operator running, and only limited routes are externally accessible. Awaiting guidance from Rastislav.

## Key Effort
Investigated and worked around broken development environments (vmaas-dev and test-infra-cluster-caas) to unblock cluster provisioning for the UI team, while progressing PR reviews and the field definition validation feature.
