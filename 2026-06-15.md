---
layout: default
title: Daily Status - 2026-06-15
date: 2026-06-15
---

# Daily Status - 2026-06-15

## Accomplishments
- Created a cluster provisioning script for the UI team using a dev-cluster catalog item, but the cluster is stuck in a progressing state due to environment issues ([OSAC-1400](https://redhat.atlassian.net/browse/OSAC-1400)-related env)
- Investigated and identified two root causes for the broken vmaas-dev environment: an expired Let's Encrypt API server certificate and no osac-operator deployed in the vmaas-dev namespace, meaning no hubs or host types are registered. Notified the team via Slack and awaiting resolution
- Addressed all CodeRabbit review findings on [osac-workspace#59](https://github.com/osac-project/osac-workspace/pull/59) (GraphQL rate limit handling) — fixed a null crash in check-suites parsing, narrowed broad exception handling, and sanitized error logging to prevent leaking sensitive details. PR was approved by Eran and merged
- Explored an alternative CaaS environment shared by Rastislav — verified connectivity, obtained Keycloak credentials, created two catalog items via the public API, and provisioned a test cluster. Cluster is also stuck progressing due to missing hub registration; awaiting guidance on routing and hub configuration
- Submitted a draft PR for [OSAC-1400](https://redhat.atlassian.net/browse/OSAC-1400) field definition validation in [fulfillment-service#691](https://github.com/osac-project/fulfillment-service/pull/691) — rebased and validated against the full test suite. Awaiting review
- Reviewed [fulfillment-service#672](https://github.com/osac-project/fulfillment-service/pull/672) (IDP health check APIs) by CrystalChun — flagged an unused proto message as dead code and suggested two improvements. Posted as Request Changes
- Explored the Fleet (GLPI) system to evaluate it for bare metal provisioning — concluded it is an inventory/asset tracker only and cannot be used to provision bare metal machines or install OpenShift

## Risks & Challenges
- **vmaas-dev environment is broken** — the API server TLS certificate expired on Jun 14 and no osac-operator is running, so no clusters can be provisioned. Awaiting cert renewal and operator deployment
- The alternative CaaS environment (test-infra-cluster-caas) also cannot fully provision clusters — hubs are not registered and only the fulfillment API and Keycloak routes are externally reachable. Awaiting response from Rastislav on routing and hub configuration
- No working CaaS environment is currently available for the UI team or for end-to-end testing

## Key Effort
Debugging and working around broken CaaS development environments while progressing on PR reviews and field validation work for [OSAC-1400](https://redhat.atlassian.net/browse/OSAC-1400).
