---
layout: default
title: Daily Status - 2026-06-15
date: 2026-06-15
---

# Daily Status - 2026-06-15

## Accomplishments
- Investigated and identified two issues breaking the vmaas-dev environment: an expired API server TLS certificate and no osac-operator running in the namespace, preventing hub and host type registration. Notified the team via Slack and awaiting resolution.
- Created a script to provision clusters from catalog items for the UI team. Identified that only one valid template exists on vmaas-dev; other catalog items reference non-existent templates. A cluster was created but is stuck due to the environment issues above.
- Addressed all CodeRabbit review findings on [osac-workspace#59](https://github.com/osac-project/osac-workspace/pull/59) (GraphQL rate limit handling) — fixed a null-crash, narrowed exception handling, and sanitized error logging. PR was approved by Eran and merged.
- Set up and validated an alternative CaaS environment shared by Rastislav. Logged in via CLI, created two catalog items via the public API, and attempted cluster provisioning. Cluster is stuck due to empty hub registration, similar to vmaas-dev. Awaiting guidance on routing and hub configuration.
- Submitted a draft PR for [OSAC-1400](https://redhat.atlassian.net/browse/OSAC-1400) field definition validation in [fulfillment-service#691](https://github.com/osac-project/fulfillment-service/pull/691) — rebased and confirmed full test suite passes. Awaiting review.
- Reviewed [fulfillment-service#672](https://github.com/osac-project/fulfillment-service/pull/672) (IDP health check APIs by CrystalChun) — flagged one unused message as dead code and made two suggestions. Posted as Request Changes.
- Helped Alexander Chuzhoy by drafting and sharing a YAML example for creating cluster catalog items via the CLI, since no documented examples existed.
- Explored Fleet (GLPI) as a potential bare metal provisioning path. Concluded it is an inventory/asset tracker only and cannot be used for provisioning or OpenShift installation.

## Risks & Challenges
- The vmaas-dev environment is broken: the API server TLS certificate expired on Jun 14 and no osac-operator is deployed, so no clusters can be provisioned. Awaiting cert renewal and operator deployment.
- The alternative CaaS environment also cannot provision clusters end-to-end due to missing hub registration and limited route exposure. Awaiting reply from Rastislav.

## Key Effort
Investigated and worked around broken dev environments (vmaas-dev and alternative CaaS) while progressing on PR reviews, code fixes, and field validation work.
