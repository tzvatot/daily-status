---
layout: default
title: Daily Status - 2026-06-15
date: 2026-06-15
---

# Daily Status - 2026-06-15

## Accomplishments
- Built a helper script for the UI team to create clusters from catalog items on vmaas-dev; identified that only one valid template exists on the environment. A cluster was created but is stuck in a progressing state due to environment issues.
- Investigated vmaas-dev cluster provisioning failures and identified two root causes: no osac-operator is deployed in the vmaas-dev namespace (meaning no hubs or host types are registered), and the API server TLS certificate expired on June 14. All clusters on the environment are affected. Notified the team via Slack; awaiting response on cert renewal and operator deployment.
- Addressed all CodeRabbit review findings on [osac-workspace#59](https://github.com/osac-project/osac-workspace/pull/59) (GraphQL rate limit handling) — fixed a null-crash in check suite processing and narrowed an overly broad exception catch to prevent silent failures. All tests pass; requested review from Eran.

## Risks & Challenges
- vmaas-dev environment is broken — clusters cannot be provisioned. The API server TLS certificate expired on June 14, breaking all external access, and no osac-operator is running in the namespace so no hubs or host types are registered. Awaiting resolution from the team.

## Key Effort
Investigating and triaging vmaas-dev environment issues blocking cluster provisioning.
