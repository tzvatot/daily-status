---
layout: default
title: Daily Status - 2026-06-15
date: 2026-06-15
---

# Daily Status - 2026-06-15

## Accomplishments
- Investigated and identified root causes for broken vmaas-dev environment: expired Let's Encrypt API server certificate and missing osac-operator deployment in the vmaas-dev namespace, which left no hubs or host types registered. Notified the team via Slack and awaiting resolution.
- Created a cluster provisioning script for the UI team using the only valid catalog item template on vmaas-dev. Cluster was created but is stuck due to the environment issues above.
- Completed [osac-workspace#59](https://github.com/osac-project/osac-workspace/pull/59) — addressed all CodeRabbit review findings (null-safety guards, narrowed exception handling, sanitized error logging), got approval from Eran, and merged the PR.
- Evaluated an alternative CaaS environment shared by Rastislav (test-infra-cluster-caas) — confirmed it is reachable with cluster templates registered, but catalog items and Keycloak auth credentials are still needed.
- Explored Fleet (GLPI) to assess its viability for bare metal provisioning. Concluded it is an inventory/asset tracking system only and cannot be used for provisioning bare metal machines or installing OpenShift.

## Risks & Challenges
- **vmaas-dev is broken** — two independent issues are blocking all cluster provisioning:
  1. Let's Encrypt API server certificate expired Jun 14, breaking all external access.
  2. No osac-operator running in the vmaas-dev namespace, so no hubs or host types are registered and all clusters are stuck.
- Waiting on cert renewal and osac-operator deployment from the infrastructure team.
- Alternative CaaS environment (test-infra-cluster-caas) is not yet usable — waiting for Keycloak credentials from Rastislav.

## Key Effort
Diagnosed and escalated the vmaas-dev environment outage while completing the GraphQL rate-limit handling PR and evaluating alternative environments and provisioning tools.
