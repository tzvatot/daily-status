---
layout: default
title: Daily Status - 2026-06-15
date: 2026-06-15
---

# Daily Status - 2026-06-15

## Accomplishments
- Created a cluster provisioning script for the UI team using a dev-cluster catalog item, identified the only valid template on vmaas-dev, and attempted provisioning — cluster was created but stuck in a progressing state due to environment issues
- Investigated vmaas-dev cluster provisioning failures — root cause: no osac-operator deployed in the vmaas-dev namespace, so no hubs or host types are registered. Also discovered the API server TLS certificate expired on Jun 14. Notified the team via Slack
- Addressed all CodeRabbit review findings on [osac-workspace#59](https://github.com/osac-project/osac-workspace/pull/59) (GraphQL rate limit handling) — fixed a null-crash in check suite handling, narrowed exception catching to prevent silent failures, sanitized error logging to avoid leaking sensitive details. All tests pass, got team approval, and merged the PR
- Explored an alternative CaaS environment (test-infra-cluster-caas) shared by Rastislav — verified connectivity, obtained Keycloak credentials, created two catalog items via the public REST API, and attempted cluster provisioning. Cluster is also stuck progressing due to empty hub registration. Awaiting guidance on routing and hub configuration
- Submitted a draft PR for [OSAC-1400](https://redhat.atlassian.net/browse/OSAC-1400) field definition validation in [fulfillment-service#691](https://github.com/osac-project/fulfillment-service/pull/691) — rebased and validated the full test suite (64 suites passing). Awaiting review
- Reviewed [fulfillment-service#672](https://github.com/osac-project/fulfillment-service/pull/672) (IDP health check APIs by CrystalChun) — posted Request Changes with one important finding around dead code from a removed RPC pattern and two minor suggestions
- Explored Fleet (GLPI) as a potential bare metal provisioning tool — concluded it is an inventory/asset tracker only and cannot be used for bare metal machine creation or OpenShift installation

## Risks & Challenges
- **vmaas-dev environment is broken** — two issues: (1) Let's Encrypt API server certificate expired Jun 14, breaking all external cluster access; (2) no osac-operator running, so no hubs or host types are registered and all clusters are stuck. Awaiting response on certificate renewal and operator deployment
- **Alternative CaaS environment (test-infra-cluster-caas) also cannot provision clusters** — hub registration appears incomplete and most cluster routes are unreachable. Awaiting response from Rastislav on routing and hub configuration
- No working environment is currently available for end-to-end cluster provisioning testing

## Key Effort
Investigated and documented environment failures blocking cluster provisioning on vmaas-dev and an alternative CaaS environment, while progressing on PR reviews and code fixes for the fulfillment service and workspace tooling.
