---
layout: default
title: Daily Status - 2026-06-15
date: 2026-06-15
---

# Daily Status - 2026-06-15

## Accomplishments

- **Created a cluster provisioning script for the UI team** — built a script to create clusters from catalog items on vmaas-dev, identified that only one valid template exists on the environment (others reference non-existent templates). Cluster was created but is stuck due to environment issues.
- **Investigated and diagnosed vmaas-dev environment failures** — found two root causes: the API server TLS certificate expired on Jun 14, breaking all external access, and no operator is deployed in the vmaas-dev namespace, so no hubs or host types are registered. All 8 clusters on the environment are stuck. Notified the team on Slack and awaiting resolution.
- **Merged PR for GraphQL rate limit handling in [osac-workspace#59](https://github.com/osac-project/osac-workspace/pull/59)** — addressed all CodeRabbit review findings including null-safety guards, narrowing exception handling, and sanitizing error logging to prevent leaking sensitive information. All tests pass, received approval from Eran, and merged.
- **Explored alternative CaaS environment (test-infra-cluster-caas)** shared by Rastislav — verified connectivity, obtained Keycloak credentials, logged in via CLI, and created two catalog items and a test cluster. Cluster is also stuck due to empty hubs, and routing to cluster API/console is not yet working. Awaiting reply from Rastislav on routing and hub configuration.
- **Submitted draft PR for field definition validation ([OSAC-1400](https://redhat.atlassian.net/browse/OSAC-1400))** in [fulfillment-service#691](https://github.com/osac-project/fulfillment-service/pull/691) — rebased on main, full test suite passes. Awaiting review.
- **Reviewed [fulfillment-service#672](https://github.com/osac-project/fulfillment-service/pull/672) (IDP health check APIs)** by CrystalChun — posted Request Changes with one important finding (unused dead-code message left over from a removed RPC) and two suggestions around optional field semantics and test coverage.
- **Explored Fleet (GLPI) for bare metal provisioning** — concluded it is an IT asset inventory tracker, not a provisioning system. It cannot create bare metal machines or install OpenShift; separate tooling (Beaker/MAAS + Assisted Installer + storage) would be needed.

## Risks & Challenges

- **vmaas-dev environment is broken** — the API server TLS certificate expired on Jun 14 and no operator is running in the namespace, so no clusters can be provisioned. Awaiting cert renewal and operator deployment.
- **Alternative CaaS environment also cannot provision clusters** — hubs are not registered and API/console routing is not configured. Awaiting guidance from Rastislav.
- Both environment issues block end-to-end cluster creation testing for the UI team.

## Key Effort

Diagnosed and escalated two broken CaaS environments while unblocking PR work on GraphQL rate limit handling and field definition validation.
