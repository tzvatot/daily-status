---
layout: default
title: Daily Status - 2026-06-15
date: 2026-06-15
---

# Daily Status - 2026-06-15

## Accomplishments
- Created a cluster provisioning script for the UI team using a dev-cluster catalog item, and identified that only one valid template exists on the vmaas-dev environment
- Investigated and root-caused vmaas-dev cluster provisioning failures: no osac-operator is deployed in the vmaas-dev namespace, so no hubs or host types are registered — all clusters are stuck. Also discovered the API server TLS certificate expired on Jun 14. Notified the team via Slack
- Addressed all CodeRabbit review findings on [osac-workspace#59](https://github.com/osac-project/osac-workspace/pull/59) (GraphQL rate limit handling) — fixed a null-crash in check suite parsing, narrowed broad exception handling, sanitized error logging to prevent leaking sensitive details. PR was approved by Eran and merged
- Set up and validated an alternative CaaS environment (test-infra-cluster-caas) shared by Rastislav. Logged in via CLI, created two catalog items via the public REST API, and attempted cluster provisioning — cluster is stuck due to empty hub registration, similar to vmaas-dev. Awaiting guidance on routing and hub configuration
- Published a draft PR for [OSAC-1400](https://redhat.atlassian.net/browse/OSAC-1400) field definition validation in [fulfillment-service#691](https://github.com/osac-project/fulfillment-service/pull/691) — rebased and confirmed the full test suite passes. Awaiting review
- Reviewed [fulfillment-service#672](https://github.com/osac-project/fulfillment-service/pull/672) (IDP health check APIs by CrystalChun) — flagged one unused message as dead code and made two suggestions. Posted as Request Changes
- Explored Fleet (GLPI) for bare metal provisioning possibilities. Concluded it is an inventory/asset tracker only and cannot be used to provision bare metal machines or install OpenShift

## Risks & Challenges
- **vmaas-dev environment is broken** — two issues: (1) the Let's Encrypt API server certificate expired Jun 14, breaking all external access, and (2) no osac-operator is running, so no hubs or host types are registered and no clusters can progress. Waiting for cert renewal and operator deployment
- **Alternative CaaS environment (test-infra-cluster-caas) also cannot provision clusters** — hub registration appears incomplete and API server routing is not externally accessible. Waiting for Rastislav's guidance

## Key Effort
Investigated and unblocked cluster provisioning across two CaaS environments (vmaas-dev and test-infra-cluster-caas), while completing PR work on GraphQL rate limit handling and field definition validation.
