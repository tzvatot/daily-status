---
layout: default
title: Daily Status - 2026-06-15
date: 2026-06-15
---

# Daily Status - 2026-06-15

## Accomplishments
- Investigated and identified root causes for broken vmaas-dev environment: expired API server TLS certificate and missing osac-operator deployment preventing hub/host type registration. Notified the team via Slack and awaiting resolution.
- Created a cluster provisioning script for the UI team using the dev-cluster catalog item. Cluster was created but is stuck due to the vmaas-dev environment issues above.
- Addressed all CodeRabbit review findings on [osac-workspace#59](https://github.com/osac-project/osac-workspace/pull/59) (GraphQL rate limit handling) — improved null-safety, narrowed exception handling, and sanitized error logging. PR was approved by Eran and merged.
- Explored an alternative CaaS environment shared by Rastislav on test-infra-cluster-caas as a workaround for the broken vmaas-dev. Verified connectivity and confirmed cluster templates are registered, but catalog items and auth credentials are still needed.
- Explored Fleet (GLPI) to assess its viability for bare metal provisioning. Concluded it is an inventory/asset tracking system only and cannot be used to provision bare metal machines or install OpenShift.

## Risks & Challenges
- vmaas-dev environment is broken — clusters cannot be provisioned due to an expired Let's Encrypt API server certificate and no osac-operator running in the namespace. Waiting for cert renewal and operator deployment.
- The alternative CaaS environment (test-infra-cluster-caas) is reachable but blocked on Keycloak credentials from Rastislav.

## Key Effort
Diagnosed and reported the vmaas-dev environment outage while completing the GraphQL rate limit PR and evaluating alternative environments and provisioning tools.
