---
layout: default
title: Daily Status - 2026-06-15
date: 2026-06-15
---

# Daily Status - 2026-06-15

## Accomplishments
- Created a cluster provisioning script for the UI team using the dev-cluster catalog item, and identified that only one valid template exists on vmaas-dev — other catalog items reference non-existent templates
- Investigated and root-caused the vmaas-dev cluster provisioning failure: no osac-operator is deployed in the vmaas-dev namespace, so no hubs or host types are registered. Also discovered the API server TLS certificate expired on Jun 14. Notified the team via Slack
- Addressed all CodeRabbit review findings on [osac-workspace#59](https://github.com/osac-project/osac-workspace/pull/59) (GraphQL rate limit handling) — fixed a null-crash in check suite handling, narrowed exception handling, and sanitized error logging to prevent leaking sensitive details. PR was approved by Eran and merged
- Explored and onboarded to an alternative CaaS environment (test-infra-cluster-caas) shared by Rastislav. Obtained credentials, confirmed CLI access, and created two catalog items and a test cluster via the public REST API. Hub registration was later fixed by Omer, but a redeployment broke OIDC auth — reported the issue to Rastislav
- Published a draft PR for [OSAC-1400](https://redhat.atlassian.net/browse/OSAC-1400) — field definition validation in [fulfillment-service#691](https://github.com/osac-project/fulfillment-service/pull/691). Rebased and confirmed all 64 test suites pass
- Reviewed [fulfillment-service#672](https://github.com/osac-project/fulfillment-service/pull/672) (IDP health check APIs by CrystalChun) — flagged one unused proto message as dead code and made two additional suggestions. Posted as Request Changes
- Provided Alexander Chuzhoy with a YAML example for creating cluster catalog items via the `osac` CLI, as no documented examples existed
- Explored Fleet (GLPI) to assess whether it could support bare metal provisioning. Concluded it is an inventory/asset tracker only and cannot provision machines or install OpenShift

## Risks & Challenges
- **vmaas-dev environment is broken** — two issues: (1) the Let's Encrypt API server certificate expired on Jun 14, breaking all external access, and (2) no osac-operator is running, so no hubs are registered and all clusters are stuck. Waiting for cert renewal and operator deployment
- **test-infra-cluster-caas auth is broken** — after a redeployment that fixed hub registration, OIDC discovery returns 404 and the fulfillment API rejects tokens. Waiting for auth to be stabilized
- [fulfillment-service#691](https://github.com/osac-project/fulfillment-service/pull/691) ([OSAC-1400](https://redhat.atlassian.net/browse/OSAC-1400)) is a draft PR waiting for review

## Key Effort
Investigated and worked around broken dev environments (vmaas-dev and test-infra-cluster-caas) to unblock cluster provisioning for the UI team, while also completing the GraphQL rate-limit PR and reviewing IDP health check APIs.
