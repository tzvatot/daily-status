---
layout: default
title: Daily Status - 2026-06-15
date: 2026-06-15
---

# Daily Status - 2026-06-15

## Accomplishments
- Created a cluster provisioning script for the UI team using a dev-cluster catalog item, but the cluster is stuck in a progressing state due to environment issues (see Risks below)
- Investigated and identified root causes for vmaas-dev cluster provisioning failures: no osac-operator deployed in the vmaas-dev namespace (so no hubs or host types are registered), and an expired API server TLS certificate. Notified the team via Slack
- Addressed all CodeRabbit review findings on [osac-workspace#59](https://github.com/osac-project/osac-workspace/pull/59) (GraphQL rate limit handling) — fixed null-safety issues, narrowed exception handling, and sanitized error logging to prevent leaking sensitive details. PR was approved by Eran and merged
- Explored an alternative CaaS environment shared by Rastislav on test-infra-cluster-caas — verified it is reachable with cluster templates registered, but awaiting Keycloak credentials to authenticate
- Explored Fleet (GLPI) as a potential bare metal provisioning path — concluded it is an inventory/asset tracker only and cannot provision machines or install OpenShift

## Risks & Challenges
- **vmaas-dev environment is broken** — clusters cannot be provisioned due to two issues: (1) the Let's Encrypt API server certificate expired on Jun 14, breaking all external access, and (2) no osac-operator is running in the vmaas-dev namespace, so no hubs or host types are registered. Awaiting reply on cert renewal and operator deployment
- **Alternative CaaS environment not yet usable** — waiting for Keycloak auth credentials from Rastislav before clusters can be created on test-infra-cluster-caas

## Key Effort
Investigated and triaged the broken vmaas-dev environment while completing the GraphQL rate limit PR and exploring alternative environments for cluster provisioning.
