---
layout: default
title: Daily Status - 2026-06-15
date: 2026-06-15
---

# Daily Status - 2026-06-15

## Accomplishments
- **Investigated and diagnosed vmaas-dev environment outage** — identified two root causes: an expired Let's Encrypt API server certificate and no osac-operator running in the vmaas-dev namespace, leaving no hubs or host types registered. Notified the team via Slack and awaiting resolution.
- **Created a cluster from a catalog item for the UI team** on vmaas-dev, but the cluster is stuck due to the environment issues above.
- **Completed [osac-workspace#59](https://github.com/osac-project/osac-workspace/pull/59)** (GraphQL rate limit handling) — addressed all CodeRabbit review findings including null-safety guards, narrowed exception handling, and sanitized error logging. PR was approved by Eran and merged.
- **Explored an alternative CaaS environment** (test-infra-cluster-caas) shared by Rastislav — verified connectivity, obtained Keycloak credentials, created two catalog items via the public API, and attempted a cluster provisioning. Cluster is stuck due to missing hub registration; awaiting guidance from Rastislav on routing and hub configuration.
- **Submitted draft PR [fulfillment-service#691](https://github.com/osac-project/fulfillment-service/pull/691)** for [OSAC-1400](https://redhat.atlassian.net/browse/OSAC-1400) (validate field definitions) — rebased and confirmed full test suite passes. Awaiting review.
- **Reviewed [fulfillment-service#672](https://github.com/osac-project/fulfillment-service/pull/672)** (IDP health check APIs by CrystalChun) — posted request-changes with one important finding around dead code and two minor suggestions.
- **Helped Alexander Chuzhoy** with catalog item creation by drafting and sharing a CLI example for creating cluster catalog items via `osac create`.
- **Explored Fleet (GLPI)** for bare metal provisioning — concluded it is an inventory/asset tracker only and cannot be used for provisioning bare metal machines or installing OpenShift.

## Risks & Challenges
- **vmaas-dev is unusable** — expired TLS certificate blocks API access and no osac-operator is deployed, so no clusters can progress. Awaiting team response on cert renewal and operator deployment.
- **Alternative CaaS environment (test-infra-cluster-caas) also cannot provision clusters** — hubs are not registered and routing to cluster APIs is not working. Awaiting input from Rastislav.

## Key Effort
Investigated and worked around broken dev environments (vmaas-dev and test-infra-cluster-caas) to unblock cluster provisioning for the UI team, while completing the GraphQL rate limit PR and submitting the field validation draft PR for review.
