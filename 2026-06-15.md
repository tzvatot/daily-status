---
layout: default
title: Daily Status - 2026-06-15
date: 2026-06-15
---

# Daily Status - 2026-06-15

## Accomplishments
- Created a cluster provisioning script for the UI team using the dev-cluster catalog item on vmaas-dev, but the cluster is stuck in PROGRESSING due to environment issues
- Investigated and identified root causes for vmaas-dev cluster provisioning failures: no osac-operator deployed in the vmaas-dev namespace (so no hubs or host types are registered), and the API server TLS certificate expired on Jun 14 — notified the team via Slack
- Addressed all CodeRabbit review findings on [osac-workspace#59](https://github.com/osac-project/osac-workspace/pull/59) (GraphQL rate limit handling) — fixed a null-crash in check suite parsing, narrowed broad exception handling, sanitized error logging to prevent leaking sensitive details, got approval from Eran, and merged the PR
- Set up an alternative CaaS environment (test-infra-cluster-caas) shared by Rastislav — verified connectivity, obtained Keycloak credentials, created two catalog items (dev-cluster and standard-cluster) via the public API, and provisioned a test cluster; cluster is also stuck in PROGRESSING due to no available hubs, awaiting guidance on hub registration and API routing
- Explored Fleet (GLPI) to assess whether it could be used for bare metal provisioning — concluded it is an inventory/asset tracker only and cannot provision machines or install OpenShift

## Risks & Challenges
- **vmaas-dev environment is broken** — two issues preventing cluster provisioning: (1) the Let's Encrypt API server certificate expired Jun 14, breaking all external access, and (2) no osac-operator is running in the vmaas-dev namespace, so no hubs or host types are registered. Awaiting replies on cert renewal and operator deployment.
- **test-infra-cluster-caas environment also cannot provision clusters** — hubs are not registered despite the operator running, and only the fulfillment API and Keycloak routes are externally reachable. Awaiting reply from Rastislav on routing and hub configuration.

## Key Effort
Attempted to provision clusters for the UI team across two CaaS environments, but both are currently blocked by missing hub registrations and infrastructure issues.
