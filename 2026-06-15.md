---
layout: default
title: Daily Status - 2026-06-15
date: 2026-06-15
---

# Daily Status - 2026-06-15

## Accomplishments
- Investigated and identified root causes for the broken vmaas-dev environment: an expired Let's Encrypt TLS certificate blocking all external API access, and no osac-operator running in the vmaas-dev namespace, leaving no hubs or host types registered. Notified the team via Slack and awaiting resolution.
- Created a cluster provisioning script for the UI team using the only valid catalog item template on vmaas-dev. The cluster was created but is stuck due to the missing hub registration issue above.
- Addressed all CodeRabbit review findings on [osac-workspace#59](https://github.com/osac-project/osac-workspace/pull/59) (GraphQL rate limit handling) — fixed null-safety issues, narrowed exception handling, sanitized error logging to prevent leaking sensitive details, and merged the PR after receiving approval from Eran.
- Explored the alternative CaaS environment shared by Rastislav (test-infra-cluster-caas) as a workaround for the broken vmaas-dev. Confirmed it is reachable with cluster templates registered, but catalog items and Keycloak auth credentials are still needed.
- Investigated Fleet (GLPI) to assess whether it could support bare metal provisioning. Concluded it is an inventory/asset tracking system only and cannot provision machines or install OpenShift.

## Risks & Challenges
- **vmaas-dev environment is down** — two separate issues are blocking all cluster provisioning: (1) expired API server TLS certificate, and (2) no osac-operator deployed, so no hubs or host types are registered. Waiting on the team for cert renewal and operator deployment.
- **Alternative CaaS environment not yet usable** — waiting on Keycloak credentials from Rastislav and need to create catalog items before it can serve as a workaround.

## Key Effort
Diagnosing and working around the broken vmaas-dev environment while completing the GraphQL rate limit PR and evaluating alternative environments for cluster provisioning.
