---
layout: default
title: Daily Status - 2026-06-15
date: 2026-06-15
---

# Daily Status - 2026-06-15

## Accomplishments
- Created a cluster provisioning script for the UI team using a dev-cluster catalog item, but the cluster is stuck in a progressing state due to environment issues ([OSAC-1400](https://redhat.atlassian.net/browse/OSAC-1400)-related environment)
- Investigated and root-caused the vmaas-dev environment outage: the API server TLS certificate expired on Jun 14, and no osac-operator is deployed in the vmaas-dev namespace, meaning no hubs or host types are registered — all 8 clusters on the environment are stuck. Notified the team via Slack
- Addressed all CodeRabbit review findings on [osac-workspace#59](https://github.com/osac-project/osac-workspace/pull/59) (GraphQL rate limit handling) — fixed a null-crash in check suite handling, narrowed broad exception catches, and sanitized error logging to prevent leaking sensitive info. PR approved by Eran and merged
- Explored and onboarded to an alternative CaaS environment shared by Rastislav. Verified connectivity, authenticated via Keycloak, and created two catalog items and a test cluster. Cluster is also stuck in a progressing state due to missing hub registration — awaiting guidance on routing and hub configuration
- Submitted a draft PR for [OSAC-1400](https://redhat.atlassian.net/browse/OSAC-1400) field definition validation in [fulfillment-service#691](https://github.com/osac-project/fulfillment-service/pull/691) — rebased and all 64 test suites pass, awaiting review
- Reviewed [fulfillment-service#672](https://github.com/osac-project/fulfillment-service/pull/672) (IDP health check APIs by CrystalChun) — posted request-changes with one important finding (unused dead-code message) and two suggestions
- Explored Fleet (GLPI) as a potential bare metal provisioning path — concluded it is an inventory/asset tracker only and cannot provision machines or install OpenShift

## Risks & Challenges
- **vmaas-dev environment is broken** — two issues blocking all cluster provisioning: (1) the Let's Encrypt API server certificate expired Jun 14, breaking all external access, and (2) no osac-operator is running, so no hubs or host types are registered. Awaiting response on cert renewal and operator deployment
- **Alternative CaaS environment also cannot provision clusters** — hubs appear unregistered and API server routing is not accessible externally. Awaiting reply from Rastislav on routing and hub configuration
- No working environment is currently available for end-to-end cluster provisioning testing

## Key Effort
Investigated and triaged broken CaaS environments (vmaas-dev and test-infra) while completing PR work on GraphQL rate limit handling and field definition validation.
