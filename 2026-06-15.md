---
layout: default
title: Daily Status - 2026-06-15
date: 2026-06-15
---

# Daily Status - 2026-06-15

## Accomplishments
- Created a cluster provisioning script for the UI team; identified that only one valid template exists on vmaas-dev, as other catalog items reference non-existent templates
- Investigated vmaas-dev cluster provisioning failures — determined root cause: no osac-operator is deployed in the vmaas-dev namespace, so no hubs or host types are registered, leaving all eight clusters stuck in a progressing state
- Discovered that the API server TLS certificate (Let's Encrypt) expired on Jun 14, breaking all external cluster access; confirmed that kubelet certificates are also affected
- Notified the team via Slack about the certificate expiry

## Risks & Challenges
- vmaas-dev environment is broken due to two issues: (1) expired API server TLS certificate blocking external access, and (2) no osac-operator running, so no hubs or host types are registered and clusters cannot progress
- Awaiting response on certificate renewal and osac-operator deployment

## Key Effort
Investigated and diagnosed vmaas-dev environment failures preventing cluster provisioning.
