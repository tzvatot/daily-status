---
layout: default
title: Daily Status - 2026-06-15
date: 2026-06-15
---

# Daily Status - 2026-06-15

## Accomplishments
- Created a cluster provisioning script for the UI team using the dev-cluster catalog item, and identified that only one valid template exists on vmaas-dev — other catalog items reference non-existent templates
- Investigated vmaas-dev cluster provisioning failures — discovered that no osac-operator is deployed in the vmaas-dev namespace, meaning no hubs or host types are registered, causing all 8 clusters to be stuck in a progressing state
- Discovered that the vmaas-dev API server TLS certificate (Let's Encrypt) expired on Jun 14, breaking all external access; notified the team on Slack
- Addressed CodeRabbit review findings on [osac-workspace#59](https://github.com/osac-project/osac-workspace/pull/59) (GraphQL rate limit handling) — fixed a null-crash in check suites processing and narrowed an overly broad exception catch to prevent silent failures; all tests pass. Resolved all review threads and requested review from Eran

## Risks & Challenges
- **vmaas-dev environment is broken** — two issues are blocking cluster provisioning:
  1. Let's Encrypt API server certificate expired Jun 14 — all external cluster access is broken
  2. No osac-operator running in the vmaas-dev namespace — no hubs or host types are registered
- Awaiting response on certificate renewal and osac-operator deployment
- [osac-workspace#59](https://github.com/osac-project/osac-workspace/pull/59) is awaiting review

## Key Effort
Investigated and diagnosed the vmaas-dev environment outage blocking cluster provisioning, while also addressing code review feedback on the GraphQL rate limit handling PR.
