---
layout: default
title: Daily Status - 2026-06-15
date: 2026-06-15
---

# Daily Status - 2026-06-15

## Accomplishments

- Created a cluster provisioning script for the UI team using a dev-cluster catalog item, and identified that only one valid template exists on vmaas-dev — other catalog items reference non-existent templates
- Investigated vmaas-dev cluster provisioning failures: all clusters are stuck because no osac-operator is deployed in the vmaas-dev namespace, so no hubs or host types are registered. Separately discovered that the API server TLS certificate expired on Jun 14
- Notified the team on Slack about the certificate expiry; awaiting reply on cert renewal and osac-operator deployment
- Completed [osac-workspace#59](https://github.com/osac-project/osac-workspace/pull/59) — hardened the GraphQL rate-limit handling PR by fixing a null-reference crash, narrowing exception handling to avoid silently swallowing failures, and sanitizing error logs to prevent leaking sensitive details. All code review findings addressed, approved by Eran, and merged

## Risks & Challenges

- **vmaas-dev environment is broken** — clusters cannot be provisioned due to two issues: (1) the Let's Encrypt API server certificate expired Jun 14, breaking all external access, and (2) no osac-operator is running in the vmaas-dev namespace, so no hubs or host types are registered. Waiting for resolution from the team

## Key Effort

Investigated and diagnosed the vmaas-dev environment outage blocking cluster provisioning, while completing the GraphQL rate-limit hardening PR.
