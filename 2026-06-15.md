---
layout: default
title: Daily Status - 2026-06-15
date: 2026-06-15
---

# Daily Status - 2026-06-15

## Accomplishments
- Investigated and identified root causes for broken vmaas-dev environment: expired Let's Encrypt API server certificate and missing osac-operator deployment in the vmaas-dev namespace, which left no hubs or host types registered. Notified the team via Slack and awaiting resolution.
- Created a cluster provisioning script for the UI team using the only valid catalog item template on vmaas-dev. Cluster was created but is stuck due to the environment issues above.
- Addressed all CodeRabbit review findings on [osac-workspace#59](https://github.com/osac-project/osac-workspace/pull/59) (GraphQL rate limit handling) — fixed null-safety issues, narrowed exception handling, and sanitized error logging to prevent leaking sensitive details. All tests pass, PR approved by Eran and merged.
- Explored an alternative CaaS environment shared by Rastislav (`test-infra-cluster-caas`) as a workaround for the broken vmaas-dev. Verified connectivity and confirmed cluster templates are registered, but catalog items and auth credentials are still needed. Waiting for Keycloak credentials from Rastislav.
- Explored Fleet (GLPI) to assess whether it could be used for bare metal provisioning. Concluded it is an inventory/asset tracking system only and cannot provision machines or install OpenShift.

## Risks & Challenges
- **vmaas-dev environment is down** — two issues are blocking all cluster provisioning: (1) the API server TLS certificate expired on Jun 14, breaking all external access, and (2) no osac-operator is running in the vmaas-dev namespace, so no hubs or host types are registered. Waiting for cert renewal and operator deployment.
- Workaround using `test-infra-cluster-caas` environment is blocked on receiving Keycloak auth credentials.

## Key Effort
Diagnosed and reported the vmaas-dev environment outage while completing the GraphQL rate limit PR and exploring alternative environments to unblock the UI team.
