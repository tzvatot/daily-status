---
layout: default
title: Daily Status - 2026-05-07
date: 2026-05-07
---

# Daily Status - 2026-05-07

## Accomplishments
- **Fixed OAuth integration for fulfillment-controller** ([MGMT-XXXXX references not present]): Resolved multiple configuration issues blocking the end-to-end cluster provisioning flow — corrected the Authorino AuthConfig issuer URL that was causing Keycloak JWKS timeouts, fixed tenant expression to return valid tenants instead of group names, and updated the ClusterOrder CRD to include all required fields that were being silently dropped.
- **Cleaned up stale cluster data**: Purged orphaned clusters from the database that were left behind by force-deleted orders, and reset cluster states to allow the controller to proceed with new provisioning attempts.
- **Validated full provisioning pipeline**: Successfully triggered an end-to-end cluster creation flow through AAP — the job progressed through L2/L3 networking, agent registration, floating IP, DNS, and HostedCluster creation before failing at a bare-metal infrastructure level (MTU probe timeout), confirming all application-level fixes are working correctly.

## Risks & Challenges
- AAP job failed after ~58 minutes due to an MTU probe timeout during cluster bootstrap. This is an ESI bare-metal networking issue (not application code) and needs investigation into VLAN/network configuration.

## Key Effort
Completing the OAuth and Authorino integration fixes for the fulfillment-controller and validating the full cluster provisioning pipeline end-to-end.
