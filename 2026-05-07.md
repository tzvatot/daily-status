---
layout: default
title: Daily Status - 2026-05-07
date: 2026-05-07
---

# Daily Status - 2026-05-07

## Accomplishments
- Resolved OAuth integration issue between the fulfillment controller and Authorino — the AuthConfig had an incorrect Keycloak issuer URL causing JWKS endpoint timeouts
- Fixed tenant assignment in JWT authentication so the controller could properly reconcile with valid tenant values
- Updated the ClusterOrder CRD to include required fields that were being silently dropped by Kubernetes
- Cleaned up stale cluster records in the database that were left behind by force-deleted orders, and reset failed cluster state to unblock the controller
- Controller successfully created a ClusterOrder with all required fields, and the full AAP automation flow executed through L2/L3 networking, agent registration, DNS setup, and HostedCluster creation
- Identified that the remaining failure (MTU probe timeout during cluster bootstrap) is an infrastructure-level networking issue, not an application-level bug

## Risks & Challenges
- Cluster creation fails at bootstrap due to an MTU probe timeout in the network operator — this appears to be an ESI bare-metal network configuration issue outside of application control
- MTU issue needs investigation before a successful end-to-end cluster creation can be demonstrated

## Key Effort
Continued debugging and fixing the fulfillment controller's OAuth and Authorino integration, reaching the point where the full automation pipeline runs successfully up to cluster bootstrap.
