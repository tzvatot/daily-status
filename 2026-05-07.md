---
layout: default
title: Daily Status - 2026-05-07
date: 2026-05-07
---

# Daily Status - 2026-05-07

## Accomplishments
- **Fixed OAuth integration for fulfillment-controller**: Resolved Authorino authentication failures by correcting the AuthConfig issuer URL (wrong port was causing Keycloak JWKS timeout) and fixing the tenant expression so JWT auth returns valid tenants instead of group names.
- **Fixed ClusterOrder creation**: Applied the updated ClusterOrder CRD from osac-operator to include required fields that were being silently dropped by Kubernetes, and cleaned up stale cluster data in the database that was blocking reconciliation.
- **Validated end-to-end cluster provisioning flow**: Successfully ran the full AAP provisioning job through L2/L3 networking, agent registration, DNS setup, and HostedCluster creation — confirming all application-level code and config fixes are working correctly.
- **Identified infrastructure-level failure**: AAP job failed at cluster bootstrap due to an MTU probe timeout, which is an ESI bare-metal networking issue unrelated to application code.

## Risks & Challenges
- Cluster provisioning is blocked by an MTU probe failure during network operator bootstrap — this appears to be an ESI/bare-metal VLAN configuration issue that needs investigation.

## Key Effort
Continued debugging and fixing the fulfillment-controller OAuth and Authorino integration, reaching full end-to-end validation of the cluster provisioning flow.
