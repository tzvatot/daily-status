---
layout: default
title: Daily Status - 2026-05-07
date: 2026-05-07
---

# Daily Status - 2026-05-07

## Accomplishments
- **Resolved OAuth integration issue with fulfillment controller**: Fixed the Authorino AuthConfig that was pointing to the wrong Keycloak issuer URL, which caused token validation timeouts. OAuth flow now works end-to-end and the API successfully returns cluster data.
- **Fixed tenant authorization for JWT-authenticated requests**: Corrected the tenant expression in AuthConfig so that JWT-authenticated calls receive valid tenant assignments, unblocking controller reconciliation.
- **Updated ClusterOrder CRD to include required fields**: Applied the latest CRD from osac-operator so that critical fields (pull secret, SSH key, release image) are no longer silently dropped by Kubernetes.
- **Cleaned up stale cluster data**: Purged orphaned clusters from the database that were left behind by force-deleted orders, and reset cluster state to allow the controller to proceed.
- **End-to-end cluster provisioning triggered successfully**: The fulfillment controller created a valid ClusterOrder with all required fields, and the downstream AAP job is running — it has passed L2/L3 networking, agent registration, floating IP, DNS, and HostedCluster creation stages.

## Risks & Challenges
- Cluster bootstrap is still in progress (~31 network operator retries remaining). Waiting to confirm the cluster reaches a fully ready state.

## Key Effort
Continued debugging and fixing the fulfillment-controller OAuth and Authorino integration to enable end-to-end CaaS cluster provisioning.
