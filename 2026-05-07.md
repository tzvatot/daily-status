---
layout: default
title: Daily Status - 2026-05-07
date: 2026-05-07
---

# Daily Status - 2026-05-07

## Accomplishments

- **Fixed OAuth + Authorino integration for fulfillment-controller** ([continued from yesterday](https://issues.redhat.com/browse/)): Resolved multiple issues blocking the end-to-end flow — corrected the Keycloak issuer URL that was causing Authorino to time out, fixed tenant assignment in JWT auth expressions, applied the updated ClusterOrder CRD that was missing required fields (which Kubernetes was silently dropping), and cleaned up stale cluster data from the database. The controller successfully created a ClusterOrder and triggered a full AAP provisioning run that passed all stages up through HostedCluster creation.
- **Identified cluster bootstrap failure as infrastructure issue**: The AAP provisioning job failed after ~58 minutes due to an MTU probe timeout in the network operator. Confirmed this is an ESI bare-metal networking issue, not an application-level bug — all application code and configuration fixes are working correctly.
- **Prepared CaaS API demo for recording**: Pivoted approach to record a demo showing CLI + API flow up to PROGRESSING state rather than waiting for full cluster readiness. Created a demo recording script, set up asciinema + agg tooling for crisp terminal recordings, and cleaned up the environment for a fresh recording.
- **Fixed the /update-daily command**: Updated the command to always check the actual current date instead of relying on potentially stale conversation context.

## Risks & Challenges

- MTU probe failure during cluster bootstrap is an ESI/bare-metal network configuration issue that needs investigation — this blocks demonstrating a fully completed cluster creation flow.

## Key Effort

Resolving the full chain of OAuth, authorization, and CRD issues blocking the fulfillment-controller's end-to-end cluster provisioning flow, and preparing the CaaS API demo recording.
