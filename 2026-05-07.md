---
layout: default
title: Daily Status - 2026-05-07
date: 2026-05-07
---

# Daily Status - 2026-05-07

## Accomplishments
- **Resolved OAuth authentication failure in fulfillment-controller**: Fixed the Authorino AuthConfig issuer URL that was pointing to the wrong Keycloak port, which caused JWKS endpoint timeouts and blocked all API calls.
- **Fixed tenant authorization for JWT-authenticated requests**: Corrected the tenant expression in the AuthConfig so that API requests receive valid tenant assignments instead of returning authorization errors.
- **Updated ClusterOrder CRD to include required fields**: Applied the latest CRD from [osac-operator](https://github.com/osac-project/osac-operator) so that cluster provisioning fields are no longer silently dropped by Kubernetes.
- **Cleaned up stale cluster data**: Purged orphaned cluster records from the database that were left behind by force-deleted orders, and reset cluster status to unblock the reconciliation loop.
- **Validated end-to-end cluster provisioning**: The fulfillment-controller successfully created a ClusterOrder with all required fields, and the AAP provisioning job is running — currently passing L2/L3 networking, agents, floating IP, DNS, and HostedCluster creation stages.

## Risks & Challenges
None.

## Key Effort
Continued debugging and fixing the fulfillment-controller's OAuth and Authorino integration to unblock end-to-end CaaS cluster provisioning.
