---
layout: default
title: Daily Status - 2026-05-07
date: 2026-05-07
---

# Daily Status - 2026-05-07

## Accomplishments
- **Resolved OAuth integration issue with fulfillment-controller**: Fixed the Authorino AuthConfig that had an incorrect Keycloak issuer URL, which was causing token validation timeouts. OAuth authentication now works correctly and returns cluster data.
- **Fixed tenant authorization configuration**: Corrected the JWT auth tenant expression in AuthConfig so the controller can properly authorize API requests.
- **Updated ClusterOrder CRD and cleaned up stale data**: Applied the latest CRD from osac-operator to include required fields that were being silently dropped, and purged stale cluster records from the database that were left behind by force-deleted orders.
- **Achieved successful end-to-end controller flow**: The fulfillment-controller now successfully reconciles, authenticates via OAuth, queries the CaaS API, and creates ClusterOrders with all required fields. The full AAP automation flow ran through L2/L3 networking, agent registration, DNS configuration, and HostedCluster creation.

## Risks & Challenges
- AAP job failed after ~58 minutes due to an MTU probe failure during cluster bootstrap. This appears to be an ESI bare-metal network configuration issue, not an application-level problem. Investigation is needed to resolve the VLAN/MTU settings before cluster creation can succeed.

## Key Effort
Completing the OAuth and Authorino integration for the fulfillment-controller, achieving a full end-to-end reconciliation flow from API authentication through ClusterOrder creation.
