---
layout: default
title: Daily Status - 2026-05-07
date: 2026-05-07
---

# Daily Status - 2026-05-07

## Accomplishments
- **Resolved OAuth integration issue** between the fulfillment controller and Authorino — the root cause was an incorrect issuer URL in the AuthConfig, which caused Authorino to time out connecting to the Keycloak JWKS endpoint.
- **Fixed tenant assignment in JWT authentication** — the auth configuration was returning incorrect tenant values, preventing the controller from processing requests.
- **Updated the ClusterOrder CRD** to include required fields that were being silently dropped by Kubernetes, and cleaned up stale cluster data from the database that was left behind by force-deleted orders.
- **Achieved a successful end-to-end controller flow** — the controller now correctly creates ClusterOrders with all required fields, and the AAP job progressed through L2/L3 networking, agent registration, DNS setup, and HostedCluster creation before hitting an infrastructure-level MTU probe failure unrelated to application code.

## Risks & Challenges
- AAP job failed after ~58 minutes due to an MTU probe timeout during cluster bootstrap — this appears to be an ESI bare-metal network configuration issue, not an application-level problem. Investigation is needed to resolve the VLAN/MTU setup before cluster creation can succeed.

## Key Effort
Continued debugging and fixing the fulfillment-controller OAuth and Authorino integration, achieving a working end-to-end flow up to an infrastructure-level network issue.
