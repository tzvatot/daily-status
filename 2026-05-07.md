---
layout: default
title: Daily Status - 2026-05-07
date: 2026-05-07
---

# Daily Status - 2026-05-07

## Accomplishments
- Resolved the OAuth integration issue between the fulfillment controller and Authorino by correcting the Keycloak issuer URL in the AuthConfig, which was causing Authorino to time out when connecting to the JWKS endpoint
- Fixed tenant assignment in the JWT authentication configuration so the controller could properly authenticate and authorize API calls
- Applied the updated ClusterOrder CRD from osac-operator to include required fields that were being silently dropped by Kubernetes
- Cleaned up stale cluster data from the database — purged orphaned clusters left behind by force-deleted orders and reset failed cluster states
- Successfully triggered a full end-to-end reconciliation: the controller created a ClusterOrder with all required fields, and the resulting AAP job is progressing through networking, agents, DNS, and HostedCluster creation stages
- Cluster bootstrap is in progress, currently waiting for the network operator to complete retries

## Risks & Challenges
None

## Key Effort
Continued debugging and fixing the fulfillment controller's OAuth and Authorino integration to enable end-to-end cluster provisioning through the CaaS API.
