---
layout: default
title: Daily Status - 2026-05-04
date: 2026-05-04
---

# Daily Status - 2026-05-04

## Accomplishments
- Re-published AAP templates in the CaaS demo environment after [osac-aap#274](https://github.com/osac-project/osac-aap/pull/274) merge, confirming that credential parameters were removed from templates and are now passed explicitly via CLI flags
- Fixed osac-operator crash loop on vmaas-dev caused by missing RBAC permissions for the PublicIP resource, and updated the operator to the latest version
- Fixed AuthConfig tenant assignment so only the template-publisher service account receives shared tenant access, resolving cluster creation failures
- Fixed broken Ansible role reference that caused AAP job failures after a recent Netris-related change ([MGMT-23420](https://issues.redhat.com/browse/MGMT-23420)) — created and merged [osac-aap#282](https://github.com/osac-project/osac-aap/pull/282), and deployed the fix to vmaas-dev
- Cleaned up 5 stale ESI routers that were causing router quota exhaustion, unblocking cluster provisioning

## Risks & Challenges
- Cluster provisioning is blocked — no bare-metal agents of the required resource class are available; all are bound to an existing cluster order. Need to free up agents before retrying.
- Identified a potential reliability issue with router cleanup during cluster deletion — orphaned routers suggest the deprovision flow may not handle partial failures or early CR deletion correctly. Needs investigation.

## Key Effort
Unblocking the CaaS demo environment by resolving a chain of infrastructure and configuration issues across the operator, AAP, and OpenStack layers.
