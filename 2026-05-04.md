---
layout: default
title: Daily Status - 2026-05-04
date: 2026-05-04
---

# Daily Status - 2026-05-04

## Accomplishments

- **CaaS demo environment**: Re-published AAP templates after merging PR [osac-aap#274](https://github.com/osac-project/osac-aap/pull/274), confirmed template parameters were removed, and validated cluster creation with explicit secret/key file flags works correctly.
- **Fixed osac-operator crash loop on vmaas-dev**: Diagnosed an informer cache sync timeout caused by missing RBAC permissions for the PublicIP resource, patched the ClusterRole, and updated the operator to the latest image — all controllers now syncing.
- **Fixed AuthConfig tenant assignment**: Resolved a cluster creation failure where all service accounts were incorrectly assigned multiple tenants; corrected the config so only the template-publisher SA gets the shared tenant.
- **Fixed manage_agents role resolution failure**: Root-caused an AAP job failure to a short-name role reference introduced in the Netris ([MGMT-23420](https://issues.redhat.com/browse/MGMT-23420)) commit, fixed it to use fully qualified collection names, deployed and verified the fix, and merged [osac-aap#282](https://github.com/osac-project/osac-aap/pull/282).
- **Cleaned up stale ESI routers blocking cluster creation**: Deleted 5 orphaned routers that were exhausting the router quota, removed stuck ClusterOrders blocked by a Route 53 IAM permission gap, cleaned stale labels from agents, and restored the environment to a clean state with free agents available.
- **Identified agent/ESI registration mismatch**: Discovered that two candidate agents are not registered in ESI Ironic, cross-referenced all agents against the Ironic node list, and identified two alternative agents that are both the correct hardware type and present in ESI.
- **Reviewed and approved [fulfillment-service#481](https://github.com/osac-project/fulfillment-service/pull/481)**: Re-reviewed the installation guide PR, confirmed all 5 prior findings were addressed, and approved.
- **Reviewed and approved [osac-aap#268](https://github.com/osac-project/osac-aap/pull/268)**: Confirmed the last open review item (Netris API merge-style PUT behavior) via Slack with the author, and approved.
- **Reviewed [osac-aap#279](https://github.com/osac-project/osac-aap/pull/279)** ([MGMT-24176](https://issues.redhat.com/browse/MGMT-24176)): Reviewed the BMH-based agent import playbook, posted 3 important findings including silently skipped updates, missing job template settings, and broken local testing docs.
- **Reviewed [osac-aap#282](https://github.com/osac-project/osac-aap/pull/282)**: Confirmed the FQCN fix and flagged a pre-existing duplicate task to the author for clarification.
- **Created `/slack-read` command**: Built a Claude Code command to read and summarize Slack threads directly from the CLI.

## Risks & Challenges

- **Route 53 IAM permissions**: The DNS cleanup step in the cluster delete flow fails because the IAM user is missing the required hosted zone permission — this is a separate blocker from the FQCN fix and will cause DNS resources to be orphaned on cluster deletion until resolved.
- **Orphaned ESI resources**: Force-deleting ClusterOrders (via finalizer removal) skips AAP cleanup, leaving routers and other ESI resources behind; at least one stale router still exists.
- **Agent availability**: Only 2 agents are both the correct hardware type and registered in ESI; one has a stale label and the other needs approval before the demo cluster can be created.

## Key Effort

Stabilizing the vmaas-dev CaaS demo environment by fixing a chain of issues — operator RBAC, tenant assignment, Ansible role resolution, stale resource cleanup, and agent/ESI registration mismatches — to unblock end-to-end cluster creation for the CaaS API demo.
