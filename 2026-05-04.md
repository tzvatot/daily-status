---
layout: default
title: Daily Status - 2026-05-04
date: 2026-05-04
---

# Daily Status - 2026-05-04

## Accomplishments

- **CaaS demo environment — re-published templates after [osac-aap#274](https://github.com/osac-project/osac-aap/pull/274):** Synced the AAP project to latest main, re-ran the publish-templates job, and verified that cluster templates no longer expose sensitive parameters directly. Tested cluster creation with the new explicit secret file flags — working correctly.
- **Fixed osac-operator crash loop on vmaas-dev:** Diagnosed a controller cache sync timeout caused by missing RBAC permissions for the PublicIP resource. Patched the ClusterRole and updated the operator to the latest image — all controllers now syncing.
- **Fixed AuthConfig tenant assignment:** Resolved a cluster creation failure where all service accounts were incorrectly assigned to multiple tenants. Corrected the configuration so only the template-publisher service account gets cross-tenant access. Cluster creation resumed successfully.
- **Fixed missing Ansible role reference — [osac-aap#282](https://github.com/osac-project/osac-aap/pull/282):** Debugged an AAP job failure caused by a short role name that couldn't be resolved after a recent refactor ([MGMT-23420](https://issues.redhat.com/browse/MGMT-23420)). Fixed all references to use fully qualified role names. PR merged and deployed to vmaas-dev — verified the fix resolves the issue.
- **Cleaned up stale ESI routers (quota exceeded):** Cluster provisioning was blocked by a full router quota. Identified and deleted 5 orphaned routers with no matching cluster orders. Retried cluster creation — provisioning now gets past router setup but is blocked on agent availability.
- **Re-reviewed [fulfillment-service#481](https://github.com/osac-project/fulfillment-service/pull/481):** All five actionable findings from the prior review were addressed. Approved the installation guide PR.

## Risks & Challenges

- **Cluster provisioning blocked on agent availability:** No `fc430`-class agents are free — all three are bound to an existing cluster order. The remaining six agents have no resource class label. Need to either free up bound agents or relabel unassigned ones before cluster provisioning can complete.
- **Possible router cleanup bug:** Four of the five deleted stale routers had no matching cluster order in Kubernetes, suggesting that router resources may be orphaned if a cluster order is deleted before the deprovision job completes or if the job fails. The operator's finalizer-based cleanup may not be reliable for partial failures.

## Key Effort

Unblocking the CaaS demo environment on vmaas-dev by fixing a chain of issues — operator crash loop, tenant misconfiguration, missing Ansible role references, and stale infrastructure resource cleanup.
