---
layout: default
title: Daily Status - 2026-05-04
date: 2026-05-04
---

# Daily Status - 2026-05-04

## Accomplishments

- Re-published CaaS templates on the demo environment after syncing to latest main, confirming that cluster creation now uses explicit secret file parameters instead of template parameters
- Diagnosed and fixed the osac-operator crash loop on vmaas-dev caused by missing RBAC permissions for the PublicIP resource
- Fixed AuthConfig tenant assignment so that only the template-publisher service account receives the shared tenant, unblocking cluster creation
- Created and merged [osac-aap#282](https://github.com/osac-project/osac-aap/pull/282) to fix a broken Ansible role reference introduced by a recent Netris commit, which was causing both cluster create and delete jobs to fail
- Cleaned up 5 stale ESI routers that were exhausting the router quota on vmaas-dev, and cleaned orphaned agent labels and stuck cluster orders left behind by the broken delete path
- Diagnosed and partially remediated broken RBAC for a team member caused by orphaned cluster resources left from force-deleted clusters — identified 5 orphaned ManagedClusters as the root cause that must be removed to fully resolve the issue
- Re-reviewed and approved [fulfillment-service#481](https://github.com/osac-project/fulfillment-service/pull/481) (installation guide) — all prior feedback addressed
- Approved [osac-aap#268](https://github.com/osac-project/osac-aap/pull/268) (Netris CaaS fixes) after confirming the last open review item via Slack
- Reviewed [osac-aap#279](https://github.com/osac-project/osac-aap/pull/279) (BMH-based agent import) — posted findings including a silently skipped update path and missing job template configuration
- Identified that only 2 of the available bare-metal agents are registered in the ESI provisioning system, narrowing down viable agents for demo cluster creation
- Created a `/slack-read` command for reading and resolving Slack threads directly from the CLI

## Risks & Challenges

- **Demo cluster not yet created:** Multiple cascading issues (broken role references, router quota, stale agent bindings, agents not registered in ESI) have blocked demo cluster creation across several attempts. Currently waiting for a teammate to free up agents tomorrow.
- **RBAC still broken for a team member:** 5 orphaned ManagedClusters continuously recreate RBAC resources, preventing cleanup. Requires admin-level deletion to fully resolve.
- **Cluster deletion path has two independent blockers:** The Ansible role reference fix ([osac-aap#282](https://github.com/osac-project/osac-aap/pull/282)) is now merged, but the DNS cleanup step still fails because the IAM user is missing a required Route 53 permission. Until the IAM issue is fixed, cluster deletions will leave orphaned DNS records and require manual finalizer removal.
- **Orphaned ESI resources accumulating:** Force-deleting clusters bypasses the AAP cleanup flow, leaving routers and other ESI resources behind that must be manually cleaned.

## Key Effort

Spent the majority of the day unblocking the CaaS demo environment on vmaas-dev — diagnosing and fixing a chain of issues across RBAC, Ansible roles, tenant configuration, ESI resource quotas, and agent availability that prevented successful cluster creation.
