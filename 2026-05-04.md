---
layout: default
title: Daily Status - 2026-05-04
date: 2026-05-04
---

# Daily Status - 2026-05-04

## Accomplishments

- **CaaS demo environment updated**: Re-published AAP templates after merging [osac-aap#274](https://github.com/osac-project/osac-aap/pull/274), confirmed template parameters were cleaned up, and validated cluster creation with the new explicit secret/key file flags.
- **Fixed osac-operator crash loop on vmaas-dev**: Diagnosed an informer cache sync timeout caused by missing RBAC permissions for the PublicIP resource, patched the ClusterRole, and updated the operator to latest — all controllers now syncing.
- **Fixed AuthConfig tenant assignment**: Resolved a cluster creation failure where all service accounts were incorrectly assigned multiple tenants; corrected the configuration so only the template-publisher SA gets cross-tenant access.
- **Fixed Ansible role resolution failure** ([osac-aap#282](https://github.com/osac-project/osac-aap/pull/282)): Debugged AAP job failures caused by a short role name introduced in the Netris ([MGMT-23420](https://issues.redhat.com/browse/MGMT-23420)) commit; fixed all references to use fully qualified collection names. PR merged and deployed to vmaas-dev.
- **Cleaned up stale ESI routers**: Deleted 5 orphaned routers that were consuming the full router quota, unblocking new cluster creation. Also cleaned stale labels from agents and force-deleted 6 stuck ClusterOrders that were blocked by a Route 53 IAM permission issue in the delete flow.
- **Fixed broken RBAC from orphaned cluster resources**: Identified that force-deleting ClusterOrders left orphaned ClusterRoleBindings referencing non-existent ClusterRoles, which broke RBAC evaluation for a team member. Deleted 23 orphaned bindings and traced the root cause to 5 orphaned ManagedCluster resources that continuously reconcile RBAC.
- **Cleaned up stuck ClusterOrder and prepared agents**: Removed a stuck order from a crashed session, cleaned stale labels from agents, approved a pending agent, and verified the environment was ready for a demo cluster attempt.
- **Demo cluster creation — partial progress**: Created a demo cluster that progressed through L2/L3 networking successfully but failed at agent binding — one of two required agents is stuck in an unbinding state from a previous cluster and needs a host reboot. Coordinated with a teammate who will free up agents tomorrow.
- **Reviewed and approved** [fulfillment-service#481](https://github.com/osac-project/fulfillment-service/pull/481) (installation guide) — all 5 prior review findings addressed.
- **Reviewed and approved** [osac-aap#268](https://github.com/osac-project/osac-aap/pull/268) (Netris CaaS fixes) — confirmed last open item via Slack with the author.
- **Reviewed** [osac-aap#279](https://github.com/osac-project/osac-aap/pull/279) ([MGMT-24176](https://issues.redhat.com/browse/MGMT-24176): BMH-based agent import) — posted review with 3 important findings including a silent skip condition on BMH updates and missing job template settings.
- **Reviewed** [osac-aap#282](https://github.com/osac-project/osac-aap/pull/282) (own FQCN fix) — found a pre-existing duplicate task block and flagged it to the author.
- **Created a `/slack-read` command** for Claude Code that reads and parses Slack threads using local authentication.

## Risks & Challenges

- **Demo cluster creation blocked**: Only one healthy bare-metal agent is available out of the two required. A teammate's cluster is occupying the needed agents and will be freed tomorrow.
- **Orphaned ManagedClusters still causing RBAC issues**: Five orphaned ManagedCluster resources continuously reconcile RBAC objects, keeping a team member's access broken. Deleting them requires elevated privileges and has not been completed yet.
- **Route 53 IAM permission gap**: The DNS cleanup step in the cluster delete flow fails because the IAM user is missing a required Route 53 permission, leaving DNS records orphaned on cluster deletion.
- **Orphaned ESI resources on cluster deletion**: The FQCN bug caused both create and delete AAP jobs to fail before ESI cleanup, leaving routers and agent labels orphaned. The fix is merged but existing orphans need manual cleanup and the IAM issue is a separate blocker.

## Key Effort

Stabilizing the vmaas-dev CaaS demo environment by fixing a chain of deployment issues (operator crash, RBAC misconfig, Ansible role resolution, router quota exhaustion, orphaned cluster resources) to unblock end-to-end demo cluster creation.
