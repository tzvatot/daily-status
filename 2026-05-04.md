---
layout: default
title: Daily Status - 2026-05-04
date: 2026-05-04
---

# Daily Status - 2026-05-04

## Accomplishments

- **CaaS demo environment updated**: Re-published AAP templates after merging [osac-aap#274](https://github.com/osac-project/osac-aap/pull/274), verified template parameters were cleaned up, and confirmed cluster creation works with the new explicit secret file flags.
- **Fixed osac-operator crash loop on vmaas-dev**: Diagnosed missing RBAC permissions for the PublicIP resource and patched the operator's ClusterRole; updated operator to latest image — all controllers now syncing.
- **Fixed AuthConfig tenant assignment**: Corrected multi-tenant assignment logic so only the template-publisher service account receives the shared tenant, unblocking cluster creation.
- **Fixed broken Ansible role reference** ([osac-aap#282](https://github.com/osac-project/osac-aap/pull/282)): Resolved AAP job failures caused by a short role name introduced in the Netris work ([MGMT-23420](https://issues.redhat.com/browse/MGMT-23420)) by switching to fully qualified collection names. Deployed fix to vmaas-dev, verified it resolved the failure, and merged the PR.
- **Cleaned up stale ESI routers**: Deleted 5 orphaned routers that were consuming the full router quota, unblocking new cluster creation.
- **Cleaned up orphaned RBAC resources**: Identified 23 orphaned ClusterRoleBindings and associated ClusterRoles left behind by force-deleted clusters that were breaking RBAC for another user. Deleted accessible resources; root cause traced to 5 orphaned ManagedCluster objects that continuously reconcile RBAC.
- **Prepared bare-metal agents for demo cluster**: Labeled and approved available agents, cleaned stale labels from previously claimed agents, and coordinated with a teammate to free additional agents for tomorrow's demo.
- **Attempted demo cluster creation (two rounds)**: First attempt blocked by agents not registered in the bare-metal provisioning system; second attempt got further but failed because one agent was stuck in an unbinding state from a previous cluster. Cleaned up resources after each failed attempt.
- **Removed duplicate Ansible task** ([osac-aap#283](https://github.com/osac-project/osac-aap/pull/283)): Confirmed with the team that a duplicated task found during the FQCN review was an accidental leftover, removed it, and merged the PR.
- **Rebased and merged validation PR** ([fulfillment-service#475](https://github.com/osac-project/fulfillment-service/pull/475) — [MGMT-24213](https://issues.redhat.com/browse/MGMT-24213)): Rebased the cluster spec update validation PR onto latest main and merged it.
- **Reviewed 3 PRs**:
  - [fulfillment-service#481](https://github.com/osac-project/fulfillment-service/pull/481) — installation guide: re-reviewed, all findings addressed, approved.
  - [osac-aap#268](https://github.com/osac-project/osac-aap/pull/268) — Netris CaaS fixes: confirmed last open item via Slack, approved.
  - [osac-aap#279](https://github.com/osac-project/osac-aap/pull/279) — [MGMT-24176](https://issues.redhat.com/browse/MGMT-24176) BMH agent import: reviewed twice, key findings addressed, merged.
- **Created `/slack-read` command**: Built a Claude Code command to read and display Slack threads for easier cross-referencing during reviews.

## Risks & Challenges

- **Demo cluster not yet operational**: Two creation attempts failed due to bare-metal agent issues (missing ESI registration, stuck unbinding state). Only one healthy agent is currently available; need two. A teammate's cluster is freeing up agents tomorrow.
- **Orphaned ManagedClusters still blocking RBAC**: 5 orphaned ManagedCluster objects continuously re-create RBAC resources, keeping another user's access broken. Requires admin-level deletion to resolve.
- **ESI router leaks on failed cluster deletions**: Multiple paths cause routers to leak (AAP delete job failures due to Route 53 IAM permissions, force-deletion bypassing cleanup). Two orphaned routers still need manual cleanup. The Route 53 IAM issue (missing permission for the DNS manager service account) is a recurring blocker for cluster delete jobs.

## Key Effort

Spent the day stabilizing the vmaas-dev CaaS demo environment — fixing operator crashes, RBAC issues, Ansible role references, and resource leaks — working toward a successful end-to-end demo cluster creation.
