---
layout: default
title: Daily Status - 2026-05-04
date: 2026-05-04
---

# Daily Status - 2026-05-04

## Accomplishments
- **CaaS demo environment:** Re-published AAP templates with updated configuration from [osac-aap#274](https://github.com/osac-project/osac-aap/pull/274) — verified that cluster creation now works with explicit secret/key file flags and proper redaction
- **Fixed osac-operator crash loop on vmaas-dev:** Diagnosed missing RBAC permissions for the PublicIP resource, patched the cluster role, and updated the operator — all controllers now syncing
- **Fixed AuthConfig tenant assignment:** Corrected tenant assignment logic so only the template-publisher service account gets multi-tenant access — unblocked cluster creation
- **Fixed broken Ansible role reference:** Root-caused AAP job failures to a short-name role reference introduced in a recent Netris commit ([MGMT-23420](https://issues.redhat.com/browse/MGMT-23420)), fixed it to use fully qualified collection names in [osac-aap#282](https://github.com/osac-project/osac-aap/pull/282) — deployed, verified, and merged
- **Cleaned up stale ESI routers:** Deleted 5 orphaned routers blocking new cluster creation due to quota exhaustion, labeled unassigned bare-metal agents, and force-deleted 6 stuck ClusterOrders to restore the environment to a clean state (0 clusters, 2 free agents)
- **Reviewed [fulfillment-service#481](https://github.com/osac-project/fulfillment-service/pull/481):** Re-reviewed the installation guide PR — all prior findings addressed, approved
- **Approved [osac-aap#268](https://github.com/osac-project/osac-aap/pull/268):** Confirmed Netris API merge-style PUT behavior via Slack with Dan, closing the last open review item — approved
- **Reviewed [osac-aap#282](https://github.com/osac-project/osac-aap/pull/282):** Confirmed the FQCN fix is correct and minimal; flagged a pre-existing duplicate task to Dan Manor
- **Reviewed [osac-aap#279](https://github.com/osac-project/osac-aap/pull/279):** Reviewed BMH-based agent import playbook ([MGMT-24176](https://issues.redhat.com/browse/MGMT-24176)) — posted 3 important findings including silent update skipping and missing job template settings
- **Created `/slack-read` command:** Built a Claude Code command for reading Slack threads with user resolution

## Risks & Challenges
- **Orphaned ESI resources on cluster deletion:** The FQCN bug ([osac-aap#282](https://github.com/osac-project/osac-aap/pull/282)) caused both create and delete AAP jobs to fail, leaving routers, agent labels, and possibly other ESI resources orphaned. The fix is merged, but a separate Route 53 IAM permission issue (missing `route53:GetHostedZone` for the DNS manager) is still blocking the DNS cleanup step in the delete flow.
- **Stale router still in ESI:** One router from a force-deleted ClusterOrder still exists because finalizer removal bypassed AAP cleanup — needs manual investigation.

## Key Effort
Restored the vmaas-dev CaaS demo environment to a working state by fixing a chain of issues — operator crash, tenant misconfiguration, broken Ansible role references, and stale infrastructure resources — and progressed multiple PR reviews.
