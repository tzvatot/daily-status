---
layout: default
title: Daily Status - 2026-05-04
date: 2026-05-04
---

# Daily Status - 2026-05-04

## Accomplishments

- **CaaS demo environment updated**: Re-published AAP templates after merging [osac-aap#274](https://github.com/osac-project/osac-aap/pull/274), verified that cluster creation now accepts pull secret and SSH key as file arguments instead of template parameters.
- **Fixed osac-operator crash loop on vmaas-dev**: Diagnosed missing RBAC permissions for the PublicIP resource and patched the cluster role — operator is now running and all controllers are syncing.
- **Fixed AuthConfig tenant assignment**: Corrected tenant assignment logic so only the template-publisher service account receives the shared tenant, unblocking cluster creation.
- **Fixed Ansible role resolution failure**: Root-caused an AAP job failure to a short role name introduced in a recent Netris commit — fixed by using fully qualified collection names. PR [osac-aap#282](https://github.com/osac-project/osac-aap/pull/282) merged.
- **Cleaned up stale ESI routers**: Deleted 5 orphaned routers that were consuming the full router quota, freeing capacity for new cluster creation.
- **Fixed broken RBAC from orphaned cluster resources**: Identified that force-deleted clusters left orphaned ClusterRoleBindings referencing non-existent ClusterRoles, breaking RBAC for another user. Deleted 23 orphaned bindings and identified 5 orphaned ManagedClusters as the root cause of continuous re-creation.
- **Cleaned up stuck and failed cluster orders**: Force-deleted stuck ClusterOrders, removed stale labels from agents, and restored the environment to a clean state with 2 free agents ready for demo use.
- **Dropped duplicate Ansible task**: Confirmed with Dan Manor that a duplicate task in the cluster infrastructure playbook was an accidental leftover from a refactor. PR [osac-aap#283](https://github.com/osac-project/osac-aap/pull/283) merged.
- **Rebased and merged validation PR**: Rebased [fulfillment-service#475](https://github.com/osac-project/fulfillment-service/pull/475) ([MGMT-24213](https://issues.redhat.com/browse/MGMT-24213) — cluster spec update validation) onto latest main and merged.
- **Reviewed and approved 3 PRs**:
  - [fulfillment-service#481](https://github.com/osac-project/fulfillment-service/pull/481) — installation guide (re-review, all findings addressed, approved)
  - [osac-aap#268](https://github.com/osac-project/osac-aap/pull/268) — Netris CaaS fixes (approved after confirming Netris API behavior with Dan)
  - [osac-aap#279](https://github.com/osac-project/osac-aap/pull/279) — [MGMT-24176](https://issues.redhat.com/browse/MGMT-24176) BMH-based agent import (two review rounds, merged)
- **Created `/slack-read` command**: Built a Claude Code command to read and summarize Slack threads for use during reviews and investigations.

## Risks & Challenges

- **Demo cluster creation blocked**: Two attempts to create a demo cluster failed — first due to agents not being registered in the bare-metal provisioning system (ESI/Ironic), then due to an agent stuck in an unbinding state from a previous cluster. Only one healthy agent is currently available, but all templates require two. A colleague will free up agents tomorrow by deleting his cluster.
- **ESI router and DNS cleanup leak**: Multiple failure paths cause ESI routers to leak (role resolution bugs, Route 53 IAM permission gaps, force-deleted ClusterOrders skipping AAP cleanup). Two orphaned routers remain and the Route 53 IAM issue is still unresolved.
- **Orphaned ManagedClusters still need deletion**: Five orphaned ManagedClusters continuously re-create RBAC resources, keeping another user's permissions broken until they are deleted.

## Key Effort

Focused on stabilizing the vmaas-dev CaaS demo environment — diagnosing and fixing a chain of operator, RBAC, Ansible, and infrastructure issues blocking end-to-end cluster creation, while reviewing and merging several team PRs.
