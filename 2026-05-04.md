---
layout: default
title: Daily Status - 2026-05-04
date: 2026-05-04
---

# Daily Status - 2026-05-04

## Accomplishments

- **Re-published CaaS templates with updated configuration** — synced AAP to latest main including [osac-aap#274](https://github.com/osac-project/osac-aap/pull/274), re-ran publish-templates, and verified template parameters were correctly removed. Tested cluster creation with explicit secret/key file flags successfully.

- **Fixed osac-operator crash loop on vmaas-dev** — diagnosed informer cache sync timeout caused by missing RBAC permissions, patched the ClusterRole, and updated the operator to the latest image. All controllers now syncing.

- **Fixed AuthConfig tenant assignment** — resolved cluster creation failure caused by all service accounts incorrectly receiving multiple tenant assignments. Corrected the configuration so only the template-publisher service account gets the shared tenant.

- **Fixed manage_agents role-not-found error** ([osac-aap#282](https://github.com/osac-project/osac-aap/pull/282)) — debugged AAP job failure caused by a short role name reference introduced in the Netris commit ([MGMT-23420](https://issues.redhat.com/browse/MGMT-23420)). Fixed by using fully qualified collection names. PR merged and deployed to vmaas-dev.

- **Cleaned up stale ESI routers blocking cluster creation** — deleted 5 orphaned routers that were consuming the full router quota, preventing new cluster provisioning. Also labeled and prepared bare-metal agents for demo cluster use.

- **Resolved broken RBAC caused by orphaned cluster resources** — force-deleting ClusterOrders (to work around a stuck delete flow) left orphaned ClusterRoleBindings referencing non-existent ClusterRoles, which broke API server RBAC evaluation for a team member. Identified 23 orphaned bindings across 5 deleted clusters and began cleanup. Root cause traced to orphaned ManagedCluster resources that continuously reconcile RBAC.

- **Cleaned up stuck ClusterOrder and prepared agents for retry** — deleted the stuck `order-wqxcw` from a crashed session, cleaned stale labels from agents, and approved a previously unapproved agent to bring the free agent count to 2.

- **Attempted demo cluster creation** — cluster provisioned and AAP job progressed through networking successfully, but failed because one of the two required agents was stuck in an unbinding state from a previous cluster. Coordinated with Ajamias on Slack to free up agents for tomorrow.

- **Reviewed and approved** [fulfillment-service#481](https://github.com/osac-project/fulfillment-service/pull/481) (installation guide) — all 5 actionable findings from prior review were addressed.

- **Reviewed and approved** [osac-aap#268](https://github.com/osac-project/osac-aap/pull/268) (Netris CaaS fixes) — confirmed last open item via Slack with Dan regarding Netris API behavior.

- **Reviewed** [osac-aap#282](https://github.com/osac-project/osac-aap/pull/282) (FQCN fix) — confirmed fix is correct and minimal. Found a pre-existing duplicate task in the create playbook and reached out to Dan to clarify if it's intentional.

- **Reviewed** [osac-aap#279](https://github.com/osac-project/osac-aap/pull/279) ([MGMT-24176](https://issues.redhat.com/browse/MGMT-24176): BMH-based agent import) — posted review with 3 important findings including silently skipped updates, missing job template settings, and broken local testing docs.

- **Created `/slack-read` command** — built a Claude Code command to read and summarize Slack threads using API authentication.

## Risks & Challenges

- **Demo cluster not yet ready** — only 1 healthy bare-metal agent available out of the 2 required. A teammate's cluster is occupying the needed agents and will be freed tomorrow.
- **Orphaned ManagedClusters still causing RBAC issues** — 5 orphaned ManagedClusters continuously reconcile RBAC resources, blocking cleanup. Deletion requires elevated permissions and hasn't been completed yet.
- **Cluster deletion flow has two independent blockers** — the FQCN bug (now fixed via [osac-aap#282](https://github.com/osac-project/osac-aap/pull/282)) was causing delete jobs to fail at agent management, and a separate Route 53 IAM permission gap is blocking DNS cleanup during deletion.
- **Force-deleting clusters leaves orphaned resources** — bypassing finalizers skips AAP cleanup, leaving stale routers, agent labels, RBAC bindings, and possibly other ESI resources that require manual intervention.

## Key Effort

Stabilizing the vmaas-dev demo environment by fixing a chain of cascading failures — operator crash, role resolution, tenant assignment, stale resource cleanup, and RBAC breakage — to enable end-to-end CaaS cluster provisioning for the demo.
