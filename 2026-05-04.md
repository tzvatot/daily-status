---
layout: default
title: Daily Status - 2026-05-04
date: 2026-05-04
---

# Daily Status - 2026-05-04

## Accomplishments

- **Re-published CaaS templates with updated configuration** ([osac-aap#274](https://github.com/osac-project/osac-aap/pull/274)): Synced the AAP project to latest, re-ran the publish-templates job, and verified that sensitive template parameters were removed. Tested cluster creation with explicit secret/key file flags — working as expected.

- **Fixed osac-operator crash loop on vmaas-dev**: Diagnosed a cache sync timeout caused by missing RBAC permissions for the PublicIP resource. Patched the ClusterRole and updated the operator image — all controllers now syncing successfully.

- **Fixed AuthConfig tenant assignment**: Resolved a cluster creation failure where all service accounts were incorrectly assigned to multiple tenants. Corrected the configuration so only the template-publisher SA gets the shared tenant.

- **Fixed Ansible role resolution failure** ([osac-aap#282](https://github.com/osac-project/osac-aap/pull/282)): Debugged AAP job failures caused by a role referenced by short name instead of its fully qualified collection name — a regression introduced in the Netris ([MGMT-23420](https://issues.redhat.com/browse/MGMT-23420)) commit. Fixed all references, deployed to vmaas-dev, verified the fix, and merged the PR.

- **Cleaned up stale ESI routers blocking cluster creation**: Identified and deleted 5 orphaned routers that were exhausting the router quota. Cleaned up stale agent labels, deleted failed ClusterOrders (including force-removing finalizers from stuck resources), and restored the environment to a clean state with free agents available.

- **Investigated and partially resolved broken RBAC from orphaned cluster resources**: Force-deleting ClusterOrders left orphaned ClusterRoleBindings referencing non-existent ClusterRoles, which broke RBAC for a team member. Identified 23 orphaned bindings across 5 deleted clusters. Root cause: orphaned ManagedCluster resources continuously reconcile RBAC — these need to be deleted to permanently fix the issue.

- **Deleted stuck ClusterOrder and prepared agents for retry**: Cleaned up a stuck order from a crashed session, removed stale labels from agents, approved an unapproved agent, and verified 2 free agents available for demo cluster creation.

- **Attempted demo cluster creation (two rounds)**: First attempt failed because candidate agents were not registered in ESI. Found alternative agents and retried — second attempt progressed through networking but failed because one agent is stuck in an unbinding state from a previous cluster (needs a host reboot).

- **Reviewed and approved** [fulfillment-service#481](https://github.com/osac-project/fulfillment-service/pull/481) (installation guide) — all actionable findings from prior review were addressed.

- **Reviewed and approved** [osac-aap#268](https://github.com/osac-project/osac-aap/pull/268) (Netris CaaS fixes) — confirmed the last open item via Slack with the author.

- **Reviewed** [osac-aap#279](https://github.com/osac-project/osac-aap/pull/279) ([MGMT-24176](https://issues.redhat.com/browse/MGMT-24176): BMH-based agent import) — posted review with 3 important findings including silently skipped updates and missing job template settings.

- **Reviewed** [osac-aap#282](https://github.com/osac-project/osac-aap/pull/282) (FQCN fix) — confirmed the fix is correct and flagged a pre-existing duplicate task block to the author.

- **Created a `/slack-read` command** for Claude Code to read and parse Slack threads using stored authentication credentials.

## Risks & Challenges

- **Demo cluster creation blocked**: Only one healthy agent is available but all templates require two. One agent is stuck in an unbinding state needing a physical host reboot; the other candidates are not registered in ESI. Either a single-node template or a rebooted agent is needed to proceed.
- **RBAC still broken for a team member**: 5 orphaned ManagedCluster resources keep reconciling ClusterRoleBindings that reference non-existent ClusterRoles. These ManagedClusters need to be deleted with elevated privileges to permanently fix the issue.
- **Cluster deletion flow has two independent blockers**: The FQCN bug (now fixed via [osac-aap#282](https://github.com/osac-project/osac-aap/pull/282)) was causing delete jobs to fail, and a separate IAM permission gap (missing Route 53 permission for the DNS manager) blocks the DNS cleanup step. Orphaned ESI resources (routers, agent labels) accumulate until both are resolved.

## Key Effort

Focused on unblocking the CaaS demo environment on vmaas-dev — diagnosing and fixing a chain of failures (operator crash, tenant misconfiguration, Ansible role resolution, stale ESI resources, broken RBAC) while also reviewing several team PRs.
