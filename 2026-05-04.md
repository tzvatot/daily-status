---
layout: default
title: Daily Status - 2026-05-04
date: 2026-05-04
---

# Daily Status - 2026-05-04

## Accomplishments
- **CaaS demo environment update:** Re-published templates with [osac-aap#274](https://github.com/osac-project/osac-aap/pull/274) changes, confirmed template parameters were cleaned up, and verified cluster creation works with the new explicit secret/key file flags.
- **Fixed osac-operator crash loop on vmaas-dev:** Diagnosed a missing RBAC permission causing informer cache sync timeouts, patched the cluster role, and updated the operator to latest — all controllers now syncing.
- **Fixed AuthConfig tenant assignment:** Resolved a misconfiguration where all service accounts were incorrectly assigned to multiple tenants, causing cluster creation to fail. Corrected tenant scoping so only the template publisher has cross-tenant access.
- **Fixed Ansible role resolution failure:** Debugged and fixed a broken role reference introduced by a recent Netris commit — role was referenced by short name instead of fully-qualified collection name. Created [osac-aap#282](https://github.com/osac-project/osac-aap/pull/282), deployed the fix to vmaas-dev, verified it works, and got it merged.
- **Cleaned up stale ESI routers:** Router quota was full due to orphaned routers from failed cluster deletions (caused by the role resolution bug above). Deleted 5 stale routers, cleaned up orphaned agent labels, force-deleted 6 stuck ClusterOrders, and restored the environment to a clean state with free agents available.
- **Discovered DNS cleanup blocker:** Cluster delete jobs fail at the DNS step because the IAM user is missing a Route 53 permission — separate issue from the role fix, needs IAM update or workaround.
- **Reviewed [fulfillment-service#481](https://github.com/osac-project/fulfillment-service/pull/481):** Re-reviewed the installation guide PR, confirmed all prior feedback was addressed, and approved.
- **Reviewed [osac-aap#268](https://github.com/osac-project/osac-aap/pull/268):** Approved Netris CaaS fixes after confirming the last open question about API behavior via Slack with Dan.
- **Reviewed [osac-aap#282](https://github.com/osac-project/osac-aap/pull/282):** Reviewed the FQCN fix and flagged a pre-existing duplicate task in the create playbook — reached out to Dan to confirm if intentional.
- **Reviewed [osac-aap#279](https://github.com/osac-project/osac-aap/pull/279):** Reviewed the BMH-based agent import PR ([MGMT-24176](https://issues.redhat.com/browse/MGMT-24176)), posted 3 important findings including a silent skip on BMH updates and missing job template settings.
- **Created `/slack-read` command:** Built a Claude Code slash command to read and summarize Slack threads directly from the CLI.

## Risks & Challenges
- Cluster delete flow is blocked by a Route 53 IAM permission gap — the DNS cleanup step fails, which could leave orphaned cloud resources on every cluster deletion until resolved.
- Force-deleting ClusterOrders (via finalizer removal) skips AAP cleanup, leaving orphaned ESI resources like routers that must be manually cleaned.

## Key Effort
Unblocking the CaaS demo environment on vmaas-dev by fixing a chain of failures — operator crash, tenant misconfiguration, Ansible role resolution, and stale resource cleanup — to get back to a clean state for demo recording.
