---
layout: default
title: Daily Status - 2026-05-04
date: 2026-05-04
---

# Daily Status - 2026-05-04

## Accomplishments

- **Re-published CaaS templates with updated configuration** — synced AAP to latest main including [osac-aap#274](https://github.com/osac-project/osac-aap/pull/274), re-ran publish-templates, and verified that sensitive template parameters were removed. Tested cluster creation with explicit secret file flags — working correctly.
- **Fixed osac-operator crash loop on vmaas-dev** — diagnosed informer cache sync timeout caused by missing RBAC permissions for the PublicIP resource. Patched the ClusterRole and updated the operator to latest image; all controllers now syncing.
- **Fixed AuthConfig tenant assignment** — resolved cluster creation failure where all service accounts were incorrectly assigned to multiple tenants. Corrected tenant scoping so only the template-publisher SA gets cross-tenant access.
- **Fixed Ansible role resolution failure and merged the fix** — debugged AAP job failure caused by a short role name that couldn't be resolved after a recent refactor. Fixed by switching to fully-qualified collection names. Deployed to vmaas-dev, verified, and merged [osac-aap#282](https://github.com/osac-project/osac-aap/pull/282).
- **Cleaned up stale ESI routers blocking cluster creation** — router quota was exhausted by 5 orphaned routers from deleted clusters. Removed them, freeing capacity for new cluster provisioning.
- **Resolved agent availability issues for demo cluster** — labeled and approved bare-metal agents, cleaned up stale claims from failed orders, deleted stuck ClusterOrders and orphaned ManagedClusters. Environment restored to clean state with 2 free agents.
- **Diagnosed and partially fixed broken RBAC from orphaned cluster resources** — force-deleted ClusterOrders had left orphaned ClusterRoleBindings referencing non-existent ClusterRoles, breaking RBAC for a team member. Identified 5 orphaned ManagedClusters as the root cause of continuous RBAC reconciliation; full fix requires deleting those ManagedClusters.
- **Attempted demo cluster creation (twice)** — first attempt failed due to agents not registered in ESI; second attempt failed because one agent was stuck in an unbinding state from a previous cluster. Currently blocked on agent availability — a colleague will free up agents tomorrow.
- **Reviewed and approved 3 PRs:**
  - [fulfillment-service#481](https://github.com/osac-project/fulfillment-service/pull/481) — installation guide, all review findings addressed
  - [osac-aap#268](https://github.com/osac-project/osac-aap/pull/268) — Netris CaaS fixes, confirmed API behavior with author
  - [osac-aap#279](https://github.com/osac-project/osac-aap/pull/279) — BMH-based agent import playbook, posted review with 3 important findings
- **Created a Slack thread reader command** for the Claude Code CLI to streamline reading Slack conversations during reviews and debugging.

## Risks & Challenges

- **Demo cluster creation still blocked** — only one healthy bare-metal agent is available; all templates require two. Waiting for a colleague to free up agents from their cluster tomorrow.
- **Orphaned RBAC resources still breaking access for a team member** — 5 orphaned ManagedClusters continuously reconcile ClusterRoleBindings that reference missing ClusterRoles. Requires privileged deletion to fully resolve.
- **Cluster deletion flow has two independent issues** — the role resolution bug (now fixed via [osac-aap#282](https://github.com/osac-project/osac-aap/pull/282)) was causing delete jobs to fail before cleaning up ESI resources, and a separate IAM permission gap (missing Route 53 permissions for the DNS manager) blocks the DNS cleanup step. Both leave orphaned infrastructure on failed deletions.
- **Force-deleting ClusterOrders bypasses cleanup** — removing finalizers to unstick resources skips the AAP-driven teardown, leaving routers, agent labels, and RBAC artifacts orphaned. This was a recurring pattern today.

## Key Effort

Stabilizing the vmaas-dev environment by fixing a chain of blocking issues (operator crash, role resolution, tenant assignment, stale resources, broken RBAC) to enable CaaS demo cluster creation.
