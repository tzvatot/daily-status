---
layout: default
title: Daily Status - 2026-05-04
date: 2026-05-04
---

# Daily Status - 2026-05-04

## Accomplishments
- **CaaS demo environment:** Re-published AAP templates after [osac-aap#274](https://github.com/osac-project/osac-aap/pull/274) merge, verified template parameters were removed, and confirmed cluster creation works with the new explicit secret/key file flags
- **Fixed osac-operator crash loop on vmaas-dev:** Diagnosed missing RBAC permissions for the PublicIP resource and patched the operator ClusterRole to restore normal operation
- **Fixed AuthConfig tenant assignment:** Corrected tenant assignment logic so only the template-publisher service account gets the shared tenant, unblocking cluster creation
- **Fixed manage_agents role resolution failure:** Root-caused an AAP job failure to a short role name introduced in the Netris ([MGMT-23420](https://issues.redhat.com/browse/MGMT-23420)) work, fixed by using fully qualified collection names in [osac-aap#282](https://github.com/osac-project/osac-aap/pull/282) — PR merged and deployed
- **Cleaned up stale ESI resources on vmaas-dev:** Deleted 5 orphaned routers to resolve a router quota block, cleaned up stale agent labels from failed cluster orders, force-deleted 6 stuck ClusterOrders, and restored the environment to a clean state with free agents available
- **Identified agents not registered in ESI:** Cross-referenced all agents against the ESI Ironic node list and found two alternative agents that are both the correct hardware type and present in ESI for demo cluster creation
- **PR reviews:**
  - Re-reviewed and approved [fulfillment-service#481](https://github.com/osac-project/fulfillment-service/pull/481) (installation guide) — all prior findings addressed
  - Approved [osac-aap#268](https://github.com/osac-project/osac-aap/pull/268) (Netris CaaS fixes) after confirming the Netris API merge-style PUT behavior
  - Reviewed [osac-aap#282](https://github.com/osac-project/osac-aap/pull/282) (FQCN fix) — flagged a pre-existing duplicate task for follow-up with the author
  - Reviewed [osac-aap#279](https://github.com/osac-project/osac-aap/pull/279) ([MGMT-24176](https://issues.redhat.com/browse/MGMT-24176): BMH agent import) — posted 3 important findings including silent update skipping and missing job template settings
- **Created `/slack-read` CLI command** for reading Slack threads directly from Claude Code

## Risks & Challenges
- **Demo cluster not yet operational:** Agents initially selected for the demo are not registered in ESI Ironic; two alternative agents have been identified but still need label cleanup and approval before retrying
- **Cluster deletion blocked by DNS permissions:** The IAM user for DNS management is missing Route 53 permissions, causing the delete flow to fail on DNS cleanup — this is a separate issue from the FQCN fix and blocks full lifecycle testing
- **Orphaned ESI resources on failed deletions:** The FQCN bug caused both create and delete AAP jobs to fail, leaving routers, agent labels, and possibly other ESI resources orphaned; now fixed for new operations but historical orphans (e.g., one remaining router) still need manual cleanup

## Key Effort
Restoring the vmaas-dev CaaS demo environment to a working state by fixing a chain of cascading issues — operator RBAC, tenant assignment, Ansible role resolution, router quota exhaustion, and agent/ESI registration mismatches.
