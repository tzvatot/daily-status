---
layout: default
title: Daily Status - 2026-05-04
date: 2026-05-04
---

# Daily Status - 2026-05-04

## Accomplishments

- **CaaS demo environment**: Re-published AAP templates after merging [osac-aap#274](https://github.com/osac-project/osac-aap/pull/274), confirmed template parameters were removed, and verified cluster creation works with the new explicit secret/key file flags.
- **Fixed osac-operator crash loop on vmaas-dev**: Diagnosed missing RBAC permissions for the PublicIP resource, patched the ClusterRole, and updated the operator — all controllers now syncing.
- **Fixed AuthConfig tenant assignment**: Resolved a misconfiguration where all service accounts were incorrectly assigned to multiple tenants, which blocked cluster creation. Only the template-publisher SA now gets the shared tenant.
- **Fixed manage_agents role resolution failure**: Root-caused an AAP job failure to a short role name introduced in a Netris commit ([MGMT-23420](https://issues.redhat.com/browse/MGMT-23420)), fixed it by using fully-qualified collection names, deployed and verified on vmaas-dev. PR: [osac-aap#282](https://github.com/osac-project/osac-aap/pull/282) (merged).
- **Cleaned up stale ESI routers**: Deleted 5 orphaned routers that were consuming the full router quota, blocking new cluster creation. Orphaned resources were left behind because the earlier role resolution bug caused delete jobs to fail silently.
- **Cleaned up stale clusters and agents**: Removed all failed demo clusters and stale agent labels. Worked around stuck delete jobs (blocked by a missing IAM permission for Route 53 DNS cleanup) by removing finalizers. Environment is now clean with 2 free agents available.
- **Identified agent/ESI registration gap**: Discovered that the two available agents are not registered in ESI Ironic, and located two alternative agents that are both the correct hardware type and present in ESI. Next step is to label and approve them for use.
- **Reviewed [fulfillment-service#481](https://github.com/osac-project/fulfillment-service/pull/481)**: Re-reviewed the installation guide PR, confirmed all 5 prior findings were addressed, approved.
- **Reviewed and approved [osac-aap#268](https://github.com/osac-project/osac-aap/pull/268)**: Netris CaaS fixes — confirmed the last open item (merge-style PUT behavior) via Slack with Dan, approved.
- **Reviewed [osac-aap#279](https://github.com/osac-project/osac-aap/pull/279)** ([MGMT-24176](https://issues.redhat.com/browse/MGMT-24176)): BMH-based agent import playbook — posted review with 3 important findings including silent update skipping and missing job template settings.
- **Reviewed [osac-aap#282](https://github.com/osac-project/osac-aap/pull/282)**: Confirmed the FQCN fix is correct; flagged a pre-existing duplicate task to Dan for clarification.
- **Created `/slack-read` command**: Built a Claude Code slash command for reading Slack threads, enabling AI-assisted review of Slack discussions.

## Risks & Challenges

- **Route 53 IAM permission gap**: The DNS cleanup step in the cluster delete flow fails because the `innabox-dns-manager` IAM user is missing a required Route 53 permission. This is a separate blocker from the FQCN fix and will cause orphaned DNS records on cluster deletion until resolved.
- **Orphaned ESI resources after force-deletes**: Force-deleting ClusterOrders via finalizer removal skips the AAP cleanup flow, leaving routers and other ESI resources behind. At least one stale router remains from this workaround.
- **Agent availability**: Only 2 of the available agents are both the correct hardware and registered in ESI. One has a stale label and the other needs approval before they can be used for the demo cluster.

## Key Effort

Stabilized the vmaas-dev CaaS demo environment by fixing multiple cascading failures (operator crash, role resolution, tenant assignment, router quota, agent registration) to unblock end-to-end cluster creation for the CaaS API demo.
