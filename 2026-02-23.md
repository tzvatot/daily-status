---
layout: default
title: Daily Status - 2026-02-23
date: 2026-02-23
---

# Daily Status - 2026-02-23

## Accomplishments

- **[MGMT-23031](https://issues.redhat.com/browse/MGMT-23031) — Changed default provisioning provider from EDA to AAP** in osac-operator (`cmd/main.go`, config sample). All tests pass, linter clean. Rebased main on upstream.

- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — Fixed re-provision logic in operator**: added `needsProvisionJob()` receiver to handle spec changes (e.g. runStrategy) triggering new provision jobs. Deployed to vmaas-dev.

- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — Fixed E2E test**: updated `spec.running` → `spec.runStrategy` checks for Always→Halted→Always cycle. Removed VMI-level checks (KubeVirt internals) in favor of VM-level printableStatus.

- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — Found and fixed phase regression bug**: phase incorrectly dropped to `Starting` on re-provision of a Running instance. Mitigated by preserving `Running` phase when already provisioned.

- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — Found and fixed re-provision timeout bug**: playbook timed out waiting for Ready on Halted VMs. Fix: skip "Wait for VM to be ready" when `vm_run_strategy == "Halted"` (PR: osac-templates#8).

- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — Re-vendored osac.templates in osac-aap** (PR osac-aap#205 merged, EE image rebuilt).

- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — Found and fixed deprovision spec validation error**: finalizer role's server-side apply rejected by immutability webhook; switched to JSON merge patch (PR: osac-aap#206).

- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — Found and fixed tenant-not-ready requeue bug**: `handleUpdate()` returned "done" instead of requeueing; fixed to use `RequeueAfter: 10s`.

- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — Added `Updating` phase to ComputeInstance API**: new `COMPUTE_INSTANCE_STATE_UPDATING = 5` proto enum in fulfillment-api (PR#61) and fulfillment-service (PR#306). Operator state machine refactored with resolver-per-phase pattern, comprehensive unit tests, all 110 tests pass (PR: osac-operator#114).

## Risks & Challenges

- E2E test for [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) still re-running with all operator fixes deployed — results pending.
- BSR publication of fulfillment-api and private-api protos still pending; operator uses literal `ComputeInstanceState(5)` until generated `.pb.go` is updated with named constant.
- fulfillment-api PR#61 awaiting review.

## Key Effort

Primary focus was end-to-end validation and bug fixing for the [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance API enhancement, including re-provision logic, phase state machine refactor, and cross-repo proto updates for the new `Updating` phase.
