---
layout: default
title: Daily Status - 2026-02-23
date: 2026-02-23
---

# Daily Status - 2026-02-23

## Accomplishments

- **[MGMT-23031](https://issues.redhat.com/browse/MGMT-23031) — Changed default provisioning provider from EDA to AAP** in osac-operator (`cmd/main.go`, config sample). All tests pass, linter clean.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — Fixed re-provision logic in operator**: added `needsProvisionJob()` receiver to handle spec changes (e.g. runStrategy) triggering new provision jobs.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — Fixed E2E test**: updated `spec.running` → `spec.runStrategy` checks for Always/Halted cycle.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — Fixed phase regression bug**: phase no longer drops to `Starting` on re-provision when instance is already `Running`.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — Fixed re-provision timeout when runStrategy=Halted**: skip "Wait for VM to be ready" step for Halted VMs (PR: osac-templates#8).
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — Removed VMI-level checks from E2E test**: VM-level printableStatus is sufficient; VMI checks were KubeVirt internals.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — Re-vendored osac.templates in osac-aap** (PR: osac-aap#205 merged, EE image rebuilt).
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — Fixed deprovision spec validation error**: switched finalizer role from server-side apply to JSON merge patch (PR: osac-aap#206).
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — Fixed tenant-not-ready requeue bug**: `handleUpdate()` now returns `RequeueAfter` instead of signaling "done".
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — Fixed tenant GC race causing stuck provisioning**: detect terminating tenants, clear reference, and requeue instead of updating a tenant with `DeletionTimestamp` set.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — E2E test passed**: all 7 checks passed (create, runStrategy Halted/Always, immutability enforcement, cleanup).
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — Removed `ensureBackwardCompatibility()`**: templates now read explicit fields directly; removed function, call sites, and 303 lines of test code.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — Added `Updating` phase to ComputeInstance API**: new proto enum value in fulfillment-api (PR#61) and fulfillment-service (PR#306); state machine refactor with resolver-per-phase pattern and comprehensive tests in osac-operator (PR#114). All 110 tests pass.

## Risks & Challenges

- **Blocker on backward-compat removal**: `osac-aap` role `extract_compute_instance_template_info` still reads `templateParameters | from_json` — provision jobs fail until this role is updated to read explicit fields (`spec.cores`, `spec.memoryGiB`, etc.).
- **Pending BSR publication**: fulfillment-api and private-api protos need BSR publish before operator can replace the `ComputeInstanceState(5)` literal with a named constant.

## Key Effort

Drove [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance API enhancement through E2E validation — discovering and fixing six bugs across operator, AAP roles, and templates — and introduced the `Updating` phase across the fulfillment API, service, and operator.
