---
layout: default
title: Daily Status - 2026-02-23
date: 2026-02-23
---

# Daily Status - 2026-02-23

## Accomplishments

- **[MGMT-23031](https://issues.redhat.com/browse/MGMT-23031) — Changed default provisioning provider from EDA to AAP**
  - Updated `cmd/main.go` and `config/samples/osac-config-secret.yaml` to reflect new default
  - All unit tests pass, linter clean, pre-commit hooks pass
  - Rebased main on upstream; resolved cherry-pick conflict for already-merged commit

- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — ComputeInstance API Enhancement: E2E Validation (continued)**
  - Fixed re-provision logic in operator — added `needsProvisionJob()` to handle spec changes (e.g. runStrategy) triggering new provision jobs
  - Deployed updated operator to vmaas-dev
  - Fixed E2E test to check `spec.runStrategy` instead of deprecated `spec.running`
  - Found and mitigated phase regression bug — phase was dropping to `Starting` on re-provision of a Running instance; kept phase as `Running` when already successfully provisioned
  - Found and fixed re-provision timeout bug — playbook timed out when `runStrategy=Halted` because VM never becomes ready; submitted fix to skip readiness wait (osac-templates PR #8, merged)
  - Removed unnecessary VMI-level checks from E2E test; VM-level status is sufficient
  - Re-vendored osac-templates in osac-aap (PR #205)
  - E2E test pending — blocked on AAP PR merge and EE image rebuild

## Risks & Challenges

- E2E validation for [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) is blocked until AAP PR #205 merges and the EE image (`ghcr.io/osac-project/osac-aap:latest`) is rebuilt
- Phase regression fix is a mitigation; full solution requires introducing an `Updating` phase in the CRD and fulfillment API (separate work item)

## Key Effort

Continued E2E validation work for the ComputeInstance API enhancement, fixing multiple operator bugs discovered during testing (re-provision logic, phase regression, playbook timeout).
