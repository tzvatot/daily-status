---
layout: default
title: Daily Status - 2026-02-23
date: 2026-02-23
---

# Daily Status - 2026-02-23

## Accomplishments

- **[MGMT-23031](https://issues.redhat.com/browse/MGMT-23031) - Changed default provisioning provider from EDA to AAP** in osac-operator (`cmd/main.go` and config sample updated). All unit tests pass, linter clean.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) - Fixed re-provision logic in operator**: added `needsProvisionJob()` receiver so spec changes (e.g. runStrategy) correctly trigger new provision jobs. Regenerate `templateParameters` on each provision.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) - Deployed operator to vmaas-dev** with re-provision fix and subsequent phase regression fix.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) - Fixed E2E test** to check `spec.runStrategy` instead of deprecated `spec.running`.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) - Found and mitigated phase regression bug**: phase was dropping to `Starting` on re-provision of a Running instance. Mitigated by preserving `Running` phase when already provisioned; follow-up needed for proper `Updating` phase in CRD + fulfillment API.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) - Found and fixed re-provision timeout bug**: playbook timed out waiting for VM Ready when runStrategy=Halted. Fixed by skipping readiness wait when `vm_run_strategy == "Halted"` (template PR submitted).
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) - Removed VMI-level checks from E2E test**, relying on VM-level printableStatus instead of KubeVirt internals.

## Risks & Challenges

- E2E test for [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) is blocked on template PR merge and EE image rebuild.
- Phase regression fix is a mitigation only; proper `Updating` phase requires CRD and fulfillment API changes (separate work item).

## Key Effort

Continued E2E validation work for [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance API enhancement, fixing operator re-provision logic, phase regression, and playbook timeout bugs discovered during testing.
