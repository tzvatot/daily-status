---
layout: default
title: Daily Status - 2026-02-23
date: 2026-02-23
---

# Daily Status - 2026-02-23

## Accomplishments
- **[MGMT-23031](https://issues.redhat.com/browse/MGMT-23031) – Changed default provisioning provider from EDA to AAP** in osac-operator (`cmd/main.go`, config sample). All tests pass, linter clean. Branch: `feature/default-provider-aap-MGMT-23031`.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) – Fixed re-provision logic in operator**: added `needsProvisionJob()` to detect spec changes (e.g. runStrategy) and trigger re-provisioning; regenerate `templateParameters` on each provision. 43.5% test coverage, linter clean.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) – Fixed E2E test**: updated from `spec.running` to `spec.runStrategy` checks for the Always→Halted→Always cycle.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) – Found and mitigated phase regression bug**: phase was dropping to `Starting` on re-provision of a Running instance; mitigated by preserving `Running` phase when already provisioned. Follow-up needed for a proper `Updating` phase (requires API changes).
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) – Found and fixed re-provision timeout bug**: Halted VM never becomes ready, causing playbook timeout. Fixed by skipping "Wait for VM to be ready" when `vm_run_strategy == "Halted"` (osac-templates PR#8 merged).
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) – Removed VMI-level checks from E2E test**, relying on VM-level `printableStatus` instead of KubeVirt internals.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) – Re-vendored osac.templates in osac-aap** (PR#205 merged), rebuilt EE image.
- **Deployed updated operator** (`quay.io/etabak/cloudkit-operator:mgmt-23103-phase-fix`) to vmaas-dev; E2E test re-running with updated EE image.

## Risks & Challenges
- Phase regression on re-provision (drops to `Starting` instead of staying `Running`) is mitigated but not fully resolved — proper fix requires introducing an `Updating` phase in the CRD and fulfillment API (separate work item).
- E2E test still in progress with the updated EE image; results pending.

## Key Effort
Continued E2E validation and bug-fixing for ComputeInstance API enhancements ([MGMT-23103](https://issues.redhat.com/browse/MGMT-23103)), including re-provision logic, phase regression, and playbook timeout fixes.
