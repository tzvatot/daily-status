---
layout: default
title: Daily Status - 2026-02-23
date: 2026-02-23
---

# Daily Status - 2026-02-23

## Accomplishments
- **[MGMT-23031](https://issues.redhat.com/browse/MGMT-23031) — Changed default provisioning provider from EDA to AAP** in osac-operator (`cmd/main.go` and config sample updated). All tests pass, linter clean.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — Fixed re-provision logic in operator**: added `needsProvisionJob()` to handle spec changes (e.g. runStrategy) triggering new provision jobs. Regenerate `templateParameters` on each provision.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — Deployed operator to vmaas-dev** with re-provision and phase regression fixes.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — Fixed E2E test** to check `spec.runStrategy` instead of deprecated `spec.running`.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — Found and mitigated phase regression bug**: phase was dropping to `Starting` on re-provision of a Running instance; fixed to preserve `Running` phase when already provisioned.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — Found and fixed re-provision timeout bug**: playbook timed out waiting for VM Ready when runStrategy=Halted; submitted PR to skip wait step in that case.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — Removed VMI-level checks from E2E test**, relying on VM-level printableStatus instead of KubeVirt internals.

## Risks & Challenges
- E2E test for [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) is blocked — requires template PR (osac-templates#8) to be merged and EE image rebuilt before it can run.
- Phase regression fix is a mitigation; full fix requires introducing an `Updating` phase in the CRD and fulfillment API (separate work item).

## Key Effort
Continued E2E validation work for ComputeInstance API enhancements, fixing re-provision logic, phase regression, and playbook timeout bugs discovered during testing.
