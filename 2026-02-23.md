---
layout: default
title: Daily Status - 2026-02-23
date: 2026-02-23
---

# Daily Status - 2026-02-23

## Accomplishments

- **[MGMT-23031](https://issues.redhat.com/browse/MGMT-23031) Default Provider Change: EDA to AAP** — Changed default provisioning provider from EDA to AAP in `cmd/main.go` and updated config sample documentation. All unit tests pass, linter clean, pre-commit hooks pass.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance API Enhancement: E2E Validation** — Fixed re-provision logic in operator so spec changes (e.g. runStrategy) correctly trigger new provision jobs. Added `needsProvisionJob()` receiver for proper lifecycle handling.
- Pushed and deployed operator to vmaas-dev (`quay.io/etabak/cloudkit-operator:mgmt-23103-phase-fix`)
- Fixed E2E test to check `spec.runStrategy` instead of deprecated `spec.running`
- Found and mitigated bug where phase regresses to `Starting` on re-provision — kept phase as `Running` when instance was already successfully provisioned
- Found and fixed bug where re-provision playbook times out when `runStrategy=Halted` — skip "Wait for VM to be ready" step when VM is halted (PR submitted to osac-templates)
- Removed VMI-level checks from E2E test in favor of VM-level `printableStatus`
- Rebased main on upstream; resolved cherry-pick conflict for already-merged commit

## Risks & Challenges

- E2E test for [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) blocked — needs template PR merged and EE image rebuilt before it can run
- Phase regression fix is a mitigation; proper solution requires introducing an `Updating` phase in CRD + fulfillment API (separate work item with API changes)

## Key Effort

Continued E2E validation work for the ComputeInstance API enhancement, fixing operator re-provision logic and related bugs discovered during testing.
