---
layout: default
title: Daily Status - 2026-02-23
date: 2026-02-23
---

# Daily Status - 2026-02-23

## Accomplishments

- **[MGMT-23031](https://issues.redhat.com/browse/MGMT-23031) – Changed default provisioning provider from EDA to AAP** in osac-operator (`cmd/main.go`, config sample). All tests pass, linter clean.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) – Fixed re-provision logic in operator**: added `needsProvisionJob()` receiver to handle spec changes (e.g., runStrategy) triggering new provision jobs. Deployed to vmaas-dev.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) – Fixed E2E test**: updated from `spec.running` to `spec.runStrategy` checks for Always→Halted→Always cycle.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) – Found and mitigated phase regression bug**: phase was dropping to `Starting` on re-provision of a Running instance. Mitigated by preserving `Running` phase when already provisioned. Deployed fix to vmaas-dev.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) – Found and fixed re-provision timeout bug**: Halted VM never becomes ready, causing 600s timeout. Fix: skip "Wait for VM to be ready" when `vm_run_strategy == "Halted"` (PR osac-templates#8).
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) – Removed VMI-level checks from E2E test**, relying on VM-level printableStatus instead.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) – Re-vendored osac.templates in osac-aap** (PR osac-aap#205 merged, EE image rebuilt).
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) – Found and fixed deprovision spec validation error**: finalizer role's server-side apply rejected by immutability webhook. Fix: switch to JSON merge patch (PR osac-aap#206).
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) – Added `Updating` phase to ComputeInstance API**: added `COMPUTE_INSTANCE_STATE_UPDATING = 5` to fulfillment-api (PR #61) and fulfillment-service (PR #306). Refactored operator phase state machine with resolver-per-phase pattern, comprehensive unit tests, all 110 tests pass (PR osac-operator#114).

## Risks & Challenges

- E2E test for [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) blocked on osac-aap PR #206 merge and EE image rebuild.
- BSR publication of fulfillment-api and private-api protos pending; operator still uses literal `ComputeInstanceState(5)` instead of named constant.

## Key Effort

Drove [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance API enhancement forward by fixing re-provision logic, discovering and resolving multiple bugs across operator and AAP layers, and introducing the `Updating` phase across the fulfillment API, service, and operator.
