---
layout: default
title: Daily Status - 2026-02-23
date: 2026-02-23
---

# Daily Status - 2026-02-23

## Accomplishments
- **[MGMT-23031](https://issues.redhat.com/browse/MGMT-23031) Default Provider Change: EDA to AAP** — Changed default provisioning provider from EDA to AAP in `cmd/main.go` and updated config sample documentation. All unit tests pass, linter clean.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance E2E Validation:**
  - Fixed re-provision logic in operator — added `needsProvisionJob()` receiver to handle spec changes (e.g. runStrategy) triggering new provision jobs
  - Fixed E2E test to use `spec.runStrategy` instead of deprecated `spec.running`
  - Found and fixed phase regression bug — phase was dropping to `Starting` on re-provision of already-running instances
  - Found and fixed re-provision playbook timeout when `runStrategy=Halted` — playbook now skips "Wait for VM to be ready" step (PR: osac-templates#8)
  - Removed VMI-level checks from E2E test in favor of VM-level printableStatus
  - Re-vendored osac.templates in osac-aap (PR: osac-aap#205 merged, EE image rebuilt)
  - Found and fixed deprovision spec validation error — switched finalizer role from server-side apply to JSON merge patch (PR: osac-aap#206)
  - Found and fixed tenant-not-ready requeue bug — controller was returning "done" instead of requeueing with backoff
  - Deployed operator with all fixes to vmaas-dev; E2E test re-running
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) Add Updating Phase to ComputeInstance API:**
  - Added `COMPUTE_INSTANCE_STATE_UPDATING = 5` to fulfillment-api proto (PR: fulfillment-api#61, review pending)
  - Added matching state to fulfillment-service private API proto (PR: fulfillment-service#306, all 17 test suites pass)
  - Added `Updating` phase to osac-operator with state machine refactor and comprehensive unit tests (PR: osac-operator#114, all 110 tests pass)

## Risks & Challenges
- BSR publication of fulfillment-api and private-api protos is pending — blocks updating the operator's generated `.pb.go` and replacing the `ComputeInstanceState(5)` literal with a named constant
- E2E test results still pending after deploying operator with all accumulated fixes

## Key Effort
Drove [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance API enhancement through E2E validation, uncovering and fixing four bugs across the operator and AAP playbooks, while simultaneously adding the `Updating` phase across the fulfillment API, service, and operator.
