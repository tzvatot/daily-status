---
layout: default
title: Daily Status - 2026-02-23
date: 2026-02-23
---

# Daily Status - 2026-02-23

## Accomplishments
- **[MGMT-23031](https://issues.redhat.com/browse/MGMT-23031) Default Provider Change: EDA to AAP** — Changed default provisioning provider from EDA to AAP in `cmd/main.go` and updated config sample documentation. All unit tests pass, linter clean, pre-commit hooks pass.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance API Enhancement — E2E Validation:**
  - Fixed re-provision logic in operator — added `needsProvisionJob()` receiver so spec changes (e.g. runStrategy) correctly trigger re-provisioning
  - Pushed and deployed operator to vmaas-dev
  - Fixed E2E test to check `spec.runStrategy` instead of deprecated `spec.running`
  - Found and mitigated bug where phase regresses to `Starting` on re-provision — keep phase as `Running` when instance was already successfully provisioned
  - Deployed operator with phase fix to vmaas-dev
  - Found and fixed bug where re-provision playbook times out when `runStrategy=Halted` — skip "Wait for VM to be ready" step when VM is halted (PR submitted to osac-templates)
  - Removed unnecessary VMI-level checks from E2E test in favor of VM-level printableStatus
  - Re-vendored osac.templates in osac-aap (PR #205)

## Risks & Challenges
- E2E test for [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) is blocked — needs AAP PR #205 merged and EE image rebuilt before it can run
- Phase regression fix is a mitigation; proper fix requires introducing an `Updating` phase in CRD + fulfillment API (separate work item with API changes)

## Key Effort
Continued E2E validation for ComputeInstance API enhancements, fixing re-provision logic, phase regression bugs, and playbook timeout issues across the operator, templates, and AAP repos.
