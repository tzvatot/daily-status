---
layout: default
title: Daily Status - 2026-02-23
date: 2026-02-23
---

# Daily Status - 2026-02-23

## Accomplishments

- **[MGMT-23031](https://issues.redhat.com/browse/MGMT-23031) Default Provider Change: EDA to AAP** — Changed default provisioning provider from EDA to AAP in `cmd/main.go` and updated config sample documentation. All tests pass, linter clean.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance API Enhancement — E2E Validation:**
  - Fixed re-provision logic in operator — added `needsProvisionJob()` receiver to handle spec changes triggering new provision jobs
  - Deployed updated operator to vmaas-dev
  - Fixed E2E test to use `spec.runStrategy` instead of deprecated `spec.running`
  - Found and mitigated bug where phase regresses to `Starting` on re-provision; follow-up `Updating` phase work tracked separately
  - Found and fixed re-provision playbook timeout when `runStrategy=Halted` — PR: osac-templates#8
  - Removed unnecessary VMI-level checks from E2E test
  - Re-vendored osac.templates in osac-aap (PR merged: osac-aap#205, EE image rebuilt)
  - Found and fixed deprovision spec validation error — switched from server-side apply to JSON merge patch (PR: osac-aap#206)
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) Add Updating Phase to ComputeInstance API:**
  - Added `COMPUTE_INSTANCE_STATE_UPDATING = 5` to fulfillment-api proto (PR: fulfillment-api#61) and fulfillment-service private API proto (PR: fulfillment-service#306)
  - Added `Updating` phase to osac-operator with state machine refactor, extracted phase transitions to resolver-per-phase pattern, comprehensive unit tests — all 110 tests pass (PR: osac-operator#114)

## Risks & Challenges

- E2E test for [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) blocked on osac-aap PR #206 merge and EE image rebuild
- BSR publication of fulfillment-api and private-api protos still pending; operator uses literal `ComputeInstanceState(5)` until named constant is available from BSR update

## Key Effort

Drove [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance API enhancement forward across multiple repos — fixing re-provision logic, discovering and resolving three bugs, and introducing the `Updating` phase across the fulfillment API, service, and operator.
