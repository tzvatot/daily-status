---
layout: default
title: Daily Status - 2026-02-23
date: 2026-02-23
---

# Daily Status - 2026-02-23

## Accomplishments

- **[MGMT-23031](https://issues.redhat.com/browse/MGMT-23031) — Changed default provisioning provider from EDA to AAP** in osac-operator (`cmd/main.go` and config sample updated). All tests pass, linter clean.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — Fixed re-provision logic in operator**: added `needsProvisionJob()` to detect spec changes (e.g. runStrategy) and trigger re-provisioning; regenerate `templateParameters` on each provision.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — Fixed E2E test** to check `spec.runStrategy` instead of deprecated `spec.running`.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — Found and mitigated phase regression bug**: phase was dropping to `Starting` on re-provision of a Running instance; fixed to preserve `Running` phase when already provisioned.
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — Found and fixed re-provision timeout bug**: playbook timed out when `runStrategy=Halted` because it waited for VM Ready; fix skips the wait step for Halted VMs (PR submitted to osac-templates).
- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — Removed VMI-level checks from E2E test**, relying on VM-level `printableStatus` instead of KubeVirt internals.
- Deployed updated operator to vmaas-dev with all fixes.

## Risks & Challenges

- E2E test for [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) is blocked until the template PR is merged and the EE image is rebuilt.
- Phase regression fix is a mitigation; a proper `Updating` phase requires CRD and fulfillment API changes (separate follow-up work item).

## Key Effort

Continued E2E validation for ComputeInstance API enhancements, fixing operator re-provision logic and multiple bugs discovered during testing.
