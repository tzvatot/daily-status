---
layout: default
title: Daily Status - 2026-02-23
date: 2026-02-23
---

# Daily Status - 2026-02-23

## Accomplishments

- **[MGMT-23031](https://issues.redhat.com/browse/MGMT-23031) - Default Provider Change: EDA to AAP**
  - Changed default provisioning provider from EDA to AAP in `cmd/main.go` and updated config sample documentation
  - All unit tests pass, linter clean, pre-commit hooks pass
  - Rebased main on upstream; resolved cherry-pick conflict for already-merged commit

- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) - ComputeInstance API Enhancement: E2E Validation**
  - Fixed re-provision logic in operator — added `needsProvisionJob()` to handle spec changes (e.g. runStrategy) that previously never triggered a new provision job
  - Built, pushed, and deployed operator to vmaas-dev cluster
  - Fixed E2E test to check `spec.runStrategy` instead of deprecated `spec.running`
  - Found and mitigated bug where phase regresses to `Starting` on re-provision — kept phase as `Running` when instance was already successfully provisioned (follow-up needed for proper `Updating` phase in CRD + fulfillment API)
  - E2E test kicked off on vmaas-dev

## Risks & Challenges

- Phase regression bug ([MGMT-23103](https://issues.redhat.com/browse/MGMT-23103)): current fix is a mitigation; a proper `Updating` phase requires CRD and fulfillment API changes in a separate work item
- E2E test still running at end of day — results pending

## Key Effort

Continued E2E validation work for ComputeInstance API enhancements, fixing operator re-provision logic and deploying to dev for end-to-end testing.
