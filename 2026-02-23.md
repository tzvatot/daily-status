---
layout: default
title: Daily Status - 2026-02-23
date: 2026-02-23
---

# Daily Status - 2026-02-23

## Accomplishments

- **[MGMT-23031](https://issues.redhat.com/browse/MGMT-23031) — Changed default provisioning provider from EDA to AAP** in osac-operator (`cmd/main.go`, config sample). All tests pass, linter clean. Rebased main on upstream.

- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — E2E Validation (continued):**
  - Fixed re-provision logic in operator — added `needsProvisionJob()` receiver to handle spec changes (e.g. runStrategy) that previously never triggered a new provision job
  - Deployed updated operator to vmaas-dev
  - Fixed E2E test to check `spec.runStrategy` instead of deprecated `spec.running`
  - Found and mitigated bug where phase regressed to `Starting` on re-provision — kept phase as `Running` when instance was already provisioned; follow-up `Updating` phase tracked separately
  - Found and fixed bug where re-provision playbook timed out when `runStrategy=Halted` — skip "Wait for VM to be ready" step (PR: osac-templates#8)
  - Removed VMI-level checks from E2E test in favor of VM-level `printableStatus`
  - Re-vendored osac.templates in osac-aap (PR merged: osac-aap#205), EE image rebuilt
  - Found and fixed deprovision failure caused by spec validation error — switched finalizer role from server-side apply to JSON merge patch (PR: osac-aap#206)
  - E2E test pending AAP PR #206 merge and EE image rebuild

- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — Added `Updating` phase to ComputeInstance API:**
  - Added `COMPUTE_INSTANCE_STATE_UPDATING = 5` to fulfillment-api proto (PR: fulfillment-api#61, review pending)
  - Added same state to fulfillment-service private API proto (PR: fulfillment-service#306, all 17 test suites pass)
  - Added `Updating` phase to osac-operator with state machine refactor, extracted phase transitions to resolver-per-phase pattern, comprehensive unit tests (PR: osac-operator#114, all 110 tests pass)
  - Pending: BSR publication of protos, update operator's generated `.pb.go`, replace literal `ComputeInstanceState(5)` with named constant

## Risks & Challenges

- E2E validation for [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) is blocked on osac-aap PR #206 merge and EE image rebuild
- `Updating` phase work depends on BSR publication of fulfillment-api and private-api protos before operator can use named constants

## Key Effort

Continued E2E validation and bug fixing for the ComputeInstance API enhancement ([MGMT-23103](https://issues.redhat.com/browse/MGMT-23103)), including re-provision logic fixes, phase regression mitigation, and introducing a new `Updating` phase across the fulfillment API, service, and operator.
