---
layout: default
title: Daily Status - 2026-05-06
date: 2026-05-06
---

# Daily Status - 2026-05-06

## Accomplishments

- **Demo cluster provisioning (continued):** Identified that 3 agents were stuck in a pending state requiring PXE boot. Found 2 alternative usable agents and relabeled them, while removing labels from the stuck agents to prevent selection.
- **Cleaned up ghost cluster `order-tfn5n`:** A stale reconciliation had auto-created a cluster order pointing to a deleted cluster's DNS, causing an unreachable API and recurring provision failures. Fully cleaned up all associated resources (cluster order, hosted cluster, managed cluster, ESI router) and verified a clean state with 2 usable agents remaining.
- **Updated Jira tooling for OSAC project migration:** The OSAC project moved from the MGMT Jira project to a dedicated OSAC project. Updated local Jira configuration to reflect the new project key, status transitions, and workflows.
- **Investigated and partially fixed fulfillment controller for demo cluster creation:** The cluster creation flow wasn't propagating secrets from the database to the cluster order resource. Upgraded the controller and dependent services to the latest versions, reconfigured Keycloak authentication (group membership, username mapping, admin group assignment), and identified that OAuth token auth hangs due to an Authorino/Keycloak JWT integration issue. Service account token auth works as a fallback; next step is to manually patch the cluster order and retry provisioning.
- **Rebuilt the `osac` CLI** from latest main to stay current with API changes.

## Risks & Challenges

- OAuth token authentication with Authorino does not work with Keycloak-issued JWTs via the external route — service account tokens work fine as a workaround, but this integration gap may need to be addressed for production use.

## Key Effort

Unblocking demo cluster creation by resolving agent availability issues, cleaning up stale cluster resources, and debugging the fulfillment controller's authentication and secret propagation pipeline.
