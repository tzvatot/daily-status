---
layout: default
title: Daily Status - 2026-05-06
date: 2026-05-06
---

# Daily Status - 2026-05-06

## Accomplishments
- **Demo cluster creation (continued):** Identified that 3 provisioning agents were stuck waiting on PXE boot action. Found 2 alternative usable agents, relabeled them for selection, and removed labels from the stuck agents to prevent them from being picked up.
- **Cleaned up ghost cluster order:** A stale reconciliation auto-created a ghost cluster order whose control plane came up but pointed DNS at a previously deleted order, making the API unreachable. Fully cleaned up all associated resources (cluster order, hosted cluster, managed cluster, ESI router) and verified a clean state with 2 usable agents remaining.
- **Updated Jira skill for OSAC project migration:** The OSAC project moved from the MGMT to the OSAC project in Jira. Updated the Jira CLI skill configuration to reflect the new project key, status transitions, and workflows.
- **Diagnosed and partially fixed fulfillment-controller for demo cluster creation:** The controller wasn't propagating secrets from the database to the cluster order resource. Updated the controller and related services to the latest versions, then resolved a chain of authentication issues including OAuth flag changes and missing Keycloak claim mappers (groups and username). Discovered that OAuth token auth is blocked by an Authorino/Keycloak JWT integration issue on the external route — service account token auth works, so the workaround is to manually patch the cluster order with the required secrets.

## Risks & Challenges
- OAuth token authentication through the external route is blocked due to an Authorino/Keycloak JWT integration issue. Service account tokens work as a workaround, and the immediate cluster order can be manually patched, but this will need resolution for production use.

## Key Effort
Unblocking demo cluster creation by resolving stuck agents, cleaning up ghost cluster resources, and debugging the fulfillment-controller authentication chain.
