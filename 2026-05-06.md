---
layout: default
title: Daily Status - 2026-05-06
date: 2026-05-06
---

# Daily Status - 2026-05-06

## Accomplishments

- **Unblocked demo cluster creation** by identifying 2 alternative available bare-metal agents after 3 original agents got stuck in a state requiring PXE boot via BMC control. Relabeled agents appropriately and removed stuck agents from selection pool.
- **Cleaned up ghost cluster order** (`order-tfn5n`) that was auto-created by the operator from a stale reconciliation of a previously deleted order. The ghost cluster had a working control plane but DNS pointed to the wrong (deleted) order, making the API unreachable. Fully removed all associated resources (cluster order, hosted cluster, managed cluster, ESI router) and verified a clean state with 2 usable agents remaining.
- **Updated Jira automation** to reflect the OSAC project migration from the MGMT project to the new OSAC project in Jira, including updated status transitions and project references.
- **Diagnosed and partially fixed the fulfillment controller** for demo cluster creation. The controller wasn't writing required secrets to the cluster order resource. Updated all services (controller, gRPC server, REST gateway, CLI) to latest versions and configured Keycloak OAuth authentication (group membership mapper, username mapper, admin group). Discovered that OAuth token auth hangs due to an integration issue between the auth gateway and Keycloak JWTs via external route — service account token auth works correctly. Workaround identified: manually patch the cluster order with the required secrets.

## Risks & Challenges

- OAuth token authentication with Keycloak JWTs is not working through the external auth gateway, blocking the controller from authenticating automatically. Service account token auth works as a workaround, and the cluster order can be manually patched to proceed with the demo.

## Key Effort

Unblocking demo cluster creation by resolving agent availability issues, cleaning up stale cluster resources, and diagnosing the fulfillment controller's OAuth authentication chain.
