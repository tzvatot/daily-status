---
layout: default
title: Daily Status - 2026-05-06
date: 2026-05-06
---

# Daily Status - 2026-05-06

## Accomplishments
- **Demo cluster creation (continued):** Identified that 3 agents were stuck in a state requiring PXE boot via BMC control. Found 2 alternative usable agents and relabeled them for cluster creation, while removing the stuck agents from selection.
- **Cleaned up ghost cluster order:** A stale reconciliation had auto-created a ghost cluster order from a previously deleted one. The control plane came up but DNS pointed to the deleted order, making the API unreachable. Fully cleaned up all associated resources (cluster order, hosted cluster, managed cluster, ESI router) and verified the environment is clean with 2 usable agents remaining.
- **Updated Jira CLI skill for OSAC project migration:** The OSAC project moved from the MGMT project to a dedicated OSAC project in Jira. Updated the Jira skill configuration to reflect the new project key, status transitions, and examples. Confirmed new structure via [OSAC-62](https://issues.redhat.com/browse/OSAC-62).
- **Investigated fulfillment controller for demo cluster creation:** The cluster creation flow wasn't propagating pull secret and SSH key from the database to the cluster order resource. Updated multiple services to their latest versions and worked through a chain of authentication issues involving Keycloak configuration (group membership mappers, username claim mappers, admin group assignments). Discovered that OAuth-based token authentication hangs due to an integration issue between Authorino and Keycloak JWTs. Service account token auth works correctly. Workaround identified: manually patch the cluster order with the required secrets from the database.

## Risks & Challenges
- OAuth token authentication with Authorino and Keycloak JWTs is not working via the external route, blocking automated secret propagation from the fulfillment controller. A manual patching workaround is available to unblock demo cluster creation.

## Key Effort
Unblocking demo cluster creation by resolving stuck agents, cleaning up ghost cluster resources, and debugging the fulfillment controller's authentication chain.
