---
layout: default
title: Daily Status - 2026-05-06
date: 2026-05-06
---

# Daily Status - 2026-05-06

## Accomplishments
- **Demo cluster prep:** Identified two alternative available bare-metal agents after the original three got stuck waiting for PXE boot, relabeled them for use, and removed the stuck agents from the selection pool
- **Ghost cluster cleanup:** Fully cleaned up a stale cluster order (`order-tfn5n`) that was auto-created by the operator from a previously deleted order — removed the cluster order, hosted cluster, managed cluster, and ESI router, and verified a clean state with two usable agents remaining
- **Jira integration update:** Updated the Jira CLI skill to reflect the OSAC project migration from the MGMT project to the new OSAC project in Jira, including updated status transitions and project references ([OSAC-62](https://issues.redhat.com/browse/OSAC-62))
- **Fulfillment controller debugging:** Updated the gRPC server, REST gateway, and fulfillment controller to latest versions to fix cluster creation not populating secrets on the cluster order resource. Configured Keycloak OAuth authentication (group membership mapper, username mapper, admins group) for the controller. Service account token auth works correctly, but OAuth token auth hangs due to an Authorino/Keycloak JWT integration issue on the external route
- **CLI rebuild:** Rebuilt the `osac` CLI from latest main to pick up recent fixes

## Risks & Challenges
- Authorino does not respond to Keycloak-issued JWTs via the external route, blocking automated cluster creation through the fulfillment controller with OAuth. Workaround is to manually patch the cluster order resource with the required secrets from the database.

## Key Effort
Unblocking demo cluster creation by cleaning up stale resources, swapping in available agents, and debugging the fulfillment controller's OAuth authentication chain.
