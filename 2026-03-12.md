---
layout: default
title: Daily Status - 2026-03-12
date: 2026-03-12
---

# Daily Status - 2026-03-12

## Accomplishments
- Validated the AAP direct provisioning flow end-to-end for both ClusterOrder and HostPool resource types in [osac-operator#110](https://github.com/osac-project/osac-operator/pull/110), confirming job triggering, status polling, phase transitions, deletion cleanup, and condition updates all work correctly on the dev cluster
- Tested HostPool creation and deletion flows against the dev environment — creation failed at the infrastructure level (no bare metal agents available), confirming the operator logic itself is correct; deletion flow completed successfully with full resource and namespace cleanup
- Performed a thorough code review of [osac-operator#110](https://github.com/osac-project/osac-operator/pull/110) and identified two critical bugs: a nil pointer dereference in the deprovisioning handler and an issue where the ClusterOrder phase was being unconditionally reset on every reconcile, causing state flickering
- Fixed both critical bugs along with several warnings (stale comments, missing constants, silent fallback on unknown types) and pushed all fixes — PR is now mergeable with no conflicts
- Rebased [osac-test-infra#15](https://github.com/osac-project/osac-test-infra/pull/15) on latest main and addressed review feedback by upgrading ansible-lint and ansible-core to current versions instead of pinning the Python version
- Rebased [osac-operator#110](https://github.com/osac-project/osac-operator/pull/110) on latest main after the networking PR [osac-operator#132](https://github.com/osac-project/osac-operator/pull/132) merged, resolving all conflicts

## Risks & Challenges
- Ongoing missile attacks in Israel — sleep-deprived, frequent shelter time, helping kids with remote schooling, cooking and childcare throughout the day. Reduced availability and focus expected
- Full "Ready" state for both ClusterOrder and HostPool cannot be validated on the dev cluster due to missing bare metal agents — this is an infrastructure limitation, not a code issue
- The `BlockDeletionOnFailure` behavior needs further design discussion — currently deletion proceeds even when the flag is set, risking orphaned resources

## Key Effort
Validated and hardened the AAP direct provisioning operator for both ClusterOrder and HostPool, fixing critical bugs found during code review and preparing the PR for final review.
