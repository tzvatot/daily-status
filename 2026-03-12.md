---
layout: default
title: Daily Status - 2026-03-12
date: 2026-03-12
---

# Daily Status - 2026-03-12

## Accomplishments
- Continued work on AAP direct provisioning support for both ClusterOrder and HostPool resource types ([osac-operator#110](https://github.com/osac-project/osac-operator/pull/110))
- Validated HostPool AAP direct provisioning end-to-end on dev cluster: creation flow triggered provision job correctly through all phase transitions, and deletion flow completed successfully with full resource cleanup
- Confirmed operator behavior is correct for both resource types — provision jobs trigger via direct API, status polling works, phase transitions and conditions update accurately, and deprovision-on-delete works as expected
- Rebased [osac-test-infra#15](https://github.com/osac-project/osac-test-infra/pull/15) on latest main and addressed review feedback by upgrading ansible-lint and ansible-core to current versions instead of pinning a Python version workaround
- Rebased [osac-operator#110](https://github.com/osac-project/osac-operator/pull/110) on latest main after a networking PR merged, resolved all conflicts, and confirmed the PR is now mergeable
- Performed a thorough code review of the PR and identified two critical bugs: a nil pointer dereference in the deprovisioning handler and an issue where the ClusterOrder phase was being unconditionally reset on every reconcile, causing status flickering
- Fixed both critical bugs along with several warnings (stale comments, missing constants, silent fallback on unknown types) and pushed all fixes — all validations pass

## Risks & Challenges
- Ongoing missile attacks in Israel — sleep-deprived, frequent shelter time, helping kids with remote schooling, cooking and childcare throughout the day. Reduced availability and focus expected
- Full "Ready" state validation is blocked by missing bare metal agents on the dev cluster — this is an infrastructure limitation, not an operator issue
- The `BlockDeletionOnFailure` behavior needs further design discussion before implementing a fix

## Key Effort
Validating and hardening the AAP direct provisioning feature across both resource types, including end-to-end testing on the dev cluster and fixing critical bugs found during code review.
