---
layout: default
title: Daily Status - 2026-03-12
date: 2026-03-12
---

# Daily Status - 2026-03-12

## Accomplishments
- Validated the AAP direct provisioning flow end-to-end for both ClusterOrder and HostPool resource types on the dev cluster ([osac-operator#110](https://github.com/osac-project/osac-operator/pull/110))
  - Confirmed provision and deprovision jobs are triggered correctly, status polling works, phase transitions and conditions are accurate, and full deletion cleanup completes successfully
  - The only unvalidated path (reaching Ready state) is blocked by missing bare metal agents on the dev cluster — an infrastructure limitation, not an operator issue
- Tested HostPool creation and deletion flows, confirming the operator handles both success and failure cases correctly, including namespace lifecycle management
- Performed a thorough code review of [osac-operator#110](https://github.com/osac-project/osac-operator/pull/110) and fixed two critical bugs: a nil pointer dereference in the deprovisioning handler and a reconcile loop that was incorrectly resetting resource phase on every iteration
- Fixed several additional warnings from the review including a stale CRD comment, undeclared constants, a silent fallback for unknown resource types, and inconsistent default value handling
- Rebased [osac-operator#110](https://github.com/osac-project/osac-operator/pull/110) on latest main after a networking PR merged, resolved conflicts, and confirmed the PR is now mergeable with all validations passing
- Addressed review feedback on osac-test-infra PR #15: upgraded ansible-lint and ansible-core to latest versions instead of pinning the Python version as a workaround

## Risks & Challenges
- Ongoing missile attacks in Israel — sleep-deprived, frequent shelter time, helping kids with remote schooling, cooking and childcare throughout the day. Reduced availability and focus expected.
- Full Ready-state validation for both ClusterOrder and HostPool is blocked by missing bare metal agents on the dev cluster
- The `BlockDeletionOnFailure` behavior needs further design work to prevent orphaned resources when deprovision jobs fail

## Key Effort
Validating and hardening the AAP direct provisioning operator for both ClusterOrder and HostPool resources, including end-to-end testing and fixing critical bugs found during code review.
