---
layout: default
title: Daily Status - 2026-02-24
date: 2026-02-24
---

# Daily Status - 2026-02-24

## Accomplishments
- Updated `osac-aap` extract_compute_instance_template_info role to set `template_parameters` to empty dict instead of parsing JSON — PR [osac-aap #207](https://github.com/osac-project/osac-aap/pull/207) merged
- Added critical missing tests and fixed status clobbering bug: 7 tests for `needsProvisionJob()`, phase regression test, tenant GC race tests; fixed `r.Update()` overwriting in-memory status after finalizer update (commit `d34f952`, all 88 tests pass, 43.6% coverage)
- Fixed warning-level code review findings: corrected `log.Error(nil, ...)` to `log.Info(...)`, fixed comment typo, fixed test JobID generation (commit `1fcd82e`)
- Cleaned up API type markers: removed 7 redundant `+kubebuilder:validation:Type=` markers, added explicit `(MUTABLE)` marker to `RestartRequestedAt`, CRD regenerated (commit `cd381a8`)
- E2E test passed after `ensureBackwardCompatibility` removal — all 7 checks passed (provision, Running, Halted, Always, cores/memoryGiB/image immutability) with operator image `quay.io/etabak/cloudkit-operator:cd381a8`
- [PR #110](https://github.com/osac-project/osac-operator/pull/110): Rebased extend-jobs-array-pattern branch on upstream/main, resolved 8 conflicts across 7 files, renamed remaining `CLOUDKIT_*` env vars to `OSAC_*`, all tests pass and linter clean, pushed and updated PR description
- Completed code review of PR #110 — found 3 critical, 4 warning, 4 note issues including silent failures on unknown resource types, factory only accepting 2 of 6 EDA webhook URLs, and EDA webhook fallback to ComputeInstance URLs instead of error

## Risks & Challenges
- [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) operator PR still pending: need to merge [PR #111](https://github.com/osac-project/osac-operator/pull/111), re-vendor, rebuild EE image, and re-run E2E test
- PR #110 has critical issues identified during code review that are still being fixed

## Key Effort
Continued [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance API enhancement with test hardening, bug fixes, and E2E validation, while rebasing and reviewing the extend-jobs-array-pattern PR #110 for ClusterOrder and HostPool.
