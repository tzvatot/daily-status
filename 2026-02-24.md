---
layout: default
title: Daily Status - 2026-02-24
date: 2026-02-24
---

# Daily Status - 2026-02-24

## Accomplishments

- Updated `osac-aap` `extract_compute_instance_template_info` role to set `template_parameters` to empty dict instead of parsing `templateParameters | from_json` (PR [osac-aap#207](https://github.com/osac-project/osac-aap/pull/207))
- Added critical missing tests and fixed status clobbering bug: 7 tests for `needsProvisionJob()`, phase regression test, tenant GC race tests; fixed `r.Update()` overwriting in-memory status after finalizer update (commit `d34f952`, 88 tests pass, 43.6% coverage)
- Fixed warning-level code review findings: corrected `log.Error(nil, ...)` to `log.Info(...)` for expected failures, fixed comment typo, fixed test JobID generation (commit `1fcd82e`)
- Cleaned up API type markers: removed 7 redundant `+kubebuilder:validation:Type=` markers, added explicit `(MUTABLE)` marker to `RestartRequestedAt`, regenerated CRD (commit `cd381a8`)
- E2E test passed after `ensureBackwardCompatibility` removal — all 7 checks passed (provision, Running, Halted, Always, cores/memoryGiB/image immutability) with operator image `quay.io/etabak/cloudkit-operator:cd381a8`
- Conducted thorough operator code review for [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) — line-by-line review covering edge cases, race conditions, test coverage, and error handling

## Risks & Challenges

- PR #110 (extend jobs array pattern to ClusterOrder and HostPool) needs rebase on upstream/main due to module rename (`osac` → `osac-project`) and CRD rename (`cloudkit` → `osac`) — 100 files changed, potential for significant merge conflicts
- [osac-aap#207](https://github.com/osac-project/osac-aap/pull/207) changes still need re-vendoring and EE image rebuild before final E2E validation on that path

## Key Effort

Completing the [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance API enhancement — adding tests, fixing bugs found during code review, cleaning up API markers, and validating end-to-end.
