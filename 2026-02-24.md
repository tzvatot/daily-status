---
layout: default
title: Daily Status - 2026-02-24
date: 2026-02-24
---

# Daily Status - 2026-02-24

## Accomplishments
- Updated `osac-aap` extract_compute_instance_template_info role to set `template_parameters` to empty dict instead of parsing `templateParameters | from_json` ([PR #207](https://github.com/osac-project/osac-aap/pull/207))
- Added critical missing tests: 7 tests for `needsProvisionJob()`, phase regression test, tenant GC race tests (DeletionTimestamp detection + terminating tenant rejection) — all 88 tests pass, 43.6% coverage
- Found and fixed status clobbering bug: `r.Update()` for finalizer was overwriting in-memory status (phase, conditions) with server response; fixed by moving status initialization after finalizer update
- Fixed warning-level code review findings: replaced `log.Error(nil, ...)` with `log.Info(...)` for expected failures, fixed comment typo, fixed test JobID generation
- Cleaned up API type markers: removed 7 redundant `+kubebuilder:validation:Type=` markers, added explicit `(MUTABLE)` marker to `RestartRequestedAt`, regenerated CRD
- E2E test passed after `ensureBackwardCompatibility` removal — all 7 checks passed (provision, Running, Halted, Always, cores/memoryGiB/image immutability)

## Risks & Challenges
- Pending: merge osac-aap PR #207, re-vendor, rebuild EE image, re-run E2E test
- Thorough operator code review still in progress (line-by-line review for edge cases, race conditions, test coverage gaps)

## Key Effort
Continued [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance API enhancement — added critical test coverage, fixed a status clobbering bug, cleaned up API markers, and validated all changes with a passing E2E test.
