---
layout: default
title: Daily Status - 2026-02-24
date: 2026-02-24
---

# Daily Status - 2026-02-24

## Accomplishments
- Updated `osac-aap` extract_compute_instance_template_info role to set `template_parameters` to empty dict instead of parsing `templateParameters | from_json` ([MGMT-23103](https://issues.redhat.com/browse/MGMT-23103)) — PR: https://github.com/osac-project/osac-aap/pull/207
- Added critical missing tests and fixed status clobbering bug: 7 tests for `needsProvisionJob()`, phase regression test, tenant GC race tests; fixed `r.Update()` overwriting in-memory status after finalizer update (commit `d34f952`, all 88 tests pass, 43.6% coverage)
- Fixed warning-level code review findings: corrected `log.Error(nil, ...)` to `log.Info(...)`, fixed comment typo, fixed test JobID generation (commit `1fcd82e`)
- Cleaned up API type markers: removed 7 redundant `+kubebuilder:validation:Type=` markers, added explicit `(MUTABLE)` marker to `RestartRequestedAt`, CRD regenerated (commit `cd381a8`)
- E2E test passed after `ensureBackwardCompatibility` removal — all 7 checks passed (provision, Running, Halted, Always, cores/memoryGiB/image immutability)

## Risks & Challenges
- Pending: merge osac-aap PR #207, re-vendor, rebuild EE image, and re-run E2E test
- Thorough operator code review still in progress (line-by-line review for edge cases, race conditions, missing test coverage)

## Key Effort
Continued [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance API enhancement — added critical tests, fixed a status clobbering bug, cleaned up API markers, and validated end-to-end with a successful E2E test run.
