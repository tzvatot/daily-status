---
layout: default
title: Daily Status - 2026-02-24
date: 2026-02-24
---

# Daily Status - 2026-02-24

## Accomplishments

- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance API Enhancement:**
  - Removed `extract_compute_instance_template_info` role from osac-aap per reviewer feedback; inlined vars directly in playbook (PR [osac-aap#207](https://github.com/osac-project/osac-aap/pull/207))
  - Added critical missing tests (7 for `needsProvisionJob()`, phase regression, tenant GC race tests) and **fixed status clobbering bug** where `r.Update()` for finalizer was overwriting in-memory status — all 88 tests pass, 43.6% coverage
  - Fixed warning-level code review findings: corrected log levels, comment typo, and test JobID generation
  - Cleaned up API type markers: removed 7 redundant `+kubebuilder:validation:Type=` markers, added explicit `(MUTABLE)` marker
  - E2E test passed after `ensureBackwardCompatibility` removal — all 7 checks passed (provision, Running, Halted, Always, immutability)
  - Removed "backward compatibility" language from `templateParameters` comment and regenerated CRD (PR [osac-operator#111](https://github.com/osac-project/osac-operator/pull/111))

- **PR #110: Extend jobs array pattern to ClusterOrder and HostPool:**
  - Rebased on upstream/main, resolved 8 conflicts across 7 files (module rename, CRD rename, env var rename)
  - Renamed remaining `CLOUDKIT_*` env vars to `OSAC_*`
  - Completed code review — found 3 critical, 4 warning, 4 note issues
  - Fixed all critical and warning issues: type switch helpers now return errors on unknown resource types, EDA `getWebhookURLs` returns error instead of silent fallback, factory accepts all 6 EDA webhook URLs, fixed unclear error return and typo

## Risks & Challenges

None

## Key Effort

Continued ComputeInstance API enhancement work including bug fixes, test coverage improvements, E2E validation, and bringing the ClusterOrder/HostPool extension PR to review-ready state.
