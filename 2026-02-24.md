---
layout: default
title: Daily Status - 2026-02-24
date: 2026-02-24
---

# Daily Status - 2026-02-24

## Accomplishments
- Updated `osac-aap` extract_compute_instance_template_info role to set `template_parameters` to empty dict instead of parsing `templateParameters | from_json` (PR: https://github.com/osac-project/osac-aap/pull/207)
- Added critical missing tests and fixed status clobbering bug: 7 tests for `needsProvisionJob()`, phase regression test, tenant GC race tests; fixed `r.Update()` overwriting in-memory status after finalizer update ([MGMT-23103](https://issues.redhat.com/browse/MGMT-23103))
- Fixed warning-level code review findings: corrected `log.Error(nil, ...)` to `log.Info(...)`, fixed comment typo, fixed test JobID generation
- Cleaned up API type markers: removed 7 redundant `+kubebuilder:validation:Type=` markers, added explicit `(MUTABLE)` marker to `RestartRequestedAt`, regenerated CRD
- E2E test passed after `ensureBackwardCompatibility` removal — all 7 checks passed (provision, Running, Halted, Always, cores/memoryGiB/image immutability)
- Conducted thorough operator code review (in progress): line-by-line review for issues, edge cases, race conditions, test coverage gaps, and error handling patterns

## Risks & Challenges
- Pending: merge osac-aap PR #207, re-vendor, rebuild EE image, re-run E2E test
- Operator code review still in progress

## Key Effort
Continued ComputeInstance API enhancement ([MGMT-23103](https://issues.redhat.com/browse/MGMT-23103)) — hardening operator with tests, bug fixes, code cleanup, and E2E validation.
