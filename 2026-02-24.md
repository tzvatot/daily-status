---
layout: default
title: Daily Status - 2026-02-24
date: 2026-02-24
---

# Daily Status - 2026-02-24

## Accomplishments

- Updated `osac-aap` `extract_compute_instance_template_info` role to set `template_parameters` to empty dict instead of parsing `templateParameters | from_json` (PR [osac-aap#207](https://github.com/osac-project/osac-aap/pull/207))
- Added critical missing tests and fixed status clobbering bug: 7 tests for `needsProvisionJob()`, phase regression test, tenant GC race tests; fixed `r.Update()` overwriting in-memory status after finalizer update (all 88 tests pass, 43.6% coverage)
- Fixed warning-level code review findings: corrected `log.Error(nil, ...)` to `log.Info(...)` for expected failures, fixed comment typo, fixed test JobID generation
- Cleaned up API type markers: removed 7 redundant `+kubebuilder:validation:Type=` markers, added explicit `(MUTABLE)` marker to `RestartRequestedAt`, regenerated CRD
- E2E test passed after `ensureBackwardCompatibility` removal — all 7 checks passed (provision, Running, Halted, Always, cores/memoryGiB/image immutability)
- Rebased PR #110 (extend jobs array pattern to ClusterOrder and HostPool) on upstream/main — resolved 8 conflicts across 7 files (module rename, CRD rename, env var rename), all tests pass
- Renamed remaining `CLOUDKIT_*` env vars to `OSAC_*` in PR #110

## Risks & Challenges

- [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) operator PR still pending: need to merge osac-aap changes, re-vendor, rebuild EE image, and re-run E2E test
- PR #110 still needs to be pushed to fork and PR description updated before it is review-ready
- Thorough operator code review (line-by-line as principal engineer) still in progress

## Key Effort

Continued [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance API enhancement work — added critical test coverage, fixed a status clobbering bug, passed E2E tests, and rebased the related PR #110 on upstream/main.
