---
layout: default
title: Daily Status - 2026-02-24
date: 2026-02-24
---

# Daily Status - 2026-02-24

## Accomplishments
- Removed `extract_compute_instance_template_info` role from osac-aap per reviewer feedback — inlined vars directly in playbook ([osac-aap PR #207](https://github.com/osac-project/osac-aap/pull/207))
- Thorough operator code review — line-by-line review for issues, edge cases, race conditions, test coverage, and error handling
- Added critical missing tests and fixed status clobbering bug — 7 `needsProvisionJob()` tests, phase regression test, tenant GC race tests; fixed `r.Update()` overwriting in-memory status after finalizer update (all 88 tests pass, 43.6% coverage)
- Fixed warning-level code review findings — corrected `log.Error(nil, ...)` to `log.Info(...)`, fixed comment typo, fixed test JobID generation
- Cleaned up API type markers — removed 7 redundant `+kubebuilder:validation:Type=` markers, added explicit `(MUTABLE)` marker to `RestartRequestedAt`
- E2E test passed after `ensureBackwardCompatibility` removal — all 7 checks passed (provision, Running, Halted, Always, immutability)
- Rebased PR #110 (extend jobs array pattern to ClusterOrder and HostPool) on upstream/main — resolved 8 conflicts across 7 files, renamed remaining `CLOUDKIT_*` env vars to `OSAC_*`
- Code review of PR #110 — found 3 critical, 4 warning, 4 note issues; fixed all critical and warning issues (type switch error handling, EDA webhook URL routing, factory config for all resource types)
- Added provider tests for ClusterOrder and HostPool — 6 AAP provider tests, 7 EDA provider tests; provisioning coverage 70.3% → 72.8%
- Removed "backward compatibility" language from `templateParameters` — updated comment and regenerated CRD ([operator PR #111](https://github.com/osac-project/osac-operator/pull/111))
- Removed `memoryGiB` `Maximum=1024` validation per reviewer feedback — provider-agnostic resource should not cap memory ([operator PR #111](https://github.com/osac-project/osac-operator/pull/111))

## Risks & Challenges
- PR #110 needed rebase due to upstream module and CRD renames — resolved
- E2E validation for `memoryGiB` fix still in progress

## Key Effort
Continued [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance API enhancement — addressing reviewer feedback, adding test coverage, fixing bugs, and extending the jobs array pattern to ClusterOrder and HostPool.
