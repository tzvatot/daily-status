---
layout: default
title: Daily Status - 2026-02-24
date: 2026-02-24
---

# Daily Status - 2026-02-24

## Accomplishments

- Updated `osac-aap` extract_compute_instance_template_info role to set `template_parameters` to empty dict instead of parsing `templateParameters | from_json` ([PR #207](https://github.com/osac-project/osac-aap/pull/207))
- Added critical missing tests for [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103): 7 tests for `needsProvisionJob()`, phase regression test, and tenant GC race tests (DeletionTimestamp detection + terminating tenant rejection)
- Found and fixed status clobbering bug where `r.Update()` for finalizer was overwriting in-memory status (phase, conditions) with server response
- Fixed warning-level code review findings: corrected `log.Error(nil, ...)` to `log.Info(...)` for expected failures, fixed comment typo, and fixed test JobID generation
- Cleaned up API type markers: removed 7 redundant `+kubebuilder:validation:Type=` markers and added explicit `(MUTABLE)` marker to `RestartRequestedAt` comment
- E2E test passed after `ensureBackwardCompatibility` removal — all 7 checks passed (provision, Running, Halted, Always, cores/memoryGiB/image immutability)
- All 88 unit tests pass, linter clean, 43.6% coverage

## Risks & Challenges

- Pending: merge [osac-operator PR #111](https://github.com/osac-project/osac-operator/pull/111), re-vendor, rebuild EE image, and re-run E2E test after osac-aap changes
- Thorough operator code review still in progress

## Key Effort

Continued work on [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance API enhancement — added critical test coverage, fixed a status clobbering bug, cleaned up API markers, and validated all changes with a successful E2E test.
