---
layout: default
title: Daily Status - 2026-02-24
date: 2026-02-24
---

# Daily Status - 2026-02-24

## Accomplishments

- Removed `extract_compute_instance_template_info` role from osac-aap per reviewer feedback — inlined vars directly in playbook, deleted role directory (PR [osac-aap#207](https://github.com/osac-project/osac-aap/pull/207))
- Added critical missing tests and fixed status clobbering bug in operator — 7 tests for `needsProvisionJob()`, phase regression test, tenant GC race tests; fixed `r.Update()` overwriting in-memory status after finalizer update (all 88 tests pass, 43.6% coverage)
- Fixed warning-level code review findings — corrected log levels, comment typo, and test JobID generation
- Cleaned up API type markers — removed 7 redundant `+kubebuilder:validation:Type=` markers, added explicit `(MUTABLE)` marker to `RestartRequestedAt`
- E2E test passed after `ensureBackwardCompatibility` removal — all 7 checks passed (provision, Running, Halted, Always, cores/memoryGiB/image immutability)
- Completed thorough code review of PR #110 (extend jobs array pattern to ClusterOrder and HostPool) — found 3 critical, 4 warning, 4 note issues
- Fixed all critical and warning issues from PR #110 code review — type switch helpers now return errors on unknown types, EDA webhook fallback replaced with error, factory accepts all 6 EDA webhook URLs
- Added provider tests for ClusterOrder and HostPool — 6 AAP provider tests, 7 EDA provider tests, coverage 70.3% → 72.8%
- Extracted AAP finalizer names to constants and updated all references
- Rebased PR #110 twice on upstream/main, resolved conflicts, all validations pass — PR ready for review
- Removed "backward compatibility" language from `templateParameters` — updated comment and regenerated CRD ([MGMT-23103](https://issues.redhat.com/browse/MGMT-23103), PR [osac-operator#111](https://github.com/osac-project/osac-operator/pull/111))
- Removed `memoryGiB` Maximum=1024 validation per reviewer feedback — provider-agnostic type should not cap memory (PR [osac-operator#111](https://github.com/osac-project/osac-operator/pull/111))
- Deployed operator with memoryGiB fix and E2E test passed — all 7 checks passed

## Risks & Challenges

- Fulfillment-api proto changes for [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) still pending — requires adding explicit spec fields, proto changes, OpenAPI spec regeneration, and lint
- Operator code review (line-by-line) still in progress

## Key Effort

Continued [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance API enhancement — fixing code review findings, adding test coverage, running E2E validation, and preparing PR #110 (ClusterOrder/HostPool jobs array pattern) for review.
