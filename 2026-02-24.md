---
layout: default
title: Daily Status - 2026-02-24
date: 2026-02-24
---

# Daily Status - 2026-02-24

## Accomplishments
- Removed `extract_compute_instance_template_info` role from osac-aap per reviewer feedback — inlined vars directly in playbook ([PR #207](https://github.com/osac-project/osac-aap/pull/207))
- Found and fixed status clobbering bug: `r.Update()` for finalizer was overwriting in-memory status with server response; added 7 `needsProvisionJob()` tests, phase regression test, and tenant GC race tests (88 tests pass, 43.6% coverage)
- Fixed warning-level code review findings: corrected log levels, comment typo, and test JobID generation
- Cleaned up API type markers: removed 7 redundant `+kubebuilder:validation:Type=` markers, added explicit `(MUTABLE)` marker to `RestartRequestedAt`
- E2E test passed after `ensureBackwardCompatibility` removal — all 7 checks passed (provision, Running, Halted, Always, immutability)
- Rebased PR #110 (extend jobs array pattern to ClusterOrder and HostPool) on upstream/main — resolved 8 conflicts, renamed `CLOUDKIT_*` env vars to `OSAC_*`, updated PR description
- Completed code review of PR #110 — found and fixed 3 critical issues (silent failures on unknown resource types, EDA webhook factory gaps, incorrect fallback behavior) and 4 warning-level issues
- Added 13 provider tests for ClusterOrder and HostPool (6 AAP + 7 EDA), bringing provisioning coverage from 70.3% to 72.8%
- Removed "backward compatibility" language from `templateParameters` — updated comment and regenerated CRD ([PR #111](https://github.com/osac-project/osac-operator/pull/111))
- Removed `memoryGiB` Maximum=1024 validation per reviewer feedback — provider-agnostic type should not cap memory ([MGMT-23103](https://issues.redhat.com/browse/MGMT-23103), [PR #111](https://github.com/osac-project/osac-operator/pull/111))

## Risks & Challenges
- E2E test for memoryGiB fix still in progress (deploy + validate)

## Key Effort
Continued [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance API enhancement — addressed reviewer feedback, fixed bugs found during thorough code review, added missing test coverage, and extended the jobs array pattern to ClusterOrder and HostPool.
