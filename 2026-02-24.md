---
layout: default
title: Daily Status - 2026-02-24
date: 2026-02-24
---

# Daily Status - 2026-02-24

## Accomplishments
- Updated `osac-aap` `extract_compute_instance_template_info` role to set `template_parameters` to empty dict instead of parsing `templateParameters | from_json` ([MGMT-23103](https://issues.redhat.com/browse/MGMT-23103)) — PR merged
- Added critical missing tests and fixed status clobbering bug: 7 tests for `needsProvisionJob()`, phase regression test, tenant GC race tests; fixed `r.Update()` overwriting in-memory status after finalizer update — all 88 tests pass, 43.6% coverage
- Fixed warning-level code review findings: corrected `log.Error(nil, ...)` to `log.Info(...)` for expected failures, fixed comment typo, fixed test JobID generation
- Cleaned up API type markers: removed 7 redundant `+kubebuilder:validation:Type=` markers, added explicit `(MUTABLE)` marker to `RestartRequestedAt`, regenerated CRD
- E2E test passed end-to-end after `ensureBackwardCompatibility` removal — all 7 checks passed (provision, Running, Halted, Always, cores/memoryGiB/image immutability)

## Risks & Challenges
- Pending: thorough line-by-line operator code review of PR branch against upstream main still in progress

## Key Effort
Continued [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance API enhancement — added test coverage, fixed a status clobbering bug, cleaned up API markers, and validated with a successful E2E test.
