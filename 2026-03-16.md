---
layout: default
title: Daily Status - 2026-03-16
date: 2026-03-16
---

# Daily Status - 2026-03-16

## Accomplishments
- Continued work on ClusterOrder AAP Direct Ready ([osac-operator#110](https://github.com/osac-project/osac-operator/pull/110)):
  - Aligned with Adrien on the approach for fixing infinite retry behavior: track configuration version on job status and use exponential backoff when retrying unchanged configurations
  - Added configuration version tracking to the shared job status type, which flows through to all resources that use it (ClusterOrder, HostPool, ComputeInstance, SecurityGroup, Subnet, VirtualNetwork)
  - Wired up configuration version population for ClusterOrder and HostPool provisioning jobs (ComputeInstance deferred to a follow-up PR to keep scope tight)
  - Added integration tests verifying configuration version is correctly set on triggered jobs, and marked infinite retry prevention tests as pending for a follow-up PR
  - All validations pass (compile, formatting, lint, tests, CRD generation, image build)
  - Deployed to vmaas-dev and validated end-to-end that configuration version is populated correctly for both ClusterOrder and HostPool provisioning jobs
- Scoped out follow-up work: one PR to populate configuration version in the ComputeInstance controller, and another to implement the exponential backoff retry mechanism

## Risks & Challenges
- Ongoing missile attacks in Israel — reduced availability and focus expected due to security situation

## Key Effort
Implementing configuration version tracking as the foundation for fixing infinite retry behavior in the AAP Direct provisioning flow.
