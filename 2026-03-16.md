---
layout: default
title: Daily Status - 2026-03-16
date: 2026-03-16
---

# Daily Status - 2026-03-16

## Accomplishments
- Aligned with Adrien on the approach for fixing infinite retry behavior in failed provisioning jobs — agreed to use a config version tracking mechanism combined with exponential backoff, where spec changes trigger immediate retries and unchanged failures use backoff
- Split the infinite retry fix into three incremental PRs to keep scope manageable: the first ([osac-operator#110](https://github.com/osac-project/osac-operator/pull/110)) adds config version tracking to job status and populates it for ClusterOrder and HostPool provisioning, follow-ups will extend it to ComputeInstance and implement the actual retry/backoff logic
- Added config version field to the shared job status type, wired it into ClusterOrder and HostPool job triggering, and added integration test coverage verifying correct behavior
- Fixed a test scaffolding issue causing a nil panic when job history trimming ran without proper configuration
- Marked future infinite retry prevention tests as pending with references to the known bug, to be enabled once the retry mechanism is implemented
- All validations passing — pushed updates to [osac-operator#110](https://github.com/osac-project/osac-operator/pull/110)

## Risks & Challenges
- Ongoing missile attacks in Israel — reduced availability and focus expected due to security situation

## Key Effort
Progressing the infinite retry fix for failed provisioning jobs by adding config version tracking to job status and splitting the work into incremental PRs.
