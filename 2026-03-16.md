---
layout: default
title: Daily Status - 2026-03-16
date: 2026-03-16
---

# Daily Status - 2026-03-16

## Accomplishments
- Agreed with Adrien on the approach for fixing infinite retry behavior: use a config version tracking mechanism combined with exponential backoff, allowing immediate retries when the spec changes and delayed retries when the same config fails repeatedly
- Split the implementation into three incremental PRs to keep scope manageable and reviewable
- Added config version tracking to the shared job status type, which flows into all resources that use it (including cluster orders, host pools, compute instances, security groups, subnets, and virtual networks)
- Wired up config version population for cluster order and host pool provisioning jobs, with compute instance deferred to a follow-up PR
- Added integration test assertions to verify config version is correctly set when jobs are triggered
- Fixed a scaffold test issue related to uninitialized job history configuration
- Added pending integration tests for the infinite retry prevention behavior, to be enabled once the backoff mechanism is implemented in a follow-up PR
- All validations pass (compile, format, lint, tests, CRD generation, image build)
- Deployed and validated end-to-end in vmaas-dev that config version is correctly populated on provision jobs for both cluster orders and host pools
- Pushed updates to draft [osac-operator#110](https://github.com/osac-project/osac-operator/pull/110)

## Risks & Challenges
- Ongoing missile attacks in Israel — reduced availability and focus expected due to security situation

## Key Effort
Implementing config version tracking for provision jobs as part of the infinite retry fix for ClusterOrder AAP Direct Ready.
