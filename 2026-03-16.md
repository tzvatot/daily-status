---
layout: default
title: Daily Status - 2026-03-16
date: 2026-03-16
---

# Daily Status - 2026-03-16

## Accomplishments
- Continued work on ClusterOrder AAP Direct Ready ([osac-operator#110](https://github.com/osac-project/osac-operator/pull/110))
- Aligned with Adrien on the infinite retry fix approach: use a config version tracking mechanism with exponential backoff and no halt, allowing immediate retries when the spec changes vs. backoff retries on unchanged failures
- Added config version tracking to the shared job status type, which flows through to all CRDs that use it (ComputeInstance, ClusterOrder, HostPool, SecurityGroup, Subnet, VirtualNetwork)
- Wired up config version population for ClusterOrder and HostPool provision jobs (ComputeInstance deferred to a follow-up PR to limit scope)
- Added integration test coverage verifying config version is set on triggered jobs; marked infinite retry prevention tests as pending for the follow-up backoff PR
- Fixed a scaffold test issue related to nil panic when job history trimming had no max set
- All validations pass (compile, format, lint, tests, CRD generation, image build)
- Deployed and E2E validated on vmaas-dev: confirmed config version is correctly populated on provision jobs for both ClusterOrder and HostPool
- Cleaned up test resources after validation

## Risks & Challenges
- Ongoing missile attacks in Israel — reduced availability and focus expected due to security situation

## Key Effort
Implementing config version tracking for provision jobs to enable an intelligent retry mechanism that distinguishes spec-change retries from failure retries.
