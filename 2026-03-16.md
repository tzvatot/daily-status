---
layout: default
title: Daily Status - 2026-03-16
date: 2026-03-16
---

# Daily Status - 2026-03-16

## Accomplishments
- Continued work on the ClusterOrder AAP Direct Ready feature ([osac-operator#110](https://github.com/osac-project/osac-operator/pull/110))
- Aligned with Adrien on the approach for fixing infinite retry behavior: track a config version on job status and use exponential backoff when the spec hasn't changed, with immediate retry when spec changes are detected
- Added config version tracking to the shared job status type, which flows through to all controllers that use it (ClusterOrder, HostPool, ComputeInstance, SecurityGroup, Subnet, VirtualNetwork)
- Wired up config version population in ClusterOrder and HostPool provisioning flows, with ComputeInstance deferred to a follow-up PR to limit scope
- Added integration tests to verify config version is correctly set on triggered jobs, and scaffolded pending tests for the retry mechanism (to be enabled in a future PR)
- Deployed and validated end-to-end in the dev environment, confirming config version is correctly populated for both ClusterOrder and HostPool provision jobs
- All validations passing (compile, format, lint, tests, CRD generation, image build)

## Risks & Challenges
- Ongoing missile attacks in Israel — reduced availability and focus expected due to security situation

## Key Effort
Implementing config version tracking on job status as the foundation for an exponential backoff retry mechanism in the OSAC operator.
