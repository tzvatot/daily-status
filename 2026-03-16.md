---
layout: default
title: Daily Status - 2026-03-16
date: 2026-03-16
---

# Daily Status - 2026-03-16

## Accomplishments
- Continued work on ClusterOrder AAP Direct Ready ([osac-operator#110](https://github.com/osac-project/osac-operator/pull/110))
- Aligned with Adrien on the infinite retry fix approach: track config version on job status combined with exponential backoff, so spec-change retries happen immediately while failure retries use backoff
- Added config version tracking to the shared job status type, which flows into all resources that use it (ClusterOrder, HostPool, ComputeInstance, SecurityGroup, Subnet, VirtualNetwork)
- Wired up config version population for ClusterOrder and HostPool provision jobs (ComputeInstance deferred to a follow-up PR to limit scope)
- Added integration test coverage verifying config version is set when jobs are triggered, and placeholder tests for the retry behavior (pending until the backoff mechanism is implemented in a follow-up PR)
- Fixed a scaffold test issue related to nil panic when job history trimming was misconfigured
- All validations pass (compile, format, lint, tests, CRD generation, image build)
- Deployed and E2E validated in vmaas-dev: confirmed config version is correctly populated on provision jobs for both ClusterOrder and HostPool

## Risks & Challenges
- Ongoing missile attacks in Israel — reduced availability and focus expected due to security situation

## Key Effort
Implementing config version tracking on job status as the foundation for an exponential backoff retry mechanism to prevent infinite retries in the AAP Direct provider.
