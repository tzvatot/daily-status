---
layout: default
title: Daily Status - 2026-03-16
date: 2026-03-16
---

# Daily Status - 2026-03-16

## Accomplishments
- Continued work on the ClusterOrder AAP Direct Ready feature ([osac-operator#110](https://github.com/osac-project/osac-operator/pull/110))
- Aligned with Adrien on the approach for fixing infinite retry behavior: track a config version on job status and use exponential backoff when the spec hasn't changed, with immediate retry when it has
- Added config version tracking to the shared job status type, which flows through to all controllers that use it (ClusterOrder, HostPool, ComputeInstance, SecurityGroup, Subnet, VirtualNetwork)
- Wired up config version population in ClusterOrder and HostPool provision flows, with ComputeInstance deferred to a follow-up PR to limit scope
- Added integration tests verifying config version is set correctly, and marked future retry-behavior tests as pending until the backoff mechanism is implemented in a subsequent PR
- Deployed to vmaas-dev and validated end-to-end that config version is correctly populated on provision jobs for both ClusterOrder and HostPool
- All validations passing (compile, format, lint, tests, CRD generation, image build)

## Risks & Challenges
- Ongoing missile attacks in Israel — reduced availability and focus expected due to security situation

## Key Effort
Implementing config version tracking on job status as the foundation for an exponential backoff retry mechanism in the OSAC operator.
