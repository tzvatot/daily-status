---
layout: default
title: Daily Status - 2026-03-16
date: 2026-03-16
---

# Daily Status - 2026-03-16

## Accomplishments
- Continued work on ClusterOrder AAP Direct Ready ([osac-operator#110](https://github.com/osac-project/osac-operator/pull/110))
- Aligned with Adrien on the infinite retry fix approach: use a config version tracking mechanism with exponential backoff and no halt, distinguishing spec-change retries from failure retries
- Added config version tracking to job status, a shared type used across multiple controllers (ComputeInstance, ClusterOrder, HostPool, SecurityGroup, Subnet, VirtualNetwork)
- Populated config version when triggering provision jobs in ClusterOrder and HostPool controllers
- Added integration test assertions verifying config version is set on triggered jobs, and marked infinite retry prevention tests as pending for a follow-up PR
- Fixed a scaffold test issue related to nil panic during job trimming
- All validations pass (compile, format, lint, tests, CRD generation, image build)
- Deployed and E2E validated config version behavior on vmaas-dev for both ClusterOrder and HostPool
- Planned follow-up PRs: one to extend config version to ComputeInstance, and another to implement the retry mechanism with exponential backoff

## Risks & Challenges
- Ongoing missile attacks in Israel — reduced availability and focus expected due to security situation

## Key Effort
Implementing config version tracking for job status to support an upcoming exponential backoff retry mechanism in the OSAC operator.
