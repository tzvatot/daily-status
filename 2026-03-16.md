---
layout: default
title: Daily Status - 2026-03-16
date: 2026-03-16
---

# Daily Status - 2026-03-16

## Accomplishments
- Aligned with Adrien on the approach for fixing infinite retry behavior in ClusterOrder provisioning: use a config version tracking mechanism combined with exponential backoff, allowing immediate retries when configuration changes but backing off on repeated failures
- Split the implementation into three incremental PRs to keep changes focused and reviewable
- Added config version tracking to the shared job status type, which will be used across multiple controllers (ClusterOrder, HostPool, ComputeInstance, etc.)
- Wired up config version population in ClusterOrder and HostPool provisioning flows ([osac-operator#110](https://github.com/osac-project/osac-operator/pull/110))
- Added integration tests verifying config version is correctly set when jobs are triggered
- Fixed a scaffold test issue related to uninitialized job history configuration
- Marked infinite retry prevention tests as pending with tracking comments, to be enabled once the backoff mechanism is implemented in a follow-up PR
- All validations passing (compile, format, lint, tests, CRD generation, image build) — pushed updated changes to [osac-operator#110](https://github.com/osac-project/osac-operator/pull/110)

## Risks & Challenges
- Ongoing missile attacks in Israel — reduced availability and focus expected due to security situation

## Key Effort
Implementing config version tracking for provision jobs as the foundation for fixing infinite retry behavior in the OSAC operator.
