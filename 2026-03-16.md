---
layout: default
title: Daily Status - 2026-03-16
date: 2026-03-16
---

# Daily Status - 2026-03-16

## Accomplishments
- Continued work on ClusterOrder AAP Direct Ready feature ([osac-operator#110](https://github.com/osac-project/osac-operator/pull/110))
- Aligned with Adrien on the infinite retry fix approach: use a config version tracking mechanism combined with exponential backoff, so that spec-change retries happen immediately while failure retries use backoff without halting
- Added config version tracking to the shared job status type, which flows through to all controllers that use it (ClusterOrder, HostPool, ComputeInstance, SecurityGroup, Subnet, VirtualNetwork)
- Wired up config version population in ClusterOrder and HostPool provisioning flows (ComputeInstance deferred to a follow-up PR to limit scope)
- Added integration test coverage verifying config version is correctly set when jobs are triggered
- Fixed a scaffold test issue related to uninitialized job history configuration
- Marked infinite retry prevention tests as pending with a known bug comment, to be enabled in a follow-up PR that implements the actual retry mechanism
- All validations passing (compile, format, lint, tests, CRD generation, image build)

## Risks & Challenges
- Ongoing missile attacks in Israel — reduced availability and focus expected due to security situation

## Key Effort
Implementing config version tracking for job status as the foundation for the infinite retry fix in the OSAC operator.
