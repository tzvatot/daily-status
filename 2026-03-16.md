---
layout: default
title: Daily Status - 2026-03-16
date: 2026-03-16
---

# Daily Status - 2026-03-16

## Accomplishments
- Continued work on ClusterOrder AAP Direct Ready feature ([osac-operator#110](https://github.com/osac-project/osac-operator/pull/110))
- Aligned with Adrien on the infinite retry fix approach: use a config version tracking mechanism combined with exponential backoff, so that spec changes trigger immediate retries while unchanged failures use backoff
- Added config version tracking to the shared job status type, which surfaces across multiple CRDs (ComputeInstance, ClusterOrder, HostPool, SecurityGroup, Subnet, VirtualNetwork)
- Wired up config version population for ClusterOrder and HostPool provision jobs, with ComputeInstance deferred to a follow-up PR to limit scope
- Added integration tests verifying config version is set correctly on triggered jobs; marked infinite retry prevention tests as pending for a future PR that will implement the backoff mechanism
- Fixed a scaffold test issue related to nil panic when job history trimming was misconfigured
- All validations passing (compile, format, lint, tests, CRD generation, image build)
- Deployed and validated end-to-end in vmaas-dev: config version correctly populated on provision jobs for both ClusterOrder and HostPool

## Risks & Challenges
- Ongoing missile attacks in Israel — reduced availability and focus expected due to security situation

## Key Effort
Implementing config version tracking and laying the groundwork for exponential backoff retry logic in the OSAC operator's job management system.
