---
layout: default
title: Daily Status - 2026-03-16
date: 2026-03-16
---

# Daily Status - 2026-03-16

## Accomplishments
- Continued work on the ClusterOrder AAP Direct Ready feature ([osac-operator#110](https://github.com/osac-project/osac-operator/pull/110))
- Aligned with Adrien on the approach for preventing infinite retries: track config versions on job status and use exponential backoff, distinguishing between spec-change retries and failure retries
- Added config version tracking to job status across all relevant custom resources (ClusterOrder, HostPool, and the shared job status type)
- Added integration tests verifying config version is correctly set when provision jobs are triggered
- Fixed a scaffold test issue related to uninitialized job history settings
- Marked infinite retry prevention tests as pending, to be enabled once the retry mechanism is implemented in a follow-up PR
- All validations passing (compile, format, lint, tests, CRD generation, image build)
- Deployed and validated end-to-end in vmaas-dev: confirmed config version is correctly populated on provision jobs for both ClusterOrder and HostPool
- Scoped remaining work into follow-up PRs: one for ComputeInstance config version population, another for the exponential backoff retry mechanism

## Risks & Challenges
- Ongoing missile attacks in Israel — reduced availability and focus expected due to security situation

## Key Effort
Implementing config version tracking for job status to support the upcoming infinite retry prevention mechanism in the OSAC operator.
