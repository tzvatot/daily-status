---
layout: default
title: Daily Status - 2026-03-16
date: 2026-03-16
---

# Daily Status - 2026-03-16

## Accomplishments
- Continued work on ClusterOrder AAP Direct Ready ([osac-operator#110](https://github.com/osac-project/osac-operator/pull/110))
- Aligned with Adrien on the infinite retry fix approach: use a config version tracking mechanism combined with exponential backoff, so that spec-change retries happen immediately while failure retries use backoff
- Added config version tracking to the shared job status type, which propagates across all CRDs that use it (ComputeInstance, ClusterOrder, HostPool, SecurityGroup, Subnet, VirtualNetwork)
- Wired up config version population for ClusterOrder and HostPool provision jobs (ComputeInstance deferred to a follow-up PR to limit scope)
- Added integration tests verifying config version is set correctly, and marked infinite retry prevention tests as pending for a future PR that implements the backoff logic
- All validations pass (compile, format, lint, tests, CRD generation, image build)
- Deployed to vmaas-dev and E2E validated that config version is correctly populated on provision jobs for both ClusterOrder and HostPool

## Risks & Challenges
Ongoing missile attacks in Israel — reduced availability and focus expected due to security situation.

## Key Effort
Implementing config version tracking as the foundation for the infinite retry fix in the OSAC operator.
