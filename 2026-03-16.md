---
layout: default
title: Daily Status - 2026-03-16
date: 2026-03-16
---

# Daily Status - 2026-03-16

## Accomplishments
- Aligned with Adrien on the approach for fixing infinite retry behavior in failed provision jobs: use a config version tracking mechanism combined with exponential backoff, so that spec changes trigger immediate retries while unchanged failures back off gracefully
- Added config version tracking to the shared job status type, which surfaces across all resource types that use it (ComputeInstance, ClusterOrder, HostPool, SecurityGroup, Subnet, VirtualNetwork) — [osac-operator#110](https://github.com/osac-project/osac-operator/pull/110)
- Wired up config version population in ClusterOrder and HostPool provision job triggers
- Added integration test assertions to verify config version is correctly set when jobs are triggered
- Fixed a scaffold test issue related to uninitialized job history configuration causing a panic
- Marked infinite retry prevention tests as pending with a known bug comment, to be enabled once the backoff mechanism is implemented in a follow-up PR
- All validations passing (compile, format, lint, tests, CRD generation, image build) — PR [osac-operator#110](https://github.com/osac-project/osac-operator/pull/110) updated and ready for review
- Scoped follow-up work: one PR to extend config version tracking to ComputeInstance, and another to implement the actual exponential backoff retry mechanism

## Risks & Challenges
- Ongoing missile attacks in Israel — reduced availability and focus expected due to security situation

## Key Effort
Implementing config version tracking for provision jobs as the foundation for fixing infinite retry behavior in the OSAC operator.
