---
layout: default
title: Daily Status - 2026-03-16
date: 2026-03-16
---

# Daily Status - 2026-03-16

## Accomplishments
- Aligned with Adrien on the approach for fixing infinite retry behavior in cluster provisioning: use a config version tracking mechanism combined with exponential backoff, distinguishing between spec-change retries (immediate) and failure retries (backoff)
- Added config version tracking to the shared job status type, which flows into all resources that use it (ClusterOrder, HostPool, ComputeInstance, SecurityGroup, Subnet, VirtualNetwork)
- Wired up config version population in ClusterOrder and HostPool provisioning flows, with ComputeInstance deferred to a follow-up PR to limit scope
- Added integration test coverage verifying config version is correctly set when jobs are triggered
- Fixed a test scaffolding issue that caused a nil panic during job history trimming
- Added pending tests for the infinite retry prevention behavior, to be enabled once the backoff mechanism is implemented in a follow-up PR
- All validations passing (compile, format, lint, tests, CRD generation, image build) — pushed updates to [osac-operator#110](https://github.com/osac-project/osac-operator/pull/110)

## Risks & Challenges
- Ongoing missile attacks in Israel — reduced availability and focus expected due to security situation

## Key Effort
Continuing work on the ClusterOrder AAP Direct Ready feature, specifically implementing config version tracking to support the upcoming infinite retry fix.
