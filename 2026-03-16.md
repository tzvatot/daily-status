---
layout: default
title: Daily Status - 2026-03-16
date: 2026-03-16
---

# Daily Status - 2026-03-16

## Accomplishments
- Continued work on ClusterOrder AAP Direct Ready feature ([osac-operator#110](https://github.com/osac-project/osac-operator/pull/110))
- Aligned with Adrien on the infinite retry fix approach: use a config version tracking mechanism combined with exponential backoff, so that spec changes trigger immediate retries while unchanged failures use backoff
- Added config version tracking to the shared job status type, which flows into all controllers that manage provisioning jobs
- Populated config version in ClusterOrder and HostPool provisioning paths (ComputeInstance deferred to a follow-up PR to limit scope)
- Added integration test assertions to verify config version is correctly set when jobs are triggered
- Fixed a scaffold test issue related to nil panic when job history trimming had no max set
- Marked infinite retry prevention tests as pending with a known bug comment, to be enabled in a follow-up PR that implements the backoff mechanism
- All validations passing (compile, format, lint, tests, CRD generation, image build)

## Risks & Challenges
- Ongoing missile attacks in Israel — reduced availability and focus expected due to security situation

## Key Effort
Advancing the ClusterOrder AAP Direct Ready feature by implementing config version tracking for job retries and splitting the work across multiple PRs for clean incremental delivery.
