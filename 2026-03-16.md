---
layout: default
title: Daily Status - 2026-03-16
date: 2026-03-16
---

# Daily Status - 2026-03-16

## Accomplishments
- Continued work on the ClusterOrder AAP Direct Ready feature ([osac-operator#110](https://github.com/osac-project/osac-operator/pull/110))
- Aligned with Adrien on the approach for preventing infinite retries: use a config version tracking mechanism combined with exponential backoff, allowing the system to distinguish between retries caused by spec changes vs. repeated failures
- Added config version tracking to the shared job status type, which surfaces across all resource types that use it (ComputeInstance, ClusterOrder, HostPool, SecurityGroup, Subnet, VirtualNetwork)
- Wired up config version population for ClusterOrder and HostPool provision jobs (ComputeInstance deferred to a follow-up PR to limit scope)
- Added integration tests verifying config version is set correctly, and marked infinite retry prevention tests as pending until the backoff mechanism is implemented in a follow-up PR
- All validations passing (compile, formatting, lint, tests, CRD generation, image build)
- Deployed to vmaas-dev and validated end-to-end that config version is correctly populated on provision jobs for both ClusterOrder and HostPool
- Scoped out follow-up work: one PR to wire up config version in ComputeInstance, and another to implement the exponential backoff retry mechanism

## Risks & Challenges
- Ongoing missile attacks in Israel — reduced availability and focus expected due to security situation

## Key Effort
Implementing config version tracking for provision jobs as the foundation for an intelligent retry mechanism in the OSAC operator.
