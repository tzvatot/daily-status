---
layout: default
title: Daily Status - 2026-03-16
date: 2026-03-16
---

# Daily Status - 2026-03-16

## Accomplishments
- Continued work on the ClusterOrder AAP Direct Ready feature ([osac-operator#110](https://github.com/osac-project/osac-operator/pull/110))
- Aligned with Adrien on the approach for fixing infinite retry behavior: track config versioning on job status combined with exponential backoff, allowing immediate retries when the spec changes and delayed retries for repeated failures
- Added config version tracking to the shared job status type, which surfaces across all CRDs that use it (ComputeInstance, ClusterOrder, HostPool, SecurityGroup, Subnet, VirtualNetwork)
- Wired up config version population for ClusterOrder and HostPool provisioning jobs, with ComputeInstance deferred to a follow-up PR to limit scope
- Added integration tests verifying config version is set correctly on triggered jobs, and marked infinite retry prevention tests as pending until the backoff mechanism is implemented in a subsequent PR
- All validations passing (compile, format, lint, tests, CRD generation, image build)
- Deployed and performed end-to-end validation in vmaas-dev, confirming config version is correctly populated on provision jobs for both ClusterOrder and HostPool

## Risks & Challenges
- Ongoing missile attacks in Israel — reduced availability and focus expected due to security situation

## Key Effort
Implementing config version tracking on job status as the foundation for fixing infinite retry behavior in the AAP Direct provider.
