---
layout: default
title: Daily Status - 2026-03-16
date: 2026-03-16
---

# Daily Status - 2026-03-16

## Accomplishments
- Continued work on ClusterOrder AAP Direct Ready ([osac-operator#110](https://github.com/osac-project/osac-operator/pull/110))
- Aligned with Adrien on the infinite retry fix approach: track config version on job status and use exponential backoff without halting, distinguishing spec-change retries from failure retries
- Added config version tracking to the shared job status type, which flows through to all resource types that use it
- Populated config version when triggering provision jobs in ClusterOrder and HostPool controllers
- Added integration tests verifying config version is set correctly, and marked infinite retry prevention tests as pending for a follow-up PR
- Fixed a scaffold test issue related to nil panic when job history trimming ran without a configured max
- All validations pass (compile, format, lint, tests, CRD generation, image build)
- Deployed and performed E2E validation in vmaas-dev, confirming config version is correctly populated on provision jobs for both ClusterOrder and HostPool
- Scoped follow-up work: one PR to add config version population in ComputeInstance, and another to implement the exponential backoff retry mechanism

## Risks & Challenges
- Ongoing missile attacks in Israel — reduced availability and focus expected due to security situation

## Key Effort
Implementing config version tracking for job status as the foundation for an exponential backoff retry mechanism in the AAP Direct provider.
