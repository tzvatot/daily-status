---
layout: default
title: Daily Status - 2026-03-16
date: 2026-03-16
---

# Daily Status - 2026-03-16

## Accomplishments
- Continued work on ClusterOrder AAP Direct readiness ([osac-operator#110](https://github.com/osac-project/osac-operator/pull/110))
- Aligned with Adrien on the infinite retry fix approach: use a config version tracking mechanism with exponential backoff, allowing immediate retries when the spec changes and gradual backoff when retrying unchanged failures
- Added config version tracking to the shared job status type, which propagates across all controllers that use it (ComputeInstance, ClusterOrder, HostPool, SecurityGroup, Subnet, VirtualNetwork)
- Populated config version when triggering provision jobs in ClusterOrder and HostPool controllers
- Added integration tests verifying config version is set correctly; marked infinite retry prevention tests as pending for a follow-up PR
- Scoped the work to avoid creep: ComputeInstance population and the actual retry/backoff mechanism will follow in separate PRs after [osac-operator#110](https://github.com/osac-project/osac-operator/pull/110) merges
- All validations pass (compile, format, lint, tests, CRD generation, image build)
- Deployed and E2E validated in vmaas-dev that config version is correctly populated on provision jobs for both ClusterOrder and HostPool

## Risks & Challenges
Ongoing missile attacks in Israel — reduced availability and focus expected due to security situation.

## Key Effort
Implementing config version tracking and retry infrastructure for the ClusterOrder AAP Direct provisioning flow.
