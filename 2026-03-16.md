---
layout: default
title: Daily Status - 2026-03-16
date: 2026-03-16
---

# Daily Status - 2026-03-16

## Accomplishments
- Aligned with Adrien on the approach for fixing infinite retry behavior in ClusterOrder provisioning: use a config version tracking mechanism combined with exponential backoff, allowing the system to distinguish between retries due to spec changes (immediate retry) vs retries due to failures (backoff)
- Added config version tracking to the shared job status type, which flows through to all resources that use it (ComputeInstance, ClusterOrder, HostPool, SecurityGroup, Subnet, VirtualNetwork)
- Wired up config version population in ClusterOrder and HostPool provisioning flows, with ComputeInstance deferred to a follow-up PR to keep scope manageable
- Added integration tests to verify config version is correctly set when provision jobs are triggered
- Fixed a test scaffolding issue causing a nil panic during job history trimming
- Added pending integration tests for infinite retry prevention, to be enabled once the backoff mechanism is implemented in a subsequent PR
- All validations passing (compile, format, lint, tests, CRD generation, image build) — pushed updates to [osac-operator#110](https://github.com/osac-project/osac-operator/pull/110)

## Risks & Challenges
- Ongoing missile attacks in Israel — reduced availability and focus expected due to security situation

## Key Effort
Implementing config version tracking for provision jobs as the foundation for fixing infinite retry behavior in the OSAC operator.
