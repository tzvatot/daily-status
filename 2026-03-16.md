---
layout: default
title: Daily Status - 2026-03-16
date: 2026-03-16
---

# Daily Status - 2026-03-16

## Accomplishments
- Continued work on the ClusterOrder AAP Direct Ready feature ([osac-operator#110](https://github.com/osac-project/osac-operator/pull/110))
- Aligned with Adrien on the approach for preventing infinite retries: track a config version on job status combined with exponential backoff, so that spec changes trigger immediate retries while unchanged failures use backoff
- Added config version tracking to the shared job status type and wired it up for ClusterOrder and HostPool provision jobs (ComputeInstance deferred to a follow-up PR to limit scope)
- Added integration test coverage verifying config version is set correctly, plus pending tests for the retry mechanism itself (to be enabled once backoff logic lands in a future PR)
- Fixed a scaffold test issue related to uninitialized job history settings
- All validations passing (compile, format, lint, tests, CRD generation, image build)
- Deployed and end-to-end validated the changes in the dev environment, confirming config version is correctly populated for both ClusterOrder and HostPool provision jobs

## Risks & Challenges
- Ongoing missile attacks in Israel — reduced availability and focus expected due to security situation

## Key Effort
Implementing config version tracking on job status as the foundation for the infinite retry prevention mechanism in the OSAC operator.
