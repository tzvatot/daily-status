---
layout: default
title: Daily Status - 2026-03-05
date: 2026-03-05
---

# Daily Status - 2026-03-05

## Accomplishments
- Investigated broken vmaas-dev cluster environment: identified 2 CIs stuck in `Deleting` phase, a tenant with no phase causing operator requeue loops, and operator not initializing tenants
- Mapped all deployment states across the cluster including fulfillment services (custom build with Secret creation code), OSAC operator, auth proxy, and AAP components
- Created investigation plan: check if `osac-operator:latest` includes tenant controller, compare with previously working image (`9586d09`), verify tenant CRDs, clean up stuck CIs by removing finalizers

## Risks & Challenges
- vmaas-dev cluster is in a broken state — operator creates tenants but never initializes them (no tenant controller in logs), blocking further testing
- 2 CIs stuck in `Deleting` phase need manual cleanup (finalizer removal)
- E2E test with userDataSecretRef Secret creation and GC cleanup blocked until cluster is restored

## Key Effort
Debugging and restoring the broken vmaas-dev cluster environment, focusing on OSAC operator tenant controller issues and stuck CI cleanup.
