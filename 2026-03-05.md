---
layout: default
title: Daily Status - 2026-03-05
date: 2026-03-05
---

# Daily Status - 2026-03-05

## Accomplishments
- Fixed vmaas-dev tenant issue: root cause was upstream operator PR #125 disabled tenant auto-creation from ComputeInstance controller. Tenants must now be pre-created manually. Applied updated tenant CRD, created `vmaas-dev` tenant, set `OSAC_TENANT_NAMESPACE=vmaas-dev` on operator deployment, and re-pulled `osac-operator:latest` with tenant controller.
- E2E test — all functional checks pass: explicit fields verified on K8s CR, userDataSecretRef set correctly, user data Secret created with correct content, provisioning succeeded and reached Running state.

## Risks & Challenges
- CI stuck in Deleting — deprovision jobs fail, blocking finalizer removal. Multiple orphaned CIs accumulating from failed test runs. Secret GC cleanup can't be verified while CR is stuck in Deleting (owner still exists). This is a pre-existing operator deprovision issue, not related to our Secret changes.

## Key Effort
Resolved vmaas-dev tenant configuration and validated end-to-end test pipeline for ComputeInstance provisioning with Secret management.
