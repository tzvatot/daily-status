---
layout: default
title: Daily Status - 2026-03-05
date: 2026-03-05
---

# Daily Status - 2026-03-05

## Accomplishments
- Fixed vmaas-dev tenant issue — root cause was upstream operator PR #125 disabled tenant auto-creation; applied updated tenant CRD, created tenant manually, and set required `OSAC_TENANT_NAMESPACE` env var
- E2E test — all functional checks pass (explicit fields on K8s CR, userDataSecretRef, user data Secret content, provisioning reached Running state)
- Debugged deprovision/deletion failure — fulfillment gRPC server nil pointer panic due to new upstream condition types (`PROVISIONED`, `RESTART_REQUIRED`) not handled in our older build
- Deployed official fulfillment-service latest (`ghcr.io/osac-project/fulfillment-service:latest`) across all 3 deployments; confirmed PR #309 (explicit fields + Secret creation) merged upstream with zero diff against upstream/main

## Risks & Challenges
- Stuck CIs need cleanup before E2E test can be re-run

## Key Effort
End-to-end validation and deployment of the OSAC fulfillment service, resolving tenant configuration and deprovision issues along the way.
