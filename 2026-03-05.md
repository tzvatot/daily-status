---
layout: default
title: Daily Status - 2026-03-05
date: 2026-03-05
---

# Daily Status - 2026-03-05

## Accomplishments
- Fixed vmaas-dev tenant issue — upstream operator PR #125 disabled tenant auto-creation; applied updated tenant CRD, created tenant manually, and set required `OSAC_TENANT_NAMESPACE` env var
- E2E test functional checks all pass — verified explicit fields on K8s CR, userDataSecretRef, user data Secret content, provisioning to Running state
- Debugged deprovision/deletion failure — fulfillment gRPC server nil pointer panic caused by unhandled upstream condition types (`PROVISIONED`, `RESTART_REQUIRED`)
- Deployed official fulfillment-service latest (`ghcr.io/osac-project/fulfillment-service:latest`) across all 3 deployments; confirmed feature branch has zero diff against upstream/main
- Fixed authconfig for new gRPC paths — updated from old `fulfillment.v1` to `osac.public.v1` and corrected service account namespace references
- Built new CLI from monorepo (`fulfillment-service/cmd/fulfillment-cli`) to replace standalone CLI incompatible with new server
- Full E2E test passes end-to-end — create, explicit fields, Secret creation + content, provisioning, Running, and deletion all verified; updated gRPC path and fixed pre-commit tooling (ansible-lint v26.3.0, ansible-core 2.20.3)
- Pushed osac-test-infra and marked [PR #14](https://github.com/osac-project/osac-test-infra/pull/14) ready for review

## Risks & Challenges
- Secret GC cleanup assertion removed from E2E tests (relies on K8s owner reference GC, validated manually) — may want automated verification later
- Fulfillment-service build had drifted from upstream operator changes, causing nil pointer panics on new condition types — need to keep deployments in sync with upstream

## Key Effort
End-to-end validation and deployment of the OSAC fulfillment service with full E2E test coverage on the vmaas-dev environment.
