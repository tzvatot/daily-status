---
layout: default
title: Daily Status - 2026-03-05
date: 2026-03-05
---

# Daily Status - 2026-03-05

## Accomplishments
- Fixed vmaas-dev tenant issue — root cause was upstream operator PR #125 disabled tenant auto-creation; applied updated tenant CRD, created tenant manually, and set required `OSAC_TENANT_NAMESPACE` env var
- E2E test — all functional checks pass (explicit fields on K8s CR, userDataSecretRef, user data Secret content, provisioning reached Running state)
- Debugged deprovision/deletion failure — fulfillment gRPC server nil pointer panic caused by new upstream condition types (`PROVISIONED`, `RESTART_REQUIRED`) not handled in our older build
- Deployed official fulfillment-service latest (`ghcr.io/osac-project/fulfillment-service:latest`) across all 3 deployments; feature branch has zero diff against upstream/main
- Clean up stuck CIs and re-run E2E test (in progress)

## Risks & Challenges
- Fulfillment-service build was out of sync with upstream operator changes, causing deprovision failures — now resolved by deploying latest

## Key Effort
End-to-end validation of the OSAC provisioning and deprovisioning flow, including fixing tenant configuration and fulfillment-service compatibility issues.
