---
layout: default
title: Daily Status - 2026-03-05
date: 2026-03-05
---

# Daily Status - 2026-03-05

## Accomplishments
- Fixed vmaas-dev tenant issue: upstream operator PR #125 disabled tenant auto-creation from ComputeInstance controller; tenants must now be pre-created manually. Applied updated tenant CRD, created `vmaas-dev` tenant, set `OSAC_TENANT_NAMESPACE=vmaas-dev` on operator deployment, and re-pulled `osac-operator:latest` with tenant controller.
- E2E test — all functional checks pass: explicit fields verified on K8s CR, userDataSecretRef set correctly, user data Secret created with correct content, provisioning succeeded and reached Running state.
- Debugged deprovision/deletion failure: deprovision AAP jobs succeed but feedback controller panics updating fulfillment API due to nil pointer panic in JSON encoder — new condition types (`PROVISIONED`, `RESTART_REQUIRED`) from upstream operator not handled by our older fulfillment-service build.
- Deployed official fulfillment-service latest: switched all 3 deployments to `ghcr.io/osac-project/fulfillment-service:latest`. Confirmed PR #309 (explicit fields + Secret creation) is merged upstream; feature branch has zero diff against upstream/main.
- In progress: clean up stuck CIs and re-run E2E test.

## Risks & Challenges
- Fulfillment-service was out of sync with upstream operator changes (new condition types causing nil pointer panics) — resolved by deploying latest.

## Key Effort
Resolved vmaas-dev tenant and fulfillment-service compatibility issues to get E2E provisioning and deprovisioning working end-to-end.
