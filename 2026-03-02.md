---
layout: default
title: Daily Status - 2026-03-02
date: 2026-03-02
---

# Daily Status - 2026-03-02

## Accomplishments

- Rebased all local osac projects on upstream/main — 15 repos total including fulfillment-api, fulfillment-service, fulfillment-cli, fulfillment-common, osac-test-infra, osac-operator, osac-aap, osac-docs, and enhancement-proposals. Resolved conflicts in osac-operator (controller setup refactoring) and osac-aap (deleted file). Skipped two branches with major conflicts or abandoned work.
- Built and deployed fulfillment-service (image `eda5ff9`). Created `rollout-fulfillment-service.sh` script for deploying pre-built images independently and updated `deploy-fulfillment-service.sh` to call the rollout script.
- Fixed missing CRD fields on cluster — E2E test failed because the operator CRD was stale (pre-PR #111), only having 3 fields instead of all 11 spec fields. Applied updated CRD from osac-operator.
- Ran E2E test — all functional checks pass (explicit fields, userDataSecretRef, Secret creation + content, provisioning, Running, deletion). Investigated GC cleanup timeout failure: found stuck CI VM from a previous failed test and deleted it. Discovered `system:admin` impersonation intermittently returns `Forbidden` on secrets instead of `NotFound`, causing false test failures. Reduced GC cleanup timeout to 2 min to match K8s event-driven GC design.

## Risks & Challenges

- `system:admin` impersonation intermittently returns `Forbidden` on secrets, causing false E2E test failures in the GC cleanup check.
- E2E re-run with 2 min GC timeout still pending.

## Key Effort

Rebasing all local osac projects on upstream, deploying fulfillment-service, and debugging E2E test failures related to stale CRDs and GC cleanup timing.
