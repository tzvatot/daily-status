---
layout: default
title: Daily Status - 2026-03-02
date: 2026-03-02
---

# Daily Status - 2026-03-02

## Accomplishments
- Rebased all local osac projects on upstream/main — 15 repos processed including conflict resolution in osac-operator (cmd/main.go) and osac-aap (deleted file conflict)
- Built and deployed fulfillment-service (image `eda5ff9`), created `rollout-fulfillment-service.sh` script for deploying pre-built images independently
- Fixed missing CRD fields on cluster — applied updated CRD from osac-operator to get all 11 spec fields (root cause: stale CRD from before PR #111)
- E2E test functional checks all passing: explicit fields, userDataSecretRef, Secret creation + content, provisioning, Running, deletion
- Investigated GC cleanup test failure — discovered stuck CI from previous failed test, found `system:admin` impersonation intermittently returns `Forbidden` instead of `NotFound`, reduced GC cleanup timeout to 2 min
- Committed E2E test with userDataSecretRef coverage (osac-test-infra-mgmt-23103 `9e01164`)

## Risks & Challenges
- Dev cluster environment broken — official operator image (`ghcr.io/osac-project/osac-operator:latest`) tenant controller not initializing tenants (phase stays empty, stuck in "tenant is not ready, requeueing" loop). Previous custom image has same issue after CRD update. Need to restore working operator + clean cluster state before E2E test can pass end-to-end.

## Key Effort
Rebasing all osac projects, fixing CRD/cluster issues, and advancing E2E test coverage for userDataSecretRef toward a full passing run.
