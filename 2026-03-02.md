---
layout: default
title: Daily Status - 2026-03-02
date: 2026-03-02
---

# Daily Status - 2026-03-02

## Accomplishments
- Rebased all local osac projects on upstream/main — 15 repos total, resolved conflicts in osac-operator (controller setup refactor) and osac-aap (deleted file), skipped 2 repos with major conflicts or abandoned work
- Built and deployed fulfillment-service (image `eda5ff9`) — created `rollout-fulfillment-service.sh` script for deploying pre-built images independently
- Fixed missing CRD fields on cluster — stale operator CRD (pre-PR #111) only had 3 fields, applied updated CRD with all 11 spec fields
- E2E test passing all functional checks: explicit fields, userDataSecretRef, Secret creation + content, provisioning, Running, and deletion
- Investigated GC cleanup test failure — found stuck CI VM from previous failed test, discovered `system:admin` impersonation intermittently returns `Forbidden` instead of `NotFound`, causing false failure
- Reduced GC cleanup timeout from 20 min to 2 min to match expected near-instant K8s event-driven GC behavior

## Risks & Challenges
- `system:admin` impersonation intermittently returns `Forbidden` on secrets, causing false E2E test failures (test checks for `NotFound` in stderr but gets `Forbidden`)
- E2E re-run with 2 min GC timeout still pending

## Key Effort
Rebased all osac repos on upstream, fixed stale CRD blocking E2E tests, and investigated GC cleanup timing and permission issues in the E2E test suite.
