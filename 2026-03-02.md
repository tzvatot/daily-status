---
layout: default
title: Daily Status - 2026-03-02
date: 2026-03-02
---

# Daily Status - 2026-03-02

## Accomplishments
- Rebased all local osac projects on upstream/main — 15 repos including fulfillment-api, fulfillment-service, osac-operator, osac-aap, and others; resolved conflicts in osac-operator (controller setup refactoring) and osac-aap (deleted file conflict)
- Built and deployed fulfillment-service (image `eda5ff9`) — created `rollout-fulfillment-service.sh` script for deploying pre-built images independently
- Fixed missing CRD fields on cluster — root cause was stale operator CRD (pre-PR #111) that only had 3 fields; applied updated CRD with all 11 spec fields
- E2E test — all functional checks pass (explicit fields, userDataSecretRef, Secret creation + content, provisioning, Running, deletion); investigated GC cleanup timeout and discovered `system:admin` impersonation intermittently gets `Forbidden` on secrets causing false failures
- Reduced GC cleanup timeout to 2 min based on K8s GC design (event-driven, not polling); re-run pending

## Risks & Challenges
- `system:admin` impersonation intermittently returns `Forbidden` on secrets instead of `NotFound`, causing false E2E test failures in GC cleanup checks
- Stuck CI VM (`vm-tlgkj`) from a previous failed test was never cleaned up — manually deleted

## Key Effort
Rebasing all osac projects on upstream, fixing stale CRD issues, and stabilizing E2E test reliability around GC cleanup behavior.
