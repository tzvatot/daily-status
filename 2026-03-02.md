---
layout: default
title: Daily Status - 2026-03-02
date: 2026-03-02
---

# Daily Status - 2026-03-02

## Accomplishments

- Rebased all local osac projects on upstream/main — clean rebases, conflict resolution, and skipped already-merged branches across 15 repos (fulfillment-api, fulfillment-service, fulfillment-cli, fulfillment-common, osac-test-infra, osac-operator, osac-aap, osac-docs, enhancement-proposals, and others)
- Built and deployed fulfillment-service (image `eda5ff9`) — created `rollout-fulfillment-service.sh` script for deploying pre-built images independently
- Fixed missing CRD fields on cluster — root cause was stale operator CRD (pre-PR #111); applied updated CRD with all 11 spec fields
- E2E test — all functional checks pass (explicit fields, userDataSecretRef, Secret creation + content, provisioning, Running, deletion)
- Investigated GC cleanup test failure — found stuck CI VM from previous failed test, discovered `system:admin` impersonation intermittently returns `Forbidden` instead of `NotFound` causing false failures, reduced GC cleanup timeout to 2 min

## Risks & Challenges

- `system:admin` impersonation intermittently gets `Forbidden` on secrets, causing false E2E test failures (test expects `NotFound` in stderr but gets `Forbidden`)
- Re-run of E2E test with 2 min GC timeout still pending

## Key Effort

Rebased all osac project branches on upstream, fixed deployment and CRD issues, and drove E2E tests to full functional pass while investigating GC cleanup timing.
