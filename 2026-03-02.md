---
layout: default
title: Daily Status - 2026-03-02
date: 2026-03-02
---

# Daily Status - 2026-03-02

## Accomplishments
- Rebased all local osac projects on upstream/main — 15 repos processed, including conflict resolution in osac-operator (cmd/main.go) and osac-aap (deleted file conflict)
- Built and deployed fulfillment-service (image `eda5ff9`), including new `rollout-fulfillment-service.sh` script for deploying pre-built images independently
- Fixed missing CRD fields on cluster — stale operator CRD (pre-PR #111) was missing spec fields; applied updated CRD with all 11 fields
- E2E test: all functional checks passing (explicit fields, userDataSecretRef, Secret creation + content, provisioning, Running, deletion)
- Investigated GC cleanup test failure — identified stuck CI VM from previous failed test run and deleted it; discovered `system:admin` impersonation intermittently returns `Forbidden` instead of `NotFound`, causing false failures
- Reduced GC cleanup timeout from 20 min to 2 min to match expected near-instant K8s GC behavior

## Risks & Challenges
- `system:admin` impersonation intermittently gets `Forbidden` on secrets, causing false E2E test failures (test checks for `NotFound` in stderr but receives `Forbidden`)
- E2E re-run with 2 min GC timeout still pending

## Key Effort
Rebasing all osac repos on upstream, fixing CRD/deployment issues, and stabilizing E2E test reliability.
