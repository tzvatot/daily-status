---
layout: default
title: Daily Status - 2026-03-02
date: 2026-03-02
---

# Daily Status - 2026-03-02

## Accomplishments
- Rebased all local osac projects on upstream/main — 15 repos processed including clean rebases, skipped already-merged commits, and resolved conflicts in osac-operator and osac-aap
- Built and deployed fulfillment-service (image `eda5ff9`) — created `rollout-fulfillment-service.sh` script for deploying pre-built images independently
- Fixed missing CRD fields on cluster — root cause was stale operator CRD (pre-PR #111); applied updated CRD with all 11 spec fields
- E2E test — all functional checks pass (explicit fields, userDataSecretRef, Secret creation + content, provisioning, Running, deletion)
- Investigated GC cleanup test failure — Secret was deleted after test ended; found stuck CI `vm-tlgkj` from previous failed test and deleted it; discovered `system:admin` impersonation intermittently gets `Forbidden` on secrets causing false failure
- Reduced GC cleanup timeout to 2 min (event-driven K8s GC should be near-instant)

## Risks & Challenges
- `system:admin` impersonation intermittently returns `Forbidden` on secrets instead of `NotFound`, causing false E2E test failures in GC cleanup check
- E2E re-run with 2 min GC timeout still pending

## Key Effort
Rebasing all local osac projects on upstream, deploying updated fulfillment-service, fixing stale CRDs, and driving E2E tests toward full green.
