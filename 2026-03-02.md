---
layout: default
title: Daily Status - 2026-03-02
date: 2026-03-02
---

# Daily Status - 2026-03-02

## Accomplishments
- Rebased all local osac projects on upstream/main — 15 repos processed including conflict resolution in osac-operator (cmd/main.go) and osac-aap (deleted file conflict)
- Built and deployed fulfillment-service (image `eda5ff9`), including new `rollout-fulfillment-service.sh` script for independent image rollouts
- Fixed missing CRD fields on cluster — root cause was stale operator CRD (pre-PR #111), applied updated CRD with all 11 spec fields
- E2E test — all functional checks pass (explicit fields, userDataSecretRef, Secret creation + content, provisioning, Running, deletion)
- Investigated GC cleanup test failure: discovered stuck CI VM from previous failed test, identified `system:admin` impersonation intermittently returning `Forbidden` instead of `NotFound`, causing false failure
- Reduced GC cleanup timeout to 2 min to match expected near-instant K8s GC behavior

## Risks & Challenges
- `system:admin` impersonation intermittently gets `Forbidden` on secrets, causing false E2E test failures (test checks for `NotFound` in stderr but gets `Forbidden`)
- E2E re-run with 2 min GC timeout still pending

## Key Effort
Rebasing all osac repos, fixing CRD issues, and stabilizing E2E test infrastructure for fulfillment-service.
