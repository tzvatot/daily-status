---
layout: default
title: Daily Status - 2026-03-02
date: 2026-03-02
---

# Daily Status - 2026-03-02

## Accomplishments
- Rebased all local osac projects on upstream/main — 15 repos processed including conflict resolution in osac-operator (controller setup refactoring) and osac-aap (deleted file conflict)
- Built and deployed fulfillment-service (image `eda5ff9`), including new `rollout-fulfillment-service.sh` script for deploying pre-built images independently
- Fixed missing CRD fields on cluster — stale operator CRD (pre-PR #111) was missing 8 spec fields; applied updated CRD with all 11 fields
- E2E test passes all functional checks: explicit fields, userDataSecretRef, Secret creation + content, provisioning, Running, and deletion
- Investigated GC cleanup test failure — discovered stuck CI VM from previous failed test run and deleted it; identified `system:admin` impersonation intermittently returning `Forbidden` instead of `NotFound`, causing false failure
- Reduced GC cleanup timeout from 20 min to 2 min to match expected near-instant K8s GC behavior

## Risks & Challenges
- `system:admin` impersonation intermittently gets `Forbidden` on secrets, causing false test failures (test checks for `NotFound` in stderr but receives `Forbidden`)
- E2E re-run with 2 min GC timeout still pending

## Key Effort
Rebased all osac repos, fixed CRD and deployment issues, and drove E2E tests to functional completion while investigating GC cleanup timing and permission edge cases.
