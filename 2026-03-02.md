---
layout: default
title: Daily Status - 2026-03-02
date: 2026-03-02
---

# Daily Status - 2026-03-02

## Accomplishments
- Rebased all local osac projects on upstream/main — 15 repos processed including clean rebases, skipped already-merged commits, and resolved conflicts in osac-operator and osac-aap
- Built and deployed fulfillment-service (image `eda5ff9`); created `rollout-fulfillment-service.sh` script for deploying pre-built images independently
- Fixed missing CRD fields on cluster — root cause was stale operator CRD (pre-PR #111) only had 3 fields; applied updated CRD with all 11 spec fields
- E2E test — all functional checks pass (explicit fields, userDataSecretRef, Secret creation + content, provisioning, Running, deletion); investigated GC cleanup timeout caused by `system:admin` impersonation intermittently returning `Forbidden` instead of `NotFound`; reduced GC cleanup timeout to 2 min
- Committed E2E test with userDataSecretRef coverage (osac-test-infra-mgmt-23103 `9e01164`)

## Risks & Challenges
- Dev cluster environment broken: official operator image (`ghcr.io/osac-project/osac-operator:latest`) tenant controller not initializing tenants (phase stays empty, "tenant is not ready, requeueing" loop). Previous custom image had same issue after CRD update. Need to restore working operator + clean cluster state before E2E test can pass end-to-end.

## Key Effort
Rebasing all osac repos, fixing CRD issues, and validating E2E tests with userDataSecretRef coverage while debugging cluster environment instability.
