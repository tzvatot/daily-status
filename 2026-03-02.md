---
layout: default
title: Daily Status - 2026-03-02
date: 2026-03-02
---

# Daily Status - 2026-03-02

## Accomplishments
- Rebased all local osac projects on upstream/main — 15 repos processed including conflict resolution in osac-operator (cmd/main.go) and osac-aap (deleted file conflict)
- Built and deployed fulfillment-service (image `eda5ff9`), created `rollout-fulfillment-service.sh` script for deploying pre-built images independently
- Fixed missing CRD fields on cluster — applied updated CRD from osac-operator with all 11 spec fields (was stale pre-PR #111)
- E2E test passes all functional checks: explicit fields, userDataSecretRef, Secret creation + content, provisioning, Running, and deletion
- Investigated GC cleanup test failure — discovered stuck CI VM from previous failed test, identified `system:admin` impersonation intermittently returning `Forbidden` instead of `NotFound`, and reduced GC timeout to 2 min

## Risks & Challenges
- `system:admin` impersonation intermittently gets `Forbidden` on secrets, causing false E2E test failures (test checks for `NotFound` in stderr but gets `Forbidden`)
- Re-run of E2E test with 2 min GC timeout still pending

## Key Effort
Rebasing all osac projects on upstream, fixing deployment and CRD issues, and driving E2E test reliability for the fulfillment-service pipeline.
