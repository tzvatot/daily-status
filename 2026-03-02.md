---
layout: default
title: Daily Status - 2026-03-02
date: 2026-03-02
---

# Daily Status - 2026-03-02

## Accomplishments
- Rebased all local osac projects on upstream/main — 15 repos processed including conflict resolution in osac-operator (cmd/main.go) and osac-aap (deleted file conflict)
- Built and deployed fulfillment-service (image `eda5ff9`), created `rollout-fulfillment-service.sh` script for deploying pre-built images independently
- Fixed missing CRD fields on cluster — applied updated CRD from osac-operator to get all 11 spec fields (root cause: stale CRD pre-PR #111)
- E2E test — all functional checks pass: explicit fields, userDataSecretRef, Secret creation + content, provisioning, Running, deletion
- Investigated GC cleanup test failure: found stuck CI VM from previous failed test, discovered `system:admin` impersonation intermittently returns `Forbidden` instead of `NotFound`, causing false failure
- Reduced GC cleanup timeout to 2 min (event-driven K8s GC should be near-instant)

## Risks & Challenges
- `system:admin` impersonation intermittently gets `Forbidden` on secrets, causing false E2E test failures (test checks for `NotFound` in stderr but gets `Forbidden`)
- E2E re-run with 2 min GC timeout still pending

## Key Effort
Rebased all osac repos on upstream, fixed CRD and deployment issues, and drove E2E tests to full functional pass while investigating GC cleanup timing and permission edge cases.
