---
layout: default
title: Daily Status - 2026-03-02
date: 2026-03-02
---

# Daily Status - 2026-03-02

## Accomplishments
- Rebased all local osac projects on upstream/main — 15 repos including fulfillment-api, fulfillment-service, osac-operator, osac-aap, and others; resolved conflicts in osac-operator (controller setup refactoring) and osac-aap (deleted file conflict)
- Built and deployed fulfillment-service (image `eda5ff9`); created `rollout-fulfillment-service.sh` script for deploying pre-built images independently
- Fixed missing CRD fields on cluster — root cause was stale operator CRD (pre-PR #111) that only had 3 fields instead of all 11 spec fields
- E2E test functional checks all passing: explicit fields, userDataSecretRef, Secret creation + content, provisioning, Running, and deletion
- Investigated Secret GC cleanup test failure — identified stuck CI from previous failed test run and discovered `system:admin` impersonation intermittently returns `Forbidden` instead of `NotFound`, causing false failures; reduced GC cleanup timeout to 2 min
- Committed E2E test with userDataSecretRef coverage (osac-test-infra-mgmt-23103 `9e01164`)

## Risks & Challenges
- Dev cluster environment is broken — official operator image (`ghcr.io/osac-project/osac-operator:latest`) has tenant controller not initializing tenants (phase stays empty, stuck in "tenant is not ready, requeueing" loop). Previous custom image has the same issue after CRD update. Need to restore working operator and clean cluster state before E2E test can pass end-to-end.

## Key Effort
Advancing E2E test infrastructure for userDataSecretRef coverage while resolving CRD and cluster environment issues blocking full test execution.
