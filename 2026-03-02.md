---
layout: default
title: Daily Status - 2026-03-02
date: 2026-03-02
---

# Daily Status - 2026-03-02

## Accomplishments
- Rebased all local osac projects on upstream/main — 15 repos processed, including conflict resolution in osac-operator (controller setup refactoring) and osac-aap (deleted file conflict)
- Built and deployed fulfillment-service (image `eda5ff9`); created `rollout-fulfillment-service.sh` script for deploying pre-built images independently
- Fixed missing CRD fields on cluster — stale operator CRD (pre-PR #111) was missing 8 of 11 spec fields; applied updated CRD from osac-operator
- E2E test: all functional checks pass (explicit fields, userDataSecretRef, Secret creation + content, provisioning, Running, deletion); investigated Secret GC cleanup timeout and discovered `system:admin` impersonation intermittently returns `Forbidden` instead of `NotFound`, causing false failures; reduced GC timeout to 2 min
- Committed E2E test with userDataSecretRef coverage (osac-test-infra-mgmt-23103 `9e01164`)
- Cleaned up stuck CI `vm-tlgkj` from previous failed test run

## Risks & Challenges
- Dev cluster environment is broken: official operator image (`ghcr.io/osac-project/osac-operator:latest`) not initializing tenants (phase stays empty, "tenant is not ready, requeueing" loop). Previous custom image (`9586d09`) has the same issue after CRD update and cleanup. Stale CIs and tenants from failed test runs compound the problem. Need to restore working operator + clean cluster state before E2E test can pass end-to-end.

## Key Effort
Rebasing all osac projects, fixing CRD/cluster issues, and advancing E2E test coverage for userDataSecretRef — blocked at end of day by broken operator tenant initialization on the dev cluster.
