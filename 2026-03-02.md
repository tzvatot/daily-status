---
layout: default
title: Daily Status - 2026-03-02
date: 2026-03-02
---

# Daily Status - 2026-03-02

## Accomplishments
- Rebased all local osac projects on upstream/main — 15 repos processed including conflict resolution in osac-operator (cmd/main.go) and osac-aap (deleted file conflict)
- Built and deployed fulfillment-service (image `eda5ff9`), created `rollout-fulfillment-service.sh` script for independent pre-built image deployment
- Fixed missing CRD fields on cluster — applied updated CRD from osac-operator (11 spec fields) to resolve stale CRD issue blocking E2E tests
- E2E test functional checks all passing: explicit fields, userDataSecretRef, Secret creation + content, provisioning, Running, deletion
- Investigated GC cleanup test timeout — identified stuck CI from previous failed test, discovered `system:admin` impersonation intermittently returns `Forbidden` causing false failures, reduced GC timeout to 2 min
- Committed E2E test with userDataSecretRef coverage (osac-test-infra-mgmt-23103 `9e01164`)

## Risks & Challenges
- Dev cluster environment broken: official operator image (`ghcr.io/osac-project/osac-operator:latest`) not initializing tenants (phase stays empty, requeue loop). Need to restore working operator + clean cluster state before E2E test can pass end-to-end.
- `system:admin` impersonation intermittently gets `Forbidden` on secrets, causing false test failures in GC cleanup check.

## Key Effort
Rebasing all osac repos, fixing CRD/E2E test issues, and validating userDataSecretRef coverage end-to-end.
