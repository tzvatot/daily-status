---
layout: default
title: Daily Status - 2026-03-02
date: 2026-03-02
---

# Daily Status - 2026-03-02

## Accomplishments

- Rebased all local osac projects on upstream/main — 15 repositories processed including conflict resolution in osac-operator (controller setup refactoring) and osac-aap (deleted file conflict)
- Built and deployed fulfillment-service (image `eda5ff9`); created `rollout-fulfillment-service.sh` script for independent image rollouts
- Fixed missing CRD fields on cluster — stale operator CRD (pre-PR #111) only had 3 fields, applied updated CRD with all 11 spec fields
- E2E test: all functional checks pass (explicit fields, userDataSecretRef, Secret creation + content, provisioning, Running, deletion); investigated Secret GC cleanup timeout — root cause was `system:admin` impersonation intermittently returning `Forbidden` instead of `NotFound`, plus a stuck CI from a previous failed run
- Reduced GC cleanup timeout from 20 min to 2 min to match K8s event-driven GC design
- Committed E2E test with userDataSecretRef coverage (osac-test-infra-mgmt-23103 `9e01164`)

## Risks & Challenges

- Dev cluster environment is broken — official operator image (`ghcr.io/osac-project/osac-operator:latest`) has tenant controller not initializing tenants (phase stays empty, stuck in "tenant is not ready, requeueing" loop). Previous custom image also affected after CRD update. Need to restore working operator and clean cluster state before E2E test can pass end-to-end.

## Key Effort

Advancing E2E test coverage for userDataSecretRef while rebasing all osac projects and troubleshooting dev cluster operator/tenant initialization issues.
