---
layout: default
title: Daily Status - 2026-03-02
date: 2026-03-02
---

# Daily Status - 2026-03-02

## Accomplishments
- Rebased all local osac projects on upstream/main — 15 repos including fulfillment-api, fulfillment-service, fulfillment-cli, fulfillment-common, osac-test-infra, osac-operator, osac-aap, osac-docs, and enhancement-proposals. Resolved conflicts in osac-operator (cmd/main.go) and osac-aap (deleted file). Skipped osac-operator-aap-direct-everywhere (major conflicts) and osac-operator-updating-phase (abandoned).
- Built and deployed fulfillment-service (image `eda5ff9`). Created `rollout-fulfillment-service.sh` script for deploying pre-built images independently and updated `deploy-fulfillment-service.sh` to call it.
- Fixed missing CRD fields on cluster — E2E test failed because the operator CRD was stale (pre-PR #111), only had `templateID`, `templateParameters`, `restartRequestedAt`. Applied updated CRD with all 11 spec fields.
- E2E test — all functional checks pass (explicit fields, userDataSecretRef, Secret creation + content, provisioning, Running, deletion). Investigated GC cleanup timeout failure: found stuck CI VM from previous failed test and deleted it. Discovered `system:admin` impersonation intermittently returns `Forbidden` on secrets instead of `NotFound`, causing false test failure. Reduced GC cleanup timeout to 2 min.

## Risks & Challenges
- `system:admin` impersonation intermittently gets `Forbidden` on secrets — test expects `NotFound` in stderr but receives `Forbidden`, causing false GC cleanup failures. Needs investigation into RBAC or impersonation behavior.
- E2E re-run with 2 min GC timeout still pending.

## Key Effort
Rebased all osac project branches on upstream/main, fixed stale CRD blocking E2E tests, and investigated GC cleanup timing and permission issues in the E2E test suite.
