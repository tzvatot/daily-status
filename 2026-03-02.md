---
layout: default
title: Daily Status - 2026-03-02
date: 2026-03-02
---

# Daily Status - 2026-03-02

## Accomplishments
- Rebased all local osac projects on upstream/main — 15 repos including fulfillment-api, fulfillment-service, osac-operator, osac-aap, and others; resolved conflicts in osac-operator (controller setup refactoring) and osac-aap (deleted file conflict)
- Built and deployed fulfillment-service (image `eda5ff9`) — created `rollout-fulfillment-service.sh` script for deploying pre-built images independently
- Fixed missing CRD fields on cluster — root cause was stale operator CRD (pre-PR #111) that only had 3 fields; applied updated CRD with all 11 spec fields
- E2E test — all functional checks pass (explicit fields, userDataSecretRef, Secret creation + content, provisioning, Running, deletion); investigated GC cleanup timeout failure caused by stuck CI VM from previous run and intermittent `Forbidden` on `system:admin` impersonation; reduced GC cleanup timeout to 2 min

## Risks & Challenges
- `system:admin` impersonation intermittently returns `Forbidden` on secrets instead of `NotFound`, causing false test failures in GC cleanup check
- E2E GC cleanup test with reduced 2 min timeout still pending re-run

## Key Effort
Rebased all local osac branches on upstream, fixed stale CRD issues, and drove E2E tests to full functional pass while investigating GC cleanup timing and auth intermittency.
