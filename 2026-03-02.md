---
layout: default
title: Daily Status - 2026-03-02
date: 2026-03-02
---

# Daily Status - 2026-03-02

## Accomplishments
- Rebased all local osac projects on upstream/main — 15 repos including fulfillment-api, fulfillment-service, osac-operator, osac-aap, and others; resolved conflicts in osac-operator (controller setup refactor) and osac-aap (deleted file conflict)
- Built and deployed fulfillment-service (image `eda5ff9`); created `rollout-fulfillment-service.sh` script for deploying pre-built images independently
- Fixed missing CRD fields on cluster — root cause was stale operator CRD (pre-PR #111) that only had 3 fields; applied updated CRD with all 11 spec fields
- E2E test — all functional checks pass (explicit fields, userDataSecretRef, Secret creation + content, provisioning, Running, deletion); investigated GC cleanup timeout and discovered `system:admin` impersonation intermittently gets `Forbidden` on secrets causing false failures; reduced GC cleanup timeout to 2 min
- Committed E2E test with userDataSecretRef coverage (osac-test-infra-mgmt-23103 `9e01164`)

## Risks & Challenges
- Dev cluster environment broken — official operator image (`ghcr.io/osac-project/osac-operator:latest`) has tenant controller not initializing tenants (phase stays empty, stuck in "tenant is not ready, requeueing" loop); previous custom image has same issue after CRD update; stale CIs and tenants from failed test runs compounding the problem; need to restore working operator + clean cluster state before E2E test can pass

## Key Effort
Rebasing all osac repos, fixing CRD and E2E test issues, and investigating broken dev cluster operator state.
