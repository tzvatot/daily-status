---
layout: default
title: Daily Status - 2026-03-02
date: 2026-03-02
---

# Daily Status - 2026-03-02

## Accomplishments
- Rebased all local osac projects on upstream/main — 15 repos including fulfillment-api, fulfillment-service, osac-operator, osac-aap, and others. Resolved conflicts in osac-operator (controller setup refactoring) and osac-aap (deleted file conflict). Skipped two branches with major conflicts or abandoned work.
- Built and deployed fulfillment-service (image `eda5ff9`). Created `rollout-fulfillment-service.sh` script for deploying pre-built images independently and updated `deploy-fulfillment-service.sh` to call it.
- Fixed missing CRD fields on cluster — E2E test failed because the operator CRD was stale (pre-PR #111), only had 3 fields instead of all 11 spec fields. Applied updated CRD from osac-operator.
- E2E test — all functional checks pass (explicit fields, userDataSecretRef, Secret creation + content, provisioning, Running, deletion). Investigated GC cleanup timeout: found stuck CI from previous failed test, deleted it. Discovered `system:admin` impersonation intermittently gets `Forbidden` on secrets causing false failures. Reduced GC cleanup timeout to 2 min.
- Committed E2E test with userDataSecretRef coverage (osac-test-infra-mgmt-23103 `9e01164`).

## Risks & Challenges
- Dev cluster environment is broken — deployed official operator image (`ghcr.io/osac-project/osac-operator:latest`) but tenant controller not initializing tenants (phase stays empty, "tenant is not ready, requeueing" loop). Previous custom image had the same issue after CRD update and cleanup. Stale CIs and tenants from failed test runs compounded the problem. Need to restore working operator + clean cluster state before E2E test can pass end-to-end.

## Key Effort
Rebasing all local branches, fixing CRD issues, and validating E2E tests for the userDataSecretRef feature while troubleshooting dev cluster operator initialization failures.
