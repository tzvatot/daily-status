---
layout: default
title: Daily Status - 2026-03-02
date: 2026-03-02
---

# Daily Status - 2026-03-02

## Accomplishments
- Rebased all local osac projects on upstream/main — 15 repos including fulfillment-api, fulfillment-service, osac-operator, osac-aap, and others. Resolved conflicts in osac-operator (controller setup refactoring) and osac-aap (deleted file). Skipped two branches with major conflicts or abandoned work.
- Built and deployed fulfillment-service (image `eda5ff9`). Created `rollout-fulfillment-service.sh` script for deploying pre-built images independently and updated `deploy-fulfillment-service.sh` to call it.
- Fixed missing CRD fields on cluster — E2E test failed because the operator CRD was stale (pre-PR #111), only had 3 fields instead of all 11 spec fields. Applied updated CRD from osac-operator.
- E2E test functional checks all pass: explicit fields, userDataSecretRef, Secret creation + content, provisioning, Running, and deletion. Investigated GC cleanup timeout failure — root cause is `system:admin` impersonation intermittently returning `Forbidden` instead of `NotFound`, causing false failure. Reduced GC cleanup timeout to 2 min to match K8s event-driven GC design. Cleaned up stuck CI from previous failed test run.
- Committed E2E test with userDataSecretRef coverage (osac-test-infra-mgmt-23103 `9e01164`).

## Risks & Challenges
- Dev cluster environment is broken — deployed official operator image (`ghcr.io/osac-project/osac-operator:latest`) but tenant controller is not initializing tenants (phase stays empty, stuck in "tenant is not ready, requeueing" loop). Previous custom image had the same issue after CRD update. Stale CIs and tenants from failed test runs compounded the problem. Need to restore a working operator and clean cluster state before E2E test can pass end-to-end.

## Key Effort
Rebased all osac project branches, fixed CRD and E2E test issues for userDataSecretRef coverage, and investigated dev cluster operator initialization failure blocking full E2E validation.
