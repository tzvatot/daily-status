---
layout: default
title: Daily Status - 2026-03-02
date: 2026-03-02
---

# Daily Status - 2026-03-02

## Accomplishments

- Rebased all local osac projects on upstream/main — 15 repos processed including conflict resolution in osac-operator (controller setup refactor) and osac-aap (deleted file conflict)
- Built and deployed fulfillment-service (image `eda5ff9`), created `rollout-fulfillment-service.sh` script for deploying pre-built images independently
- Fixed missing CRD fields on cluster — stale operator CRD (pre-PR #111) was missing spec fields; applied updated CRD with all 11 fields
- Ran E2E test with all functional checks passing: explicit fields, userDataSecretRef, Secret creation + content, provisioning, Running, and deletion
- Investigated Secret GC cleanup test failure — found stuck CI VM from previous failed test, discovered `system:admin` impersonation intermittently returns `Forbidden` instead of `NotFound`, causing false failure
- Reduced GC cleanup timeout to 2 min (event-driven K8s GC should be near-instant)

## Risks & Challenges

- `system:admin` impersonation intermittently gets `Forbidden` on secrets, causing false E2E test failures (test expects `NotFound` in stderr but receives `Forbidden`)
- E2E re-run with 2 min GC timeout still pending

## Key Effort

Rebasing all osac projects on upstream, deploying fulfillment-service, and debugging E2E test failures related to stale CRDs and GC cleanup timing.
