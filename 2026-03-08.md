---
layout: default
title: Daily Status - 2026-03-08
date: 2026-03-08
---

# Daily Status - 2026-03-08

## Accomplishments
- Created PR for pre-commit Python 3.13 compatibility ([osac-test-infra#15](https://github.com/osac-project/osac-test-infra/pull/15)) — pinned `language_version: python3.13` on ansible-lint hook since ansible-core 2.18.1 doesn't support Python 3.14
- Investigated and implemented fix for [MGMT-23348](https://issues.redhat.com/browse/MGMT-23348): duplicate provisioning job triggered by back-to-back reconciles reading from stale informer cache
  - Fix: `needsProvisionJob` reads from API server (not cache) when no job is found, to confirm provisioning is actually needed
  - Added test reproducing the race (calls Reconcile twice, asserts TriggerProvision called once)
  - Refactored `needsProvisionJob` into `shouldTriggerProvision` — encapsulates job lookup, returns decision + latest job
  - All validations pass: compile, fmt, lint (0 issues), 123 tests, CRDs, image build
- Created draft PR — [osac-operator#131](https://github.com/osac-project/osac-operator/pull/131)
- Built, deployed, and E2E validated on vmaas-dev — full CLI fields test passed: create, explicit fields, Secret, provision, Running, delete

## Risks & Challenges
None

## Key Effort
Investigated and fixed a race condition causing duplicate provisioning jobs in the OSAC operator.
