---
layout: default
title: Daily Status - 2026-02-26
date: 2026-02-26
---

# Daily Status - 2026-02-26

## Accomplishments
- Addressed PR #309 review: fulfillment-service now creates a K8s Secret in hub namespace from userDataSecretRef content instead of passing a plain reference. Separated Secret lifecycle (`ensureUserDataSecret`/`createUserDataSecret`/`updateUserDataSecret`/`deleteUserDataSecret`) from spec building (`addExplicitFields`). Added `gvks.Secret` constant and related constants. Wrote 5 new unit tests covering create, update, skip-when-absent, delete, and delete-when-absent scenarios. Committed `3cf33d4` and pushed to fork.
- Created deploy script for fulfillment-service (`deploy-fulfillment-service.sh`) that handles worktree → main repo cherry-pick → build → push → deploy → reset workflow with interactive y/n prompts at each step.
- Validated old `cloudkit.openshift.io` CRDs for removal — confirmed only 1 resource using the old group (ClusterOrder in `innabox-demo`). Awaiting Adrien's ack before deleting cluster-wide CRDs.

## Risks & Challenges
- Removal of old `cloudkit.openshift.io` CRDs blocked pending Adrien's acknowledgment before deleting cluster-wide resources.
- Multiple open PRs awaiting review/merge: fulfillment-service PR #309, fulfillment-cli PR #110, and osac-test-infra PR #14 (draft, dependent on CLI PR).

## Key Effort
Implementing Secret-based userDataSecretRef handling in fulfillment-service PR #309 and preparing for old CRD cleanup.
