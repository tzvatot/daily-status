---
layout: default
title: Daily Status - 2026-02-26
date: 2026-02-26
---

# Daily Status - 2026-02-26

## Accomplishments
- Addressed fulfillment-service PR #309 review: implemented K8s Secret creation from `userDataSecretRef` content with owner reference for automatic cleanup, immutable create-once semantics, and 4 unit tests
- Created deploy script for fulfillment-service (`deploy-fulfillment-service.sh`) with worktree cherry-pick, build, push, deploy, and reset workflow
- Deployed fulfillment-service and ran E2E test — CLI fields test passed (create, verify fields, provision, Running, delete)
- Created [osac-installer#45](https://github.com/osac-project/osac-installer/pull/45) to add `secrets` create permission to `hub-access` Role (minimal RBAC for fulfillment-service Secret creation)
- Patched dev cluster RBAC as workaround — manually added secrets permissions to hub-access Role on vmaas-dev

## Risks & Challenges
- E2E test with `userDataSecretRef` blocked on RBAC — fulfillment-service now creates Secrets but `hub-access` Role in osac-installer is missing `secrets` permissions (fix in osac-installer#45, pending merge)
- Removal of old `cloudkit.openshift.io` CRDs blocked pending Adrien's ack — validated only 1 resource using old group (ClusterOrder in `innabox-demo`)
- Open PRs awaiting review: fulfillment-service PR #309, fulfillment-cli PR #110, osac-test-infra PR #14 (draft until CLI PR merges)

## Key Effort
Implementing Secret creation with owner references in fulfillment-service (PR #309 review feedback), fixing RBAC gaps, and validating the full flow via E2E testing.
