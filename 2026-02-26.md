---
layout: default
title: Daily Status - 2026-02-26
date: 2026-02-26
---

# Daily Status - 2026-02-26

## Accomplishments
- Addressed PR #309 review in fulfillment-service: create K8s Secret from `userDataSecretRef` content instead of passing a plain reference
  - Separated Secret lifecycle (`ensureUserDataSecret`/`createUserDataSecret`/`updateUserDataSecret`/`deleteUserDataSecret`) from spec building (`addExplicitFields`)
  - Added `gvks.Secret` constant, `userDataSecretSuffix`/`userDataSecretKey` constants
  - Added 5 new unit tests (create, update, skip when absent, delete, delete when absent)
- Created deploy script for fulfillment-service (`deploy-fulfillment-service.sh`) — handles worktree → main repo cherry-pick → build → push → deploy → reset with interactive prompts
- Validated old `cloudkit.openshift.io` CRDs: confirmed only 1 resource using old group (ClusterOrder in `innabox-demo`); awaiting Adrien's ack before removal

## Risks & Challenges
- Removal of old `cloudkit.openshift.io` CRDs blocked pending Adrien's acknowledgment before deleting cluster-wide CRDs
- Multiple open PRs awaiting review/merge: fulfillment-service PR #309, fulfillment-cli PR #110, osac-test-infra PR #14 (draft, depends on CLI PR)

## Key Effort
Enhancing fulfillment-service Secret handling for userDataSecretRef and preparing old CRD cleanup.
