---
layout: default
title: Daily Status - 2026-02-26
date: 2026-02-26
---

# Daily Status - 2026-02-26

## Accomplishments
- Addressed fulfillment-service PR #309 review: implemented K8s Secret creation from `userDataSecretRef` content with owner references for automatic garbage collection, separated Secret lifecycle from spec building, added 6 unit tests
- Created deploy script for fulfillment-service (`deploy-fulfillment-service.sh`) handling worktree cherry-pick, build, push, deploy, and reset with interactive prompts
- Deployed fulfillment-service and ran E2E test — CLI fields test passed (create, verify fields, provision, Running, delete)
- Created PR to osac-installer for hub-access secrets RBAC ([osac-installer#45](https://github.com/osac-project/osac-installer/pull/45)) — added secrets permissions required for userDataSecretRef
- Patched dev cluster RBAC as workaround — manually added secrets permissions to hub-access Role on vmaas-dev

## Risks & Challenges
- E2E test with `userDataSecretRef` blocked on RBAC — `hub-access` Role missing `secrets` permissions; fix submitted via osac-installer#45, awaiting merge
- Old `cloudkit.openshift.io` CRD removal pending Adrien's ack before deleting cluster-wide CRDs

## Key Effort
Implementing Secret management for `userDataSecretRef` in fulfillment-service, including PR review changes, RBAC fixes, and E2E test validation.
