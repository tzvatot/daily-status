---
layout: default
title: Daily Status - 2026-02-26
date: 2026-02-26
---

# Daily Status - 2026-02-26

## Accomplishments

- Addressed fulfillment-service PR #309 review: create K8s Secret from `userDataSecretRef` content instead of passing a plain reference
  - Separated Secret lifecycle (`ensureUserDataSecret`/`createUserDataSecret`/`updateUserDataSecret`/`deleteUserDataSecret`) from spec building (`addExplicitFields`)
  - Added `gvks.Secret` constant, `userDataSecretSuffix`/`userDataSecretKey` constants
  - Added 5 new unit tests (create, update, skip when absent, delete, delete when absent)
- Identified old `cloudkit.openshift.io` CRDs to remove from cluster (`computeinstances`, `clusterorders`, `hostpools`, `tenants`) — causes `kubectl get computeinstance` to default to wrong API group
- Followed up on open PRs: fulfillment-service PR #309, fulfillment-cli PR #110, osac-test-infra PR #14

## Risks & Challenges

- Old `cloudkit.openshift.io` CRDs still present on cluster, causing incorrect API group resolution for `kubectl get computeinstance`
- osac-test-infra PR #14 blocked until fulfillment-cli PR #110 merges

## Key Effort

Reworked fulfillment-service Secret handling for PR #309 review feedback, separating Secret lifecycle management from spec building with full unit test coverage.
