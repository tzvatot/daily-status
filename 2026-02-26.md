---
layout: default
title: Daily Status - 2026-02-26
date: 2026-02-26
---

# Daily Status - 2026-02-26

## Accomplishments
- Addressed fulfillment-service PR #309 review: implemented K8s Secret creation from `userDataSecretRef` content, separated Secret lifecycle management from spec building, added constants and 5 new unit tests (commit `3cf33d4`)
- Created deploy script for fulfillment-service (`deploy-fulfillment-service.sh`) handling worktree cherry-pick, build, push, deploy, and reset with interactive prompts
- Deployed fulfillment-service and ran E2E test — CLI fields test passed (create, verify fields, provision, Running, delete)
- Created PR to osac-installer for hub-access secrets RBAC ([osac-installer#45](https://github.com/osac-project/osac-installer/pull/45)) — adds `secrets` permissions required for fulfillment-service to manage Secrets in hub namespace

## Risks & Challenges
- E2E test with `userDataSecretRef` blocked on RBAC — `hub-access` Role in osac-installer missing `secrets` permissions; workaround is to manually patch dev cluster while waiting for osac-installer#45 to merge
- Removal of old `cloudkit.openshift.io` CRDs pending Adrien's acknowledgment before deleting cluster-wide resources
- Multiple open PRs awaiting review: fulfillment-service PR #309, fulfillment-cli PR #110, osac-test-infra PR #14 (draft, depends on CLI PR)

## Key Effort
Implementing Secret management for `userDataSecretRef` in fulfillment-service and driving the associated PRs, RBAC changes, and E2E validation across multiple repos.
