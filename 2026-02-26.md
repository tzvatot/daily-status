---
layout: default
title: Daily Status - 2026-02-26
date: 2026-02-26
---

# Daily Status - 2026-02-26

## Accomplishments

- Addressed fulfillment-service PR #309 review: service now creates a K8s Secret in hub namespace from userDataSecretRef content instead of passing a plain reference. Separated Secret lifecycle from spec building, added constants, and wrote 5 new unit tests (commit `3cf33d4`, pushed to fork)
- Created deploy script for fulfillment-service (`deploy-fulfillment-service.sh`) — handles worktree → main repo cherry-pick → build → push → deploy → reset with interactive prompts
- Validated old `cloudkit.openshift.io` CRDs: confirmed only 1 resource using old group (ClusterOrder in `innabox-demo`), awaiting Adrien's ack before removal

## Risks & Challenges

- Removal of old `cloudkit.openshift.io` CRDs blocked pending Adrien's acknowledgment — these CRDs cause `kubectl get computeinstance` to default to the wrong API group
- Multiple open PRs awaiting review/merge: fulfillment-service PR #309, fulfillment-cli PR #110, osac-test-infra PR #14 (draft, depends on CLI PR)

## Key Effort

Addressed PR review feedback for fulfillment-service Secret handling and prepared for removal of legacy `cloudkit.openshift.io` CRDs from the cluster.
