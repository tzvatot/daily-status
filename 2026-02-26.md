---
layout: default
title: Daily Status - 2026-02-26
date: 2026-02-26
---

# Daily Status - 2026-02-26

## Accomplishments
- Addressed fulfillment-service PR #309 review: refactored to create a K8s Secret from `userDataSecretRef` content instead of passing a plain reference. Separated Secret lifecycle from spec building, added constants, and wrote 5 new unit tests
- Created deploy script for fulfillment-service (`deploy-fulfillment-service.sh`) — handles worktree cherry-pick, build, push, deploy, and reset with interactive prompts
- Deployed fulfillment-service and ran E2E test — CLI explicit fields test passed (create, verify fields, provision, Running, delete)
- Created [osac-installer#45](https://github.com/osac-project/osac-installer/pull/45) to add `secrets` RBAC permissions to `hub-access` Role, required for fulfillment-service to manage Secrets in hub namespace

## Risks & Challenges
- E2E test with `userDataSecretRef` blocked on RBAC — `hub-access` Role in osac-installer is missing `secrets` permissions; workaround is to manually patch dev cluster while waiting for osac-installer PR to merge
- Removal of old `cloudkit.openshift.io` CRDs pending Adrien's acknowledgment before deleting cluster-wide resources
- Open PRs awaiting review: fulfillment-service #309, fulfillment-cli #110, osac-test-infra #14 (draft until CLI PR merges)

## Key Effort
Implementing and testing Secret management for `userDataSecretRef` in fulfillment-service, including PR review changes, RBAC fixes, and E2E validation.
