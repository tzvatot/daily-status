---
layout: default
title: Daily Status - 2026-05-10
date: 2026-05-10
---

# Daily Status - 2026-05-10

## Accomplishments
- **Cleaned up 21 orphaned OpenStack networks** in vmaas-dev that were left over from failed/force-deleted cluster orders — identified via team Slack report, verified all were orphaned (no active ClusterOrders or routers), deleted all 21, confirmed clean state
- **Addressed open PR comments**: fixed grammar issue in [osac-workspace#17](https://github.com/osac-project/osac-workspace/pull/17) CI failure count message, and re-triggered failing e2e tests on a fulfillment-service PR
- **Closed stale Jira ticket** [MGMT-24156](https://issues.redhat.com/browse/MGMT-24156) — all related PRs were merged weeks ago but ticket was still marked "In Progress"
- **Implemented E2E tests for explicit cluster configuration fields** ([OSAC-150](https://redhat.atlassian.net/browse/OSAC-150)) — added test coverage verifying that explicit cluster config fields flow correctly from CLI through the API (with redaction) to the ClusterOrder custom resource. Opened [osac-test-infra#36](https://github.com/osac-project/osac-test-infra/pull/36), test passing against vmaas-dev
- **Investigated CRD revert issue on vmaas-dev** — confirmed the installer submodule has the correct CRD version, but the dev environment was deployed from an older version and something keeps reverting manual applies. Applied the latest CRD as a workaround; root cause still under investigation
- **Self-reviewed and iterated on** [osac-test-infra#36](https://github.com/osac-project/osac-test-infra/pull/36) — fixed assertion logic, corrected API response handling, and improved test cleanup reliability based on CodeRabbit feedback
- **Reviewed** [osac-workspace#17](https://github.com/osac-project/osac-workspace/pull/17) (AI bot context improvements) — verified all referenced paths against the repo, suggested additional cross-repo dependencies, approved

## Risks & Challenges
- Something is reverting the ClusterOrder CRD on vmaas-dev after manual applies — root cause is unclear and needs further investigation. This could affect E2E test reliability for explicit cluster configuration fields.

## Key Effort
Implemented and validated E2E test coverage for explicit cluster configuration fields, ensuring the new config path works end-to-end from CLI to the cluster order resource.
