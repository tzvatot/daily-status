---
layout: default
title: Daily Status - 2026-05-10
date: 2026-05-10
---

# Daily Status - 2026-05-10

## Accomplishments
- **Cleaned up 21 orphaned OpenStack networks** in vmaas-dev that were left over from failed or force-deleted cluster orders — identified via Slack, verified all were orphaned (no active orders or routers), deleted all, confirmed zero remaining
- **Addressed open PR comments**: fixed a grammar issue in CI failure reporting in [osac-workspace#16](https://github.com/osac-project/osac-workspace/pull/16), and retriggered failing e2e tests on a fulfillment-service PR
- **Closed stale Jira ticket** [MGMT-24156](https://issues.redhat.com/browse/MGMT-24156) — all related PRs had been merged but the ticket was still marked "In Progress"
- **Implemented E2E tests for explicit cluster configuration fields** ([OSAC-150](https://redhat.atlassian.net/browse/OSAC-150)) — added tests that verify explicit config fields flow correctly from the CLI through the API (with redaction) into the ClusterOrder custom resource. Opened [osac-test-infra#36](https://github.com/osac-project/osac-test-infra/pull/36), tests passing against vmaas-dev
- **Investigated CRD revert issue on vmaas-dev** — confirmed the installer submodule has the correct CRD, but the dev environment was deployed from an older version and something keeps reverting manual applies. Re-applied the latest CRD as a workaround; root cause still under investigation
- **Self-reviewed and iterated on** [osac-test-infra#36](https://github.com/osac-project/osac-test-infra/pull/36) — fixed assertion logic for empty template parameters, corrected API response handling, and improved test cleanup reliability based on CodeRabbit feedback
- **Reviewed** [osac-workspace#17](https://github.com/osac-project/osac-workspace/pull/17) (AI bot context improvements) — verified all referenced paths against the repo, suggested additional cross-repo dependencies, approved

## Risks & Challenges
- Something keeps reverting the ClusterOrder CRD on vmaas-dev after manual applies — root cause is unclear and needs further investigation. This may affect E2E test reliability until resolved.

## Key Effort
Implemented and validated end-to-end tests for explicit cluster configuration fields, ensuring the new config path works correctly from CLI to cluster order creation.
