---
layout: default
title: Daily Status - 2026-05-10
date: 2026-05-10
---

# Daily Status - 2026-05-10

## Accomplishments
- **Cleaned up 21 orphaned OpenStack networks** in vmaas-dev that were left over from failed/force-deleted cluster orders — identified via Slack, verified all were orphaned (no active ClusterOrders or routers), deleted all 21, confirmed clean state
- **Addressed open PR comments**: fixed singular/plural grammar in [osac-workspace#16](https://github.com/osac-project/osac-workspace/pull/16) CI failure message; re-triggered failing e2e tests on fulfillment-service PR #470
- **Closed stale Jira ticket** [MGMT-24156](https://issues.redhat.com/browse/MGMT-24156) — all PRs had been merged weeks ago but ticket was still marked "In Progress"
- **Implemented E2E tests for explicit cluster configuration fields** ([OSAC-150](https://redhat.atlassian.net/browse/OSAC-150)) — added test coverage to verify that explicit cluster config fields flow correctly from CLI through the API (with redaction) to the ClusterOrder custom resource; fixed a response parsing bug discovered during testing; opened [osac-test-infra#36](https://github.com/osac-project/osac-test-infra/pull/36)
- **Investigated CRD revert issue on vmaas-dev** — determined the installer submodule has the correct CRD, but vmaas-dev was deployed from an older version and something is still reverting manual CRD updates; applied the latest CRD as a workaround, root cause still under investigation
- **Self-reviewed and iterated on** [osac-test-infra#36](https://github.com/osac-project/osac-test-infra/pull/36) — fixed assertion logic for empty template parameters, corrected API response parsing, improved test cleanup to handle timeout scenarios (per CodeRabbit feedback); all tests passing
- **Reviewed** [osac-workspace#17](https://github.com/osac-project/osac-workspace/pull/17) (AI bot context improvements) — verified referenced file paths, suggested additional cross-repo dependencies, approved

## Risks & Challenges
- Something on vmaas-dev keeps reverting the ClusterOrder CRD to an older version after manual applies — root cause is unclear and needs further investigation; this blocks reliable testing of explicit cluster config fields

## Key Effort
Implementing and validating E2E test coverage for explicit cluster configuration fields, including investigating and working around a CRD revert issue on the dev environment.
