---
layout: default
title: Daily Status - 2026-05-26
date: 2026-05-26
---

# Daily Status - 2026-05-26

## Accomplishments
- Merged [osac-workspace#18](https://github.com/osac-project/osac-workspace/pull/18) — fix for PR notification to link author and reviewer names to GitHub profiles
- Merged [fulfillment-service#584](https://github.com/osac-project/fulfillment-service/pull/584) — fix for value serialization in the custom JSON encoder
- Built and verified catalog items demo flow for [OSAC-58](https://redhat.atlassian.net/browse/OSAC-58) on vmaas-dev: created two catalog items ("Developer Sandbox" with locked fields, "Production Cluster" with editable fields and schema validation), tested cluster creation, validation rules, and end-to-end pipeline through controller reconciliation, ClusterOrder creation, and AAP job launch. AAP jobs triggered successfully but failed on a missing pull secret (environment setup issue, not a catalog items bug)
- Diagnosed and fixed a 4-layer TLS/OAuth failure chain on vmaas-dev blocking the fulfillment controller and Authorino:
  - Fixed controller CA bundle to include both cluster CAs so OIDC discovery to Keycloak succeeds
  - Added newly required IDP configuration flags to the controller deployment for Keycloak admin API access
  - Resolved Authorino pod startup failure caused by a volume mount collision between TLS cert and CA bundle paths, by patching the Authorino CR
  - Restored Authorino's ability to find custom CAs by mounting them on Go's RHEL fallback CA path
- Fixed the pull secret setup for demo clusters — created the expected provider default secret, verified both clusters progressed through the full provisioning pipeline, then cleaned up
- Built a demo recording pipeline and an HTML slide deck with embedded asciinema playback for the catalog items demo; first demo recording completed
- Reviewed [osac-workspace#21](https://github.com/osac-project/osac-workspace/pull/21) (create-pr skill for fork-based PR workflow) — identified missing DCO sign-off check, missing linter integration, and mock generation version risk; posted review requesting changes
- Filed [OSAC-1038](https://redhat.atlassian.net/browse/OSAC-1038) to enforce catalog item name uniqueness per tenant — discovered during demo that duplicate names break CLI name-based lookup; proposed a unique DB constraint

## Risks & Challenges
- **Innabox CA certificate expires 2026-05-28 (2 days).** All TLS connections using this CA (fulfillment gRPC, controller) will break. Needs immediate rotation or renewal.
- This was the 3rd TLS/OAuth incident on vmaas-dev in the past month, indicating fragility in the environment's certificate and auth configuration.

## Key Effort
Prepared and verified the catalog items demo end-to-end on vmaas-dev for [OSAC-58](https://redhat.atlassian.net/browse/OSAC-58), including fixing a multi-layer TLS/OAuth breakage that was blocking the environment.
