---
layout: default
title: Daily Status - 2026-05-26
date: 2026-05-26
---

# Daily Status - 2026-05-26

## Accomplishments
- Merged [osac-workspace#18](https://github.com/osac-project/osac-workspace/pull/18) — fixed PR notification to link author and reviewer names to GitHub profiles.
- Merged [fulfillment-service#584](https://github.com/osac-project/fulfillment-service/pull/584) — fixed value serialization in the custom JSON encoder.
- Progressed on catalog items demo for [OSAC-58](https://redhat.atlassian.net/browse/OSAC-58): created two catalog items ("Developer Sandbox" with locked fields, "Production Cluster" with editable fields and schema validation), verified RBAC behavior, and tested cluster creation and input validation. Demo flow is blocked by the fulfillment controller crash (see below).
- Investigated fulfillment-controller CrashLoopBackOff on vmaas-dev — identified root cause as a TLS CA mismatch between the fulfillment API's CA bundle and the CA that signed the Keycloak TLS certificate, which prevents OAuth discovery and breaks all gRPC calls. Fix identified but not yet applied.

## Risks & Challenges
- **Innabox CA certificate expires 2026-05-28 (2 days).** All TLS connections using this CA (fulfillment gRPC, controller) will break. Needs immediate rotation or renewal.
- Fulfillment controller is crash-looping on vmaas-dev due to the CA mismatch described above, blocking the catalog items demo end-to-end flow.

## Key Effort
Stood up and validated catalog item functionality for the vmaas-dev demo while diagnosing and root-causing the fulfillment controller TLS/OAuth failure that blocks the full demo flow.
