---
layout: default
title: Daily Status - 2026-05-26
date: 2026-05-26
---

# Daily Status - 2026-05-26

## Accomplishments

- Merged [osac-workspace#18](https://github.com/osac-project/osac-workspace/pull/18) — fix for PR notification linking authors and reviewers to GitHub profiles.
- Merged [fulfillment-service#584](https://github.com/osac-project/fulfillment-service/pull/584) — fix for JSON serialization issue in the fulfillment service.
- Built and verified catalog items for the [OSAC-58](https://redhat.atlassian.net/browse/OSAC-58) demo on vmaas-dev: created a "Developer Sandbox" catalog item (non-editable fields) and a "Production Cluster" catalog item (editable fields with schema validation). Tested cluster creation from both, validated input rejection and mutual exclusivity rules, and verified the full end-to-end flow from catalog item selection through controller reconciliation and AAP job launch. AAP jobs were triggered successfully but failed due to a missing pull secret in the environment (not a catalog items issue). Still need to restore the AAP project source to upstream/main.
- Diagnosed and resolved a 4-layer TLS/OAuth failure on vmaas-dev affecting the fulfillment controller and Authorino (the 3rd such incident on this environment). Issues included a CA certificate mismatch preventing OAuth discovery, missing identity provider configuration flags introduced by the latest image, a volume mount conflict preventing Authorino pods from starting, and a custom CA trust path issue. All four layers are now fixed — the controller is running and reconciling, and the authorization pipeline is fully functional.
- Created the provider default credentials needed for AAP provisioning. Re-ran the demo flow end-to-end and confirmed clusters progressed through provisioning into the networking phase successfully.
- Updated the demo script to reference the correct template name. Built a demo recording wrapper for producing MP4 screen recordings.

## Risks & Challenges

- **Innabox CA certificate expires 2026-05-28** (2 days from now). All TLS connections relying on this CA — including fulfillment gRPC and the controller — will break if it is not rotated or renewed.
- The AAP project source on vmaas-dev is still pointed at a non-default branch and needs to be restored to upstream/main before the demo recording.

## Key Effort

Resolved a multi-layer TLS/OAuth outage on vmaas-dev and verified the full catalog items demo flow end-to-end for [OSAC-58](https://redhat.atlassian.net/browse/OSAC-58).
