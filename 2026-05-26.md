---
layout: default
title: Daily Status - 2026-05-26
date: 2026-05-26
---

# Daily Status - 2026-05-26

## Accomplishments
- Merged [osac-workspace#18](https://github.com/osac-project/osac-workspace/pull/18) — fix for PR notification bot to link author and reviewer names to GitHub profiles.
- Merged [fulfillment-service#584](https://github.com/osac-project/fulfillment-service/pull/584) — fix for value serialization in the custom JSON encoder.
- Catalog Items Demo on vmaas-dev ([OSAC-58](https://redhat.atlassian.net/browse/OSAC-58)): created two catalog items ("Developer Sandbox" with locked fields, "Production Cluster" with editable fields and schema validation), verified cluster creation and validation behavior, and ran the full demo flow end-to-end through controller reconciliation and AAP job launch. AAP jobs were triggered successfully but failed due to a missing pull secret in the environment — not a catalog items bug. Still need to update the demo script template variable and restore the AAP project source to upstream/main.
- Diagnosed and fixed a 4-layer TLS/OAuth authentication chain failure between the fulfillment controller, Authorino, and Keycloak on vmaas-dev. Root causes included a missing CA certificate in the trust bundle, new mandatory IDP configuration flags in the latest controller image, a volume mount collision introduced by the Authorino operator, and a CA trust path issue after relocating the certificate mount. All four layers are now resolved — the controller is running and reconciling, and both demo clusters progressed to provisioning.

## Risks & Challenges
- **Innabox CA certificate expires 2026-05-28 (2 days).** All TLS connections relying on this CA (fulfillment gRPC, controller) will break if it is not rotated or renewed before then.
- This was the 3rd TLS/OAuth incident on vmaas-dev in the past month, indicating the environment's certificate and auth configuration is fragile.

## Key Effort
Stood up the catalog items demo end-to-end on vmaas-dev and resolved a multi-layer TLS/OAuth breakage blocking the fulfillment controller.
