---
layout: default
title: Daily Status - 2026-05-26
date: 2026-05-26
---

# Daily Status - 2026-05-26

## Accomplishments

- Merged [osac-workspace#18](https://github.com/osac-project/osac-workspace/pull/18) — fix for PR notification linking author and reviewer names to GitHub profiles
- Merged [fulfillment-service#584](https://github.com/osac-project/fulfillment-service/pull/584) — fix for JSON serialization issue in the custom encoder
- Prepared and verified the Catalog Items demo on vmaas-dev ([OSAC-58](https://redhat.atlassian.net/browse/OSAC-58)):
  - Created two catalog items ("Developer Sandbox" with locked fields, "Production Cluster" with editable fields and schema validation) and verified cluster creation, field validation, and template mutual exclusivity all work correctly
  - Verified full end-to-end demo flow from catalog item creation through controller reconciliation, ClusterOrder creation, and AAP job launch; AAP jobs triggered successfully but failed on a missing pull secret (environment setup issue, not a catalog items bug)
  - Built a demo recording script and an HTML slide deck with embedded asciinema playback; final polished recording still pending
- Diagnosed and fixed a 4-layer TLS/OAuth failure blocking the fulfillment controller and Authorino on vmaas-dev (3rd such incident on this environment):
  - Fixed CA bundle mismatch preventing the controller's OIDC discovery against Keycloak
  - Added newly required identity provider configuration flags to the controller deployment
  - Resolved an Authorino volume mount conflict that prevented pods from starting, by patching the Authorino operator CR
  - Fixed Authorino's CA trust path so custom CAs are found by Go's TLS on RHEL
  - After fixes, re-created demo clusters with a provider default pull secret, verified the full pipeline through to AAP provisioning jobs
- Reviewed [osac-workspace#21](https://github.com/osac-project/osac-workspace/pull/21) (create-pr skill for fork-based PR workflow) — posted Request Changes noting missing DCO sign-off check, missing linter integration, and mock generator version risk
- Filed [OSAC-1038](https://redhat.atlassian.net/browse/OSAC-1038) to enforce catalog item name uniqueness per tenant, found during demo when duplicate names broke CLI name-based lookup

## Risks & Challenges

- **Innabox CA certificate expires 2026-05-28 (2 days).** All TLS connections using this CA (fulfillment gRPC, controller) will break. Needs rotation or renewal.
- vmaas-dev environment continues to be fragile — this was the 3rd TLS/OAuth incident requiring manual debugging and patching. No automated detection or self-healing exists for these failures.

## Key Effort

Prepared and validated the Catalog Items demo end-to-end on vmaas-dev, including diagnosing and fixing a multi-layer TLS/OAuth breakage that was blocking the environment.
