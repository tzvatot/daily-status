---
layout: default
title: Daily Status - 2026-05-26
date: 2026-05-26
---

# Daily Status - 2026-05-26

## Accomplishments
- Merged PR to fix PR notification formatting in the workspace repo ([osac-workspace#18](https://github.com/osac-project/osac-workspace/pull/18))
- Merged PR to fix value serialization in the fulfillment service's custom JSON encoder ([fulfillment-service#584](https://github.com/osac-project/fulfillment-service/pull/584))
- Built and tested catalog items for the [OSAC-58](https://redhat.atlassian.net/browse/OSAC-58) demo on vmaas-dev: created a "Developer Sandbox" item with locked fields and a "Production Cluster" item with editable fields and schema validation; verified cluster creation and input validation work correctly. Full demo flow is blocked by the fulfillment controller being down.
- Diagnosed fulfillment-controller CrashLoopBackOff on vmaas-dev — root-caused to a TLS CA mismatch: the CA bundle used by the controller does not include the CA that signed Keycloak's TLS certificate, causing OAuth discovery to fail and breaking all gRPC calls. Fix identified (patch the CA bundle) but not yet applied.

## Risks & Challenges
- Innabox CA certificate expires **2026-05-28** (2 days). All TLS connections using this CA — including fulfillment gRPC and the controller — will break without rotation or renewal.
- Fulfillment controller on vmaas-dev is crash-looping due to the CA mismatch; the catalog items demo ([OSAC-58](https://redhat.atlassian.net/browse/OSAC-58)) cannot show the full end-to-end flow until the CA bundle is patched and the controller is restarted.

## Key Effort
Catalog items demo setup and diagnosis of the fulfillment controller CA/TLS failure blocking it on vmaas-dev.
