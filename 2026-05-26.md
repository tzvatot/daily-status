---
layout: default
title: Daily Status - 2026-05-26
date: 2026-05-26
---

# Daily Status - 2026-05-26

## Accomplishments
- Merged [osac-workspace#18](https://github.com/osac-project/osac-workspace/pull/18) — PR notification now links author and reviewer names to GitHub profiles
- Merged [fulfillment-service#584](https://github.com/osac-project/fulfillment-service/pull/584) — fixed JSON serialization issue in the fulfillment service
- Built and verified catalog items demo flow on vmaas-dev for [OSAC-58](https://redhat.atlassian.net/browse/OSAC-58): created two catalog items ("Developer Sandbox" with locked fields, "Production Cluster" with editable fields and schema validation), tested cluster creation from both, and verified the full pipeline through controller reconciliation and AAP job launch. AAP jobs triggered successfully but failed due to a missing pull secret in the environment (not a catalog items bug). Fixed the environment issue and re-verified the full pipeline end-to-end
- Created demo recording and an HTML slide deck with embedded asciinema playback for the catalog items demo. Final polished recording still pending
- Diagnosed and fixed a 4-layer TLS/OAuth connectivity failure on vmaas-dev between the fulfillment controller, Authorino, and Keycloak. Root causes included a missing CA certificate in the trust bundle, new mandatory identity provider flags in the latest controller image, a volume mount collision in Authorino preventing pod startup, and a CA trust path issue after relocating the certificate mount. All layers resolved — controller is running and reconciling successfully
- Reviewed [osac-workspace#21](https://github.com/osac-project/osac-workspace/pull/21) (create-pr skill for fork-based PR workflow) — posted request-changes review noting missing DCO sign-off check, missing linter integration, and a code generation version pinning risk
- Filed [OSAC-1038](https://redhat.atlassian.net/browse/OSAC-1038) for catalog item name uniqueness enforcement per tenant — discovered during demo that duplicate names are allowed, breaking name-based CLI lookup

## Risks & Challenges
- **Innabox CA certificate expires 2026-05-28** (2 days from now). All TLS connections using this CA (fulfillment gRPC, controller) will break. Needs rotation or renewal urgently
- This was the 3rd TLS/OAuth incident on vmaas-dev in a month, indicating the dev environment's certificate and auth infrastructure is fragile

## Key Effort
Built and demonstrated the catalog items feature end-to-end on vmaas-dev, including resolving a multi-layer TLS/OAuth environment failure that was blocking the demo pipeline.
