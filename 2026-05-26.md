---
layout: default
title: Daily Status - 2026-05-26
date: 2026-05-26
---

# Daily Status - 2026-05-26

## Accomplishments
- Merged [osac-workspace#18](https://github.com/osac-project/osac-workspace/pull/18) — fixed PR notification to link author and reviewer names to GitHub profiles
- Merged [fulfillment-service#584](https://github.com/osac-project/fulfillment-service/pull/584) — fixed JSON serialization issue in the fulfillment service
- Progressed catalog items demo on vmaas-dev ([OSAC-58](https://redhat.atlassian.net/browse/OSAC-58)): created two catalog items ("Developer Sandbox" with locked fields and "Production Cluster" with editable fields and schema validation), verified cluster creation and input validation work correctly. Demo is blocked by fulfillment controller crash-looping — clusters remain stuck in an unspecified state
- Diagnosed fulfillment-controller crash-loop on vmaas-dev: identified root cause as a TLS CA mismatch — the controller's CA bundle does not include the CA that signed Keycloak's TLS certificate, causing all OAuth discovery and token calls to fail. Fix (patching the CA bundle) is identified but not yet applied

## Risks & Challenges
- **Innabox CA certificate expires 2026-05-28 (2 days)** — all TLS connections relying on this CA (fulfillment gRPC, controller) will break. Needs immediate rotation or renewal
- Fulfillment controller is crash-looping on vmaas-dev, blocking the catalog items demo end-to-end flow. CA bundle patch and controller restart are pending
- This is the third TLS/OAuth-related outage for the fulfillment controller since late April, pointing to a recurring operational gap in certificate and CA management on the dev environment

## Key Effort
Advancing the catalog items demo on vmaas-dev and diagnosing the fulfillment controller TLS failure blocking it.
