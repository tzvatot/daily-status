---
layout: default
title: Daily Status - 2026-05-26
date: 2026-05-26
---

# Daily Status - 2026-05-26

## Accomplishments
- Merged [osac-workspace#18](https://github.com/osac-project/osac-workspace/pull/18) — fix linking PR notification author and reviewer names to GitHub profiles
- Merged [fulfillment-service#584](https://github.com/osac-project/fulfillment-service/pull/584) — fix for value serialization in the custom JSON encoder
- Progressed catalog items demo on vmaas-dev for [OSAC-58](https://redhat.atlassian.net/browse/OSAC-58):
  - Created two demo catalog items ("Developer Sandbox" with locked fields, "Production Cluster" with editable fields and schema validation)
  - Verified end-to-end demo flow: catalog item creation → cluster creation → controller reconciliation → AAP job launch
  - Validated error handling (invalid inputs rejected, mutual exclusivity enforced)
  - Iterated on demo recording script (improved encoding quality, added comparison table, refined retry logic)
  - Final demo recording still pending
- Diagnosed and fixed a 4-layer TLS/OAuth authentication chain failure on vmaas-dev:
  - Fixed CA bundle mismatch preventing the controller from discovering the OAuth token endpoint
  - Added required identity provider configuration flags to the controller deployment
  - Resolved Authorino volume mount conflict causing pod startup failures
  - Fixed Authorino CA trust path so OIDC discovery and JWT validation work correctly
  - Provisioned missing cluster credentials to unblock AAP provisioning jobs
- Reviewed [osac-workspace#21](https://github.com/osac-project/osac-workspace/pull/21) — new skill for fork-based PR workflows; requested changes (missing sign-off checks, missing linter integration, dependency version risk)
- Filed [MGMT-24459](https://redhat.atlassian.net/browse/MGMT-24459) to enforce catalog item name uniqueness per tenant, discovered during demo testing

## Risks & Challenges
- **Innabox CA certificate expires 2026-05-28 (2 days)** — all TLS connections using this CA (fulfillment gRPC, controller) will break; needs immediate rotation or renewal
- This was the 3rd TLS/OAuth incident on vmaas-dev in a month, indicating fragility in the environment's certificate and auth configuration

## Key Effort
Drove the catalog items demo end-to-end on vmaas-dev, including diagnosing and fixing a multi-layer TLS/OAuth breakage that was blocking the demo environment.
