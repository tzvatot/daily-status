---
layout: default
title: Daily Status - 2026-05-26
date: 2026-05-26
---

# Daily Status - 2026-05-26

## Accomplishments
- Merged [osac-workspace#18](https://github.com/osac-project/osac-workspace/pull/18) — fix for PR notification linking authors and reviewers to GitHub profiles
- Merged [fulfillment-service#584](https://github.com/osac-project/fulfillment-service/pull/584) — fix for value serialization in the custom JSON encoder
- Built and verified catalog items demo flow for [OSAC-58](https://redhat.atlassian.net/browse/OSAC-58) on vmaas-dev: created two catalog items ("Developer Sandbox" with locked fields, "Production Cluster" with editable fields and schema validation), tested cluster creation, validation rules, and end-to-end flow through controller reconciliation and AAP job launch. AAP jobs triggered successfully but failed on a missing pull secret (environment setup issue, not a catalog items bug)
- Diagnosed and resolved a 4-layer TLS/OAuth failure chain blocking the fulfillment controller and Authorino on vmaas-dev: fixed CA bundle to include both cluster CAs, added newly required IDP configuration flags to the controller deployment, resolved an Authorino volume mount collision that prevented pods from starting, and corrected the CA trust path so Authorino's OIDC discovery works. Controller is now running and reconciling successfully
- Created the provider default secret with pull secret and SSH key needed for AAP provisioning, verified the full pipeline end-to-end (clusters reached provisioning state, AAP jobs progressed into networking phase)
- Iterated on the demo recording script: improved retry logic, added formatted command output and a side-by-side catalog item comparison table, added a worker node count field, and improved video encoding quality. Updated the demo template variable to match the current environment

## Risks & Challenges
- **Innabox CA certificate expires 2026-05-28 (2 days)** — all TLS connections using this CA (fulfillment gRPC, controller) will break. Needs urgent rotation or renewal
- This was the 3rd TLS/OAuth incident on vmaas-dev in a month, indicating fragility in the environment's certificate and auth configuration
- Final demo recording and AAP project source restoration still pending

## Key Effort
Resolved a multi-layer TLS/OAuth infrastructure failure on vmaas-dev and built out the catalog items demo flow for [OSAC-58](https://redhat.atlassian.net/browse/OSAC-58), getting the end-to-end pipeline working from catalog item creation through AAP job launch.
