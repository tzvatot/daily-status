---
layout: default
title: Daily Status - 2026-05-26
date: 2026-05-26
---

# Daily Status - 2026-05-26

## Accomplishments
- Merged [osac-workspace#18](https://github.com/osac-project/osac-workspace/pull/18) — fixed PR notification to link author and reviewer names to GitHub profiles
- Merged [fulfillment-service#584](https://github.com/osac-project/fulfillment-service/pull/584) — fixed JSON serialization issue in the fulfillment service
- Built and verified catalog items demo on vmaas-dev for [OSAC-58](https://redhat.atlassian.net/browse/OSAC-58): created two catalog item types (fixed-config "Developer Sandbox" and user-editable "Production Cluster"), tested cluster creation, input validation, and full end-to-end flow through controller reconciliation and AAP job launch. Minor demo script cleanup remains.
- Diagnosed and fixed a 4-layer TLS/OAuth authentication chain failure between the fulfillment controller, Authorino, and Keycloak on vmaas-dev:
  - Fixed the controller's CA bundle to include both cluster CAs so OAuth token discovery could reach Keycloak
  - Added newly required identity provider configuration flags to the controller deployment
  - Resolved an Authorino pod startup failure caused by a volume mount collision between the TLS cert and CA bundle paths
  - Ensured Authorino could find the custom CAs by mounting them at a path Go's TLS stack automatically reads on RHEL
- Confirmed the full chain is now working: controller is reconciling, clusters are progressing, and AAP provisioning jobs are being triggered

## Risks & Challenges
- **Innabox CA certificate expires 2026-05-28 (2 days)** — all TLS connections using this CA (fulfillment gRPC, controller) will break and require certificate rotation or renewal
- AAP provisioning jobs failed due to a missing pull secret in the demo environment (environment setup issue, not a code bug)
- This is the 3rd TLS/OAuth incident on vmaas-dev in the past month, indicating fragility in the environment's certificate and auth configuration

## Key Effort
Enabled the catalog items demo end-to-end on vmaas-dev and resolved a multi-layer TLS/OAuth failure blocking the fulfillment controller and Authorino.
