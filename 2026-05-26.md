---
layout: default
title: Daily Status - 2026-05-26
date: 2026-05-26
---

# Daily Status - 2026-05-26

## Accomplishments

- Merged [osac-workspace#18](https://github.com/osac-project/osac-workspace/pull/18) — PR notifications now link author and reviewer names to GitHub profiles
- Merged [fulfillment-service#584](https://github.com/osac-project/fulfillment-service/pull/584) — fixed JSON serialization issue in the fulfillment service
- Progressed catalog items demo on vmaas-dev ([OSAC-58](https://redhat.atlassian.net/browse/OSAC-58)): created two catalog items ("Developer Sandbox" and "Production Cluster"), verified cluster creation, field validation, and end-to-end demo flow through controller reconciliation and AAP job launch. AAP jobs triggered successfully but failed on a missing pull secret (environment setup issue, not a catalog items bug). Demo script update and AAP project source restoration still pending
- Diagnosed and resolved a 4-layer TLS/OAuth authentication chain failure on vmaas-dev affecting the fulfillment controller and Authorino:
  - Fixed CA bundle mismatch that prevented the controller from discovering the OAuth token endpoint
  - Added required IDP configuration flags to the controller deployment for Keycloak admin API access
  - Resolved an Authorino volume mount conflict that prevented new pods from starting
  - Corrected Authorino's CA trust path so OIDC discovery and JWT validation work correctly
- After fixing the auth chain, resolved the missing pull secret issue in the provider environment, re-ran the full demo flow end-to-end, and confirmed AAP provisioning jobs progressed into infrastructure provisioning successfully
- Prepared demo recording tooling and updated demo script configuration

## Risks & Challenges

- **Innabox CA certificate expires 2026-05-28 (2 days)** — all TLS connections using this CA (fulfillment gRPC, controller) will break and need rotation or renewal
- This was the 3rd TLS/OAuth incident on vmaas-dev in recent weeks, indicating fragility in the environment's certificate and auth configuration

## Key Effort

Resolved a multi-layer TLS/OAuth authentication chain failure on vmaas-dev and verified the catalog items demo flow end-to-end for [OSAC-58](https://redhat.atlassian.net/browse/OSAC-58).
