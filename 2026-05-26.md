---
layout: default
title: Daily Status - 2026-05-26
date: 2026-05-26
---

# Daily Status - 2026-05-26

## Accomplishments
- Merged [osac-workspace#18](https://github.com/osac-project/osac-workspace/pull/18) — fix for PR notification to link author and reviewer names to GitHub profiles
- Merged [fulfillment-service#584](https://github.com/osac-project/fulfillment-service/pull/584) — fix for value serialization in the custom JSON encoder
- Progressed on catalog items demo on vmaas-dev ([OSAC-58](https://redhat.atlassian.net/browse/OSAC-58)): created two catalog items ("Developer Sandbox" with locked fields and "Production Cluster" with editable fields and schema validation), verified RBAC behavior, and tested cluster creation and validation from both items. Demo flow is blocked — clusters stuck at unspecified state due to fulfillment controller crash-looping
- Investigated fulfillment-controller crash-loop on vmaas-dev and identified root cause: the CA bundle used by the controller does not include the CA that signed Keycloak's TLS certificate, causing OAuth discovery to fail and breaking all gRPC calls. Fix identified but not yet applied

## Risks & Challenges
- **Innabox CA certificate expires 2026-05-28 (2 days)** — all TLS connections using this CA (fulfillment gRPC, controller) will break if not rotated or renewed
- Fulfillment controller is crash-looping on vmaas-dev, blocking the catalog items demo end-to-end flow. CA bundle patch and controller restart still pending

## Key Effort
Catalog items demo setup and debugging the fulfillment controller TLS/OAuth failure blocking the demo on vmaas-dev.
