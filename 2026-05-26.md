---
layout: default
title: Daily Status - 2026-05-26
date: 2026-05-26
---

# Daily Status - 2026-05-26

## Accomplishments
- Merged [osac-workspace#18](https://github.com/osac-project/osac-workspace/pull/18) — fix for PR notification to link author and reviewer names to GitHub profiles
- Merged [fulfillment-service#584](https://github.com/osac-project/fulfillment-service/pull/584) — fix for value serialization in the custom JSON encoder
- Progressed catalog items demo on vmaas-dev ([OSAC-58](https://redhat.atlassian.net/browse/OSAC-58)): created two catalog items ("Developer Sandbox" with locked fields, "Production Cluster" with editable fields and schema validation), verified end-to-end demo flow from catalog item creation through cluster ordering and AAP job launch. AAP jobs triggered successfully but failed on a missing pull secret (environment setup issue, not a catalog items bug). Still need to record the final demo and restore AAP project source to upstream
- Diagnosed and resolved a 4-layer TLS/OAuth breakage in the fulfillment controller and Authorino on vmaas-dev: fixed CA bundle mismatch for Keycloak, added newly required IDP configuration flags to the controller deployment, resolved an Authorino volume mount collision that prevented pods from starting, and corrected the CA trust path so Authorino's OIDC discovery works. Controller is now running and reconciling successfully
- Created a provider default secret with pull secret and SSH key to unblock AAP provisioning jobs; verified the full pipeline end-to-end (clusters reached provisioning state, AAP jobs progressed past pull secret into networking)
- Built and iterated on a demo recording script with improved encoding quality, visible catalog item creation commands, side-by-side comparison tables, and smarter retry logic. Final recording still pending
- Reviewed [osac-workspace#21](https://github.com/osac-project/osac-workspace/pull/21) (create-pr skill for fork-based PR workflow) — flagged missing DCO sign-off check, missing linter integration, and mock generation version risk; submitted as Request Changes
- Filed [MGMT-24459](https://redhat.atlassian.net/browse/MGMT-24459) to enforce catalog item name uniqueness per tenant — discovered during demo that duplicate names break CLI name-based lookup

## Risks & Challenges
- **Innabox CA certificate expires 2026-05-28 (2 days).** All TLS connections relying on this CA (fulfillment gRPC, controller) will break. Needs immediate rotation or renewal.
- This is the third TLS/OAuth incident on vmaas-dev in a month, indicating fragility in the environment's certificate and auth configuration.

## Key Effort
Stood up and validated the catalog items demo end-to-end on vmaas-dev, which required resolving a multi-layer TLS/OAuth breakage in the fulfillment controller and Authorino.
