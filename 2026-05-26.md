---
layout: default
title: Daily Status - 2026-05-26
date: 2026-05-26
---

# Daily Status - 2026-05-26

## Accomplishments
- Merged [osac-workspace#18](https://github.com/osac-project/osac-workspace/pull/18) — fix for PR notification linking author and reviewer names to GitHub profiles
- Merged [fulfillment-service#584](https://github.com/osac-project/fulfillment-service/pull/584) — fix for JSON serialization issue in the custom encoder
- Verified and demonstrated the catalog items feature on vmaas-dev for [OSAC-58](https://redhat.atlassian.net/browse/OSAC-58): created two catalog items ("Developer Sandbox" with locked fields, "Production Cluster" with editable fields and schema validation), tested cluster creation from both, and validated the full end-to-end flow through controller reconciliation and AAP job launch. Final demo recording still pending
- Diagnosed and resolved a multi-layer TLS/OAuth failure on vmaas-dev affecting the fulfillment controller and Authorino — fixed CA trust bundle to include both cluster CAs, added required IDP configuration flags to the controller, resolved an Authorino volume mount conflict via its operator CR, and corrected the CA trust path so Authorino's OIDC discovery succeeds. This is the third TLS/auth incident on vmaas-dev in the past month
- Fixed a missing provider credentials issue on vmaas-dev that was blocking AAP provisioning jobs, verified both demo clusters progressed through the full pipeline, then cleaned up
- Built a demo recording script for the catalog items demo, including improved encoding quality, retry logic for transient errors, and a cluster comparison table showing differences between catalog item configurations
- Reviewed [osac-workspace#21](https://github.com/osac-project/osac-workspace/pull/21) (create-pr skill for fork-based PR workflow, [OSAC-436](https://redhat.atlassian.net/browse/OSAC-436)) — requested changes for missing DCO sign-off check, missing linter integration, and mockgen version pinning
- Filed [MGMT-24459](https://redhat.atlassian.net/browse/MGMT-24459) for a catalog item name uniqueness gap discovered during the demo — duplicate names can break name-based lookup in the CLI

## Risks & Challenges
- **Innabox CA certificate expires 2026-05-28 (2 days)** — all TLS connections using this CA (fulfillment gRPC, controller) will break and need rotation or renewal
- Recurring TLS/auth instability on vmaas-dev (third incident in a month) continues to consume significant debugging time
- AAP project source still pointed at a fork branch instead of upstream/main

## Key Effort
Verified the catalog items demo flow end-to-end on vmaas-dev for [OSAC-58](https://redhat.atlassian.net/browse/OSAC-58), including resolving a multi-layer TLS/OAuth environment failure that was blocking the controller and Authorino.
