---
layout: default
title: Daily Status - 2026-09-03
date: 2026-09-03
---

# Daily Status - 2026-09-03

## Accomplishments
- **[OSAC-4441](https://redhat.atlassian.net/browse/OSAC-4441):** Rebased PR [osac#646](https://github.com/osac-project/osac/pull/646) (order-independent ClusterOrder feedback conditions) onto main to fix CI failures caused by a trailing newline regression and a linter incompatibility with the newer Go version. CI re-triggered; awaiting results. A pre-existing flaky unit test remains unrelated to this PR.
- **[OSAC-4441](https://redhat.atlassian.net/browse/OSAC-4441):** Continued SNO + OSAC deployment on the caas-ci lab host to validate the PROGRESSING condition changes from [osac#646](https://github.com/osac-project/osac/pull/646) in a real provisioning flow.
  - Resolved yesterday's cluster-creation blocker — root cause was an outdated CLI tool that mishandled static network config input; updated the tool and the install proceeded.
  - Successfully installed OCP 4.21 SNO — cluster is healthy with all operators available.
  - Hit a transient timing issue where the recipe attempted to verify cluster login before OpenShift APIs were fully ready post-install; self-resolved after the cluster finished settling.
  - Hit a critical disk-full blocker (70 GB root filesystem exhausted by VM images and container storage) that paused the VM mid-install. Recovered by relocating VM disk images to a larger partition (372 GB free) via a transparent symlink, then resumed the VM and verified OCP recovered fully.
  - Cleaned up the failed OSAC infrastructure Helm release and re-launched the OSAC installation in a persistent remote session; left running overnight.

## Risks & Challenges
- OSAC installation outcome is unknown — left running overnight. Disk-full could recur if container storage grows further on the root partition; only VM images were relocated.
- The caas-ci installation depends on several OLM-managed operators (CNV, MCE, AAP, cert-manager, AMQ Streams) that can take 15–30+ minutes each and may time out.
- Real validation of the PROGRESSING condition behavior is still blocked on the OSAC install completing, building the CLI, and creating a test cluster.

## Key Effort
Driving the end-to-end lab deployment of SNO + OSAC to validate the order-independent PROGRESSING feedback conditions introduced in [osac#646](https://github.com/osac-project/osac/pull/646) ([OSAC-4441](https://redhat.atlassian.net/browse/OSAC-4441)).
