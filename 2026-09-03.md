---
layout: default
title: Daily Status - 2026-09-03
date: 2026-09-03
---

# Daily Status - 2026-09-03

## Accomplishments

- Continued work on [OSAC-4441](https://redhat.atlassian.net/browse/OSAC-4441) — rebased PR [osac#646](https://github.com/osac-project/osac/pull/646) (order-independent ClusterOrder feedback conditions) to fix CI failures caused by a stale branch and a trailing newline issue. CI re-triggered; awaiting confirmation that it passes. A pre-existing flaky unit test remains unrelated to this PR.
- Continued SNO + OSAC deployment on a bare-metal lab host to validate the PROGRESSING condition behavior with a real ClusterOrder provisioning flow ([OSAC-4441](https://redhat.atlassian.net/browse/OSAC-4441)):
  - Resolved yesterday's cluster-creation blocker — root cause was an outdated CLI tool that misinterpreted a config value as a raw string instead of a file path; updated the tool to the current version.
  - Successfully installed OCP 4.21.31 as a single-node cluster on the lab VM.
  - Hit a transient timing issue where the OSAC installer's cluster-login check ran before OpenShift APIs were fully available post-install; self-resolved after the cluster finished settling.
  - Hit a critical blocker: the host's root filesystem (70 GB) filled up during OSAC infrastructure deployment, pausing the VM due to I/O errors. Freed space by relocating VM disk images to a larger partition (372 GB free) via a symlink, making the fix transparent and permanent for future runs.
  - Cleaned up the partially installed OSAC infrastructure Helm release and re-launched the full OSAC installation in a background session, left running overnight.

## Risks & Challenges

- OSAC installation outcome is unknown — left running overnight. Disk-full could recur if container storage grows on the root partition; the symlink fix only covers VM images.
- The lab deployment requires several OLM-managed operators (CNV, MCE, AAP, cert-manager, AMQ Streams) that can take 15–30+ minutes to install and may time out.
- Real end-to-end validation of the PROGRESSING condition is still blocked on the OSAC installation completing successfully, followed by building the CLI tool and creating a test cluster.

## Key Effort

Deploying a full OSAC environment on a bare-metal SNO cluster to validate the order-independent PROGRESSING feedback condition changes in [osac#646](https://github.com/osac-project/osac/pull/646).
