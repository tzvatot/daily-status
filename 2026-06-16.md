---
layout: default
title: Daily Status - 2026-06-16
date: 2026-06-16
---

# Daily Status - 2026-06-16

## Accomplishments
- Investigated cluster provisioning failures on test-infra-cluster-caas — identified two root causes: missing pull secret in catalog item configuration, and missing bare metal agent infrastructure (`hardware-inventory` namespace not set up). Flagged the infrastructure gap to the team via Slack. Zero successful cluster provisioning jobs have ever run on this environment.
- Created [OSAC-1552](https://redhat.atlassian.net/browse/OSAC-1552) to track a validation gap where the server silently accepts cluster creation without required editable fields being provided.
- Reviewed [OSAC-1415](https://redhat.atlassian.net/browse/OSAC-1415) (cluster upgrade feature gaps) and posted a comment outlining 6 open design areas for the enhancement proposal, including upgrade cancellation, scheduling, maintenance windows, and notifications.
- Reviewed CodeRabbit comments on [osac-test-infra#44](https://github.com/osac-project/osac-test-infra/pull/44) (E2E tests for catalog item lifecycle) — all comments were already addressed; CodeRabbit approved.
- Reviewed [fulfillment-service#686](https://github.com/osac-project/fulfillment-service/pull/686) (database triggers for catalog item referential integrity) — posted request for changes with 2 important findings and 2 suggestions.
- Replied to Alberto on Slack with guidance on catalog item creation via CLI, explaining why a dedicated subcommand with flags isn't practical for this use case.
- Created epic [OSAC-1555](https://redhat.atlassian.net/browse/OSAC-1555) for an OpenShift AI cluster catalog item, scoped to provide a catalog item pre-configured for AI workloads. Linked to [OSAC-1531](https://redhat.atlassian.net/browse/OSAC-1531).
- Implemented catalog item create subcommands for the CLI ([OSAC-1542](https://redhat.atlassian.net/browse/OSAC-1542)), adding dedicated subcommands for each catalog item type under `osac create`. All unit tests pass. Submitted draft [fulfillment-service#710](https://github.com/osac-project/fulfillment-service/pull/710), awaiting review.

## Risks & Challenges
- Cluster provisioning on test-infra-cluster-caas is blocked — the environment lacks bare metal agent infrastructure, and all available templates require it. No clusters can be successfully provisioned until the infra team sets up the necessary namespace and registers agents. The controller is stuck in a retry loop re-triggering failed jobs.

## Key Effort
Investigated and diagnosed cluster provisioning failures on test-infra-cluster-caas, identifying missing infrastructure dependencies that block all cluster creation on the environment.
