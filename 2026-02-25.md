---
layout: default
title: Daily Status - 2026-02-25
date: 2026-02-25
---

# Daily Status - 2026-02-25

## Accomplishments

- [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance API Enhancement — completed full dependency chain updates across fulfillment stack:
  - **fulfillment-common**: Regenerated from fulfillment-api v0.0.30, tagged v0.0.40 after PR #63 merge
  - **fulfillment-service**: Added explicit fields to private API proto, regenerated public API from v0.0.30, updated go.mod to fulfillment-common v0.0.40 — all checks pass (PR #308)
  - **fulfillment-cli**: Added 10 new explicit field flags (`--cores`, `--memory-gib`, `--image`, `--image-source-type`, `--ssh-key`, `--boot-disk-size`, `--boot-disk-storage-class`, `--additional-disk`, `--run-strategy`, `--user-data-secret-ref`) with kubectl-style compound flag parsing, updated to fulfillment-common v0.0.40 — all checks pass (PR #110)
- Started E2E test for CLI with explicit spec fields in osac-test-infra (`test_compute_instance_cli_explicit_fields` role)

## Risks & Challenges

- E2E test for CLI explicit fields still in progress — needs completion before full validation of the CLI path

## Key Effort

Drove the [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance explicit spec fields through the entire fulfillment dependency chain (common, service, CLI) and began E2E test coverage.
