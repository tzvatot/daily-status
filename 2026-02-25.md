---
layout: default
title: Daily Status - 2026-02-25
date: 2026-02-25
---

# Daily Status - 2026-02-25

## Accomplishments

- **[MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance API Enhancement — full stack implementation across 6 repos:**
  - fulfillment-api PR #62 merged and tagged v0.0.30, BSR publish completed
  - fulfillment-common regenerated from v0.0.30, PR #63 merged, tagged v0.0.40
  - fulfillment-service: added explicit fields to private API proto, regenerated public API from v0.0.30, updated go.mod to fulfillment-common v0.0.40, mapped explicit fields in `buildSpec()` controller (PR #308 merged, PR #309 open)
  - Fixed hub-access RBAC: added `osac.openshift.io` rules (was only `cloudkit.openshift.io`)
  - fulfillment-cli: added 10 new explicit field flags (`--cores`, `--memory-gib`, `--image`, `--additional-disk` with kubectl-style format, etc.), updated go.mod (PR #110 open)
  - osac-test-infra: wrote and passed full-stack E2E test (`test_compute_instance_cli_fields`) validating CLI → fulfillment-service → operator → provisioning flow (PR #14 draft, depends on CLI PR #110)
  - Cleaned up 56 leftover test ComputeInstances from DB via CLI

## Risks & Challenges

- fulfillment-service deployment pending — worktree `.git` file breaks container build, requires cherry-pick to main repo
- osac-test-infra E2E test PR #14 is draft and blocked on fulfillment-cli PR #110 merge

## Key Effort

Implemented and validated the full [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance explicit spec fields feature across the entire fulfillment stack — from proto definitions through service, CLI, and E2E testing.
