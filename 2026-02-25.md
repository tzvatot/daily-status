---
layout: default
title: Daily Status - 2026-02-25
date: 2026-02-25
---

# Daily Status - 2026-02-25

## Accomplishments
- [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance API Enhancement — completed full stack implementation across multiple repos
- **fulfillment-api** PR #62 merged and tagged v0.0.30, BSR publish completed
- **fulfillment-common** regenerated from fulfillment-api v0.0.30, PR #63 merged, tagged v0.0.40
- **fulfillment-service**: Added explicit fields to private API proto, regenerated public API from v0.0.30, updated go.mod to fulfillment-common v0.0.40, implemented `addExplicitFields()` in `buildSpec()` controller to map cores, memoryGiB, image, bootDisk, additionalDisks, runStrategy, sshKey, userDataSecretRef. Fixed hub-access RBAC by adding `osac.openshift.io` rules. PR: fulfillment-service#308
- **fulfillment-cli**: Added 10 new explicit field flags (`--cores`, `--memory-gib`, `--image`, `--image-source-type`, `--ssh-key`, `--boot-disk-size`, `--boot-disk-storage-class`, `--additional-disk`, `--run-strategy`, `--user-data-secret-ref`) with kubectl-style compound flag parsing. Updated fulfillment-common to v0.0.40. PR: fulfillment-cli#110
- **osac-test-infra**: E2E test for CLI with explicit spec fields passed — full stack test covering CLI → fulfillment-service → operator → provisioning. Fixed `kubectl get computeinstance` API group, `buildSpec()` panic on int32 with unstructured, and hub-access RBAC. Cleaned up 56 leftover test CIs. PR: osac-test-infra#13

## Risks & Challenges
- Deploying updated fulfillment-service requires cherry-picking commits from worktree branch to main repo (worktree `.git` file breaks container build)

## Key Effort
Implemented and validated the full ComputeInstance explicit spec fields feature across the fulfillment stack (API, common, service, CLI, E2E tests).
