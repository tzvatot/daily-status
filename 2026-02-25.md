---
layout: default
title: Daily Status - 2026-02-25
date: 2026-02-25
---

# Daily Status - 2026-02-25

## Accomplishments

- [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance API Enhancement — completed major work across the fulfillment stack:
  - **fulfillment-api** PR #62 merged and tagged v0.0.30; BSR publish completed
  - **fulfillment-common** regenerated from fulfillment-api v0.0.30, PR [#63](https://github.com/osac-project/fulfillment-common/pull/63) merged, tagged v0.0.40
  - **fulfillment-service** — added explicit fields to private API proto, regenerated public API from v0.0.30, updated go.mod to fulfillment-common v0.0.40, mapped explicit fields in `buildSpec` controller (`addExplicitFields()` for cores, memoryGiB, image, bootDisk, additionalDisks, runStrategy, sshKey, userDataSecretRef), added tests, fixed hub-access RBAC (`osac.openshift.io` rules). PR [#308](https://github.com/osac-project/fulfillment-service/pull/308)
  - **fulfillment-cli** — added 10 new explicit field flags (`--cores`, `--memory-gib`, `--image`, `--image-source-type`, `--ssh-key`, `--boot-disk-size`, `--boot-disk-storage-class`, `--additional-disk`, `--run-strategy`, `--user-data-secret-ref`) with kubectl-style compound format, updated to fulfillment-common v0.0.40. PR [#110](https://github.com/osac-project/fulfillment-cli/pull/110)
  - **osac-test-infra** — started E2E test (`test_compute_instance_cli_explicit_fields`) covering CLI path with new explicit spec flags

## Risks & Challenges

- Fulfillment-service deployment requires cherry-picking commits from worktree branch to main repo (worktree `.git` file breaks container build)
- E2E test and deployment tasks still in progress

## Key Effort

Drove the [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance explicit spec fields feature across the full fulfillment stack — from proto definitions through common types, service mapping, CLI flags, and E2E test scaffolding.
