---
layout: default
title: Daily Status - 2026-02-25
date: 2026-02-25
---

# Daily Status - 2026-02-25

## Accomplishments

- [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance API Enhancement — completed full stack implementation across multiple repos
- **fulfillment-api** PR #62 merged and tagged v0.0.30, BSR publish completed
- **fulfillment-common**: Regenerated Go types from fulfillment-api v0.0.30, PR #63 merged, tagged v0.0.40
- **fulfillment-service**: Added explicit fields to private API proto, regenerated public API from v0.0.30, updated go.mod to fulfillment-common v0.0.40, mapped explicit fields in `buildSpec()` controller with tests, fixed hub-access RBAC for `osac.openshift.io` (PR #308)
- **fulfillment-cli**: Added 10 new explicit field flags (`--cores`, `--memory-gib`, `--image`, `--additional-disk`, etc.) with kubectl-style compound flag parsing, updated to fulfillment-common v0.0.40 (PR #110)
- **osac-test-infra**: E2E test for CLI with explicit spec fields passed — full stack test covering CLI → fulfillment-service → operator → provisioning (PR #13)
- Cleaned up 56 leftover test ComputeInstances from DB via CLI

## Risks & Challenges

- Deploying updated fulfillment-service requires cherry-picking commits from worktree branch to main repo (worktree `.git` file breaks container build)

## Key Effort

Implemented and validated the full [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance explicit spec fields feature across the fulfillment stack (API, common, service, CLI) with end-to-end testing, with only the service deployment remaining.
