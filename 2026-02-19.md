---
layout: default
title: Daily Status - 2026-02-19
date: 2026-02-19
---

# Daily Status - 2026-02-19

## Accomplishments
- Fixed `ensureBackwardCompatibility()` to support missing fields — added `additionalDisks` and `userDataSecretRef` mappings to templateParameters, changed function to return error instead of silently swallowing ([MGMT-23103](https://issues.redhat.com/browse/MGMT-23103))
- Deployed operator to vmaas-dev cluster (`quay.io/etabak/cloudkit-operator:mgmt-23103-c960a0f`)
- Addressed Template PR #6 review comments:
  - Cleaned up `default_arg_specs`: removed spec fields, kept only `exposed_ports`
  - Replaced `cloud_init_config` with `userDataSecretRef` (secret copy between namespaces, conditional cloud-init disk)
  - Fixed `delete.yaml`: `running: false` → `runStrategy: Halted`
  - Added cleanup in delete.yaml for additional disk DataVolumes, user-data secret, and legacy cloud-init secret
  - Added `default_spec` in defaults/main.yaml and wired up fallbacks in create.yaml
  - Fixed `userDataSecretRef` access pattern
  - Updated README with separated template parameters and spec fields documentation
- Prepared E2E API fields test playbook covering runStrategy mutability and field immutability

## Risks & Challenges
- Template PR awaiting re-review — operator still requires `ensureBackwardCompatibility()` to translate spec fields to templateParameters until merged
- Operator-side `default_spec` support still pending (reading defaults from template's osac.yaml and merging missing spec fields in reconciler)

## Key Effort
Continued [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance API enhancement work, focusing on backward compatibility fixes, template PR review feedback, and E2E test preparation.
