---
layout: default
title: Daily Status - 2026-02-19
date: 2026-02-19
---

# Daily Status - 2026-02-19

## Accomplishments
- Fixed `ensureBackwardCompatibility()` to support missing fields — added `additionalDisks` and `userDataSecretRef` mappings to templateParameters, changed function to return error instead of silently swallowing ([MGMT-23103](https://issues.redhat.com/browse/MGMT-23103))
- Deployed updated operator to vmaas-dev cluster (`quay.io/etabak/cloudkit-operator:mgmt-23103-c960a0f`)
- Addressed Template PR #6 review comments:
  - Cleaned up `default_arg_specs`, removing spec fields and keeping only `exposed_ports`
  - Replaced `cloud_init_config` with `userDataSecretRef` (secret copying, conditional cloud-init disk)
  - Fixed `delete.yaml`: `running: false` → `runStrategy: Halted`
  - Added cleanup in delete.yaml for additional disk DataVolumes, user-data secret, and legacy cloud-init secret
  - Added `default_spec` in defaults/main.yaml for ComputeInstance spec field defaults
  - Wired up `default_spec` fallbacks in create.yaml spec extraction
  - Updated README with separated template parameters and spec fields documentation

## Risks & Challenges
- E2E API fields test still in progress — validating runStrategy mutability and field immutability
- Template PR awaiting re-review; operator still requires `ensureBackwardCompatibility()` shim until template PR merges
- Operator-side `default_spec` support not yet started (next step: operator reads `default_spec` from template's osac.yaml and merges missing spec fields before invoking template)

## Key Effort
Continued [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance API enhancement — fixing backward compatibility, addressing template PR review feedback, and progressing E2E validation.
