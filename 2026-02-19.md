---
layout: default
title: Daily Status - 2026-02-19
date: 2026-02-19
---

# Daily Status - 2026-02-19

## Accomplishments
- Fixed `ensureBackwardCompatibility()` to support missing fields — added `additionalDisks` and `userDataSecretRef` mappings to templateParameters, changed function to return error instead of silently swallowing (commit `c960a0f`)
- Deployed operator to vmaas-dev cluster (`quay.io/etabak/cloudkit-operator:mgmt-23103-c960a0f`)
- Addressed Template PR #6 review comments for [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103):
  - Cleaned up `default_arg_specs`: removed spec fields, kept only `exposed_ports`
  - Replaced `cloud_init_config` with `userDataSecretRef` (secret copy from CI namespace to VM namespace, conditional cloud-init disk)
  - Fixed `delete.yaml`: `running: false` → `runStrategy: Halted`
  - Added cleanup in delete.yaml for additional disk DataVolumes, user-data secret, and legacy cloud-init secret
  - Added `default_spec` in defaults/main.yaml for ComputeInstance spec field defaults
  - Wired up `default_spec` fallbacks in create.yaml spec extraction
  - Updated README: separated template parameters from spec fields documentation
  - Pushed to fork (commit `3e03128`)

## Risks & Challenges
- Template PR awaiting re-review — until merged, operator needs `ensureBackwardCompatibility()` to translate spec fields to templateParameters for existing templates on the cluster
- E2E API fields test still in progress

## Key Effort
Continued [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance API enhancement work, focusing on backward compatibility fixes, template PR review feedback, and E2E test preparation.
