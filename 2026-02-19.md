---
layout: default
title: Daily Status - 2026-02-19
date: 2026-02-19
---

# Daily Status - 2026-02-19

## Accomplishments
- Fixed `ensureBackwardCompatibility()` to support missing fields (`additionalDisks`, `userDataSecretRef` mappings; error return instead of silent swallow) — committed `c960a0f`
- Deployed operator to vmaas-dev cluster (`quay.io/etabak/cloudkit-operator:mgmt-23103-c960a0f`)
- Addressed Template PR #6 review comments ([MGMT-23103](https://issues.redhat.com/browse/MGMT-23103)):
  - Cleaned up `default_arg_specs` — removed spec fields, kept only `exposed_ports`
  - Replaced `cloud_init_config` with `userDataSecretRef` (secret copy from CI namespace to VM namespace, conditional cloud-init disk)
  - Fixed `delete.yaml`: `running: false` → `runStrategy: Halted`; added cleanup for additional disk DataVolumes, user-data secret, legacy cloud-init secret
  - Added `default_spec` in `defaults/main.yaml` for ComputeInstance spec field defaults and wired up fallbacks in `create.yaml`
  - Fixed `userDataSecretRef` safe access pattern
  - Updated README separating template parameters from spec fields
  - Pushed to fork (`3e03128`)
- Prepared E2E API fields test playbook covering runStrategy mutability and field immutability

## Risks & Challenges
- Template PR awaiting re-review — operator still requires `ensureBackwardCompatibility()` translation layer until template PR merges
- Operator-side `default_spec` support not yet implemented (defaults not yet discoverable via API)

## Key Effort
Continued [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance API enhancement — fixing backward compatibility, addressing template PR review feedback, and preparing E2E validation.
