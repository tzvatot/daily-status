---
layout: default
title: Daily Status - 2026-02-19
date: 2026-02-19
---

# Daily Status - 2026-02-19

## Accomplishments
- Fixed `ensureBackwardCompatibility()` to support missing fields — added `additionalDisks` and `userDataSecretRef` mappings to templateParameters, changed function to return error instead of silently swallowing ([MGMT-23103](https://issues.redhat.com/browse/MGMT-23103))
- Deployed operator to vmaas-dev cluster (`quay.io/etabak/cloudkit-operator:mgmt-23103-c960a0f`)
- Addressed Template PR #6 review comments ([MGMT-23103](https://issues.redhat.com/browse/MGMT-23103)):
  - Cleaned up `default_arg_specs`: removed spec fields, kept only `exposed_ports`
  - Replaced `cloud_init_config` with `userDataSecretRef` (secret copy from CI namespace to VM namespace, conditional cloud-init disk)
  - Fixed `delete.yaml`: `running: false` → `runStrategy: Halted`
  - Added cleanup in delete.yaml for additional disk DataVolumes, user-data secret, and legacy cloud-init secret
  - Added `default_spec` in defaults/main.yaml for ComputeInstance spec field defaults and wired up fallbacks in create.yaml
  - Fixed `userDataSecretRef` access with proper default handling
  - Updated README separating template parameters from spec fields documentation
  - Pushed to fork `etabak-fork/feature/support-runstrategy-mgmt-23103`

## Risks & Challenges
- Template PR awaiting re-review — until merged, operator requires `ensureBackwardCompatibility()` to translate spec fields to templateParameters for existing templates on the cluster
- E2E API fields test still in progress (runStrategy mutability and field immutability validation)

## Key Effort
Continued [MGMT-23103](https://issues.redhat.com/browse/MGMT-23103) ComputeInstance API enhancement — fixing backward compatibility, addressing template PR review comments, and preparing E2E test validation.
