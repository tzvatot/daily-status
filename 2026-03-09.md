---
layout: default
title: Daily Status - 2026-03-09
date: 2026-03-09
---

# Daily Status - 2026-03-09

## Accomplishments
- **ClusterOrder AAP Direct Ready**: Rebased feature branch (7 commits) on updated main, resolving conflicts across 5 commits (`cmd/main.go`, controller tests, EDA provider, factory)
- Simplified EDA provider from 6-URL to 2-URL constructor; extracted `createProviderFromEnv` and `setupWebhookController` generic helper to eliminate duplication
- Full validation passed: build, fmt, lint (0 issues), 122 tests, manifests, code generation, and container image build
- Completed thorough code review identifying 4 issues: webhook fallback firing when provider is configured (in both `handleUpdate` and `handleDelete` for ClusterOrder and HostPool), and missing controller-level tests
- Planned 3 fix commits: remove webhook fields from reconcilers, rename env vars to align with upstream naming, and refactor `createProviderFromEnv` to builder pattern
- **[MGMT-23348](https://issues.redhat.com/browse/MGMT-23348) — PR #131 review feedback** (Prevent duplicate provision job triggers): Addressed Adrien's review comments — added API server read-through to config version mismatch branch and unified nil-job and version-mismatch paths under `DesiredConfigVersion != ReconciledConfigVersion`
- Introduced `provisionAction` enum, extracted `checkAPIServerForNonTerminalJob` helper, and restructured `shouldTriggerProvision` logic
- Second review round: extracted `triggerProvisionJob`/`pollProvisionJob` helpers for switch readability (4bdf673), added tests for 100% coverage on all changed functions (ac32861)
- Deployed to vmaas-dev and passed E2E CLI fields test (create, explicit fields, Secret, provision, Running, delete) — 129 tests pass, 0 lint issues

## Risks & Challenges
- ClusterOrder/HostPool controllers still have webhook/provider dual-firing issue that needs fix commits before merging PR #110
- No controller-level tests exist yet for ClusterOrder/HostPool provisioning (only provider-level)
- E2E re-test still pending for latest PR #131 changes (ac32861)

## Key Effort
Rebasing and stabilizing the AAP direct provider feature branch for ClusterOrder/HostPool, and addressing PR review feedback on duplicate provision job prevention.
