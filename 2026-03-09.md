---
layout: default
title: Daily Status - 2026-03-09
date: 2026-03-09
---

# Daily Status - 2026-03-09

## Accomplishments

- **ClusterOrder AAP Direct Ready**: Rebased feature branch (7 commits) on updated main, resolving conflicts in 5 commits across `cmd/main.go`, controller tests, EDA provider, and factory
- Simplified EDA provider from 6-URL to 2-URL constructor; each controller now creates its own provider instance via `createProviderFromEnv`
- Extracted `setupWebhookController` generic helper to eliminate dupl linter violations between cluster and host pool controller setup
- Full validation passed: build, fmt, lint (0 issues), 122 tests, manifests, code generation, and container image build
- Completed thorough code review identifying 4 issues: webhook fallback dual-firing in ClusterOrder and HostPool controllers, and missing controller-level tests
- Planned 3 fix commits: remove webhook fields from reconcilers, rename env vars to align with upstream naming, and refactor `createProviderFromEnv` to builder pattern
- **PR #131 — Prevent duplicate provision job triggers** ([MGMT-23348](https://issues.redhat.com/browse/MGMT-23348)): Addressed all review comments from Adrien
- Unified nil-job and config-version-mismatch branches per reviewer suggestion; extracted `checkAPIServerForNonTerminalJob` helper
- Introduced `provisionAction` enum and restructured `shouldTriggerProvision` with `triggerProvisionJob`/`pollProvisionJob` helpers for readability
- Added API server cache guard tests achieving 100% coverage on all changed functions (129 tests pass, 0 lint issues)
- Deployed twice to vmaas-dev and passed E2E CLI fields tests both times (commits 8f74fed and ac32861)

## Risks & Challenges

- ClusterOrder/HostPool controllers still have webhook dual-firing issue where webhooks fire regardless of whether the provider handled provisioning — fix commits planned but not yet implemented
- No controller-level tests exist for ClusterOrder/HostPool provisioning (only provider-level tests), unlike ComputeInstance which has both
- Deploy and test flow for cluster order still pending — infra requirements not yet identified

## Key Effort

Rebased and validated the AAP Direct provider feature branch for ClusterOrder support, and fully addressed two rounds of review feedback on PR #131 for preventing duplicate provision job triggers.
