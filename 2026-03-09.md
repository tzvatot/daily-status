---
layout: default
title: Daily Status - 2026-03-09
date: 2026-03-09
---

# Daily Status - 2026-03-09

## Accomplishments
- Rebased `feature/aap-direct-provider-everywhere` branch (7 commits) onto updated main, resolving conflicts in 5 commits across `cmd/main.go`, controller tests, EDA provider, and factory
- Simplified EDA provider from 6-URL to 2-URL constructor; each controller now creates its own provider instance via extracted `createProviderFromEnv`
- Extracted `setupWebhookController` generic helper to eliminate dupl linter violations between cluster and host pool controller setup
- Full validation passed: build, fmt, lint (0 issues), 122 tests, manifests, code generation, and container image build
- Completed code review identifying implemented features (Jobs array in ClusterOrder/HostPool status, AAP provider type switches, helper extraction) and gaps (missing controller-level tests, webhook/provider dual-firing issues)
- Addressed PR #131 review comments from Adrien for [MGMT-23348](https://issues.redhat.com/browse/MGMT-23348): unified nil-job and version-mismatch paths, introduced `provisionAction` enum, extracted `checkAPIServerForNonTerminalJob` helper
- All 125 tests pass with 0 lint issues after PR #131 changes; pushed commit 8f74fed

## Risks & Challenges
- Webhook fallback still fires even when provider is configured in ClusterOrder `handleUpdate` and `handleDelete` — needs fix before merging PR #110
- No controller-level tests exist for ClusterOrder/HostPool provisioning (only provider-level tests); ComputeInstance has equivalent tests but the other two do not
- HostPool likely has the same webhook/provider dual-firing issue — needs verification
- E2E testing and vmaas-dev deployment still pending for both PRs

## Key Effort
Rebasing and adapting the AAP direct provider feature branch to significant upstream restructuring, while simultaneously addressing review feedback on the duplicate provision prevention PR.
