---
layout: default
title: Daily Status - 2026-03-09
date: 2026-03-09
---

# Daily Status - 2026-03-09

## Accomplishments
- **ClusterOrder AAP Direct Ready**: Rebased feature branch (7 commits) onto updated main, resolving conflicts in 5 commits across `cmd/main.go`, controller tests, EDA provider, and factory
- Simplified EDA provider from 6-URL to 2-URL constructor; each controller now creates its own provider instance via extracted `createProviderFromEnv`
- Extracted generic `setupWebhookController` helper to eliminate dupl linter violations between cluster and host pool controller setup
- Full validation passed: build, fmt, lint (0 issues), 122 tests, manifests, code generation, and container image build
- Completed code review identifying 3 planned fix commits: remove webhook dual-firing, rename env vars to match upstream convention, and refactor `createProviderFromEnv` to builder pattern
- **PR #131 — Prevent duplicate provision job triggers** ([MGMT-23348](https://issues.redhat.com/browse/MGMT-23348)): Addressed all three rounds of review from Adrien
- Unified nil-job and version-mismatch paths under `DesiredConfigVersion != ReconciledConfigVersion` check
- Extracted `checkAPIServerForNonTerminalJob` helper with API server read-through for both branches
- Refactored to `triggerProvisionJob`/`pollProvisionJob` helpers for switch readability
- Removed jobs list mutation from `checkAPIServerForNonTerminalJob`, added stale-cache requeue guard in `pollProvisionJob`
- Achieved 100% test coverage on all changed functions (129 tests pass, 0 lint issues)
- Successfully deployed and E2E tested three iterations to vmaas-dev (commits 8f74fed, ac32861, 6145060)
- Cleaned up leftover CIs from failed earlier E2E runs (vm-4q6h7, vm-klfxp)

## Risks & Challenges
- ClusterOrder/HostPool controllers lack controller-level tests (only provider-level tests exist, unlike ComputeInstance)
- Webhook fallback still fires when provider is configured in ClusterOrder and HostPool `handleUpdate`/`handleDelete` — planned fix commits not yet applied
- Deploy to vmaas-dev and test flow for cluster order still pending

## Key Effort
Rebased and validated the AAP Direct provider feature branch for ClusterOrder/HostPool, and completed three rounds of PR review feedback for the duplicate provision job prevention fix.
