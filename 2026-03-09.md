---
layout: default
title: Daily Status - 2026-03-09
date: 2026-03-09
---

# Daily Status - 2026-03-09

## Accomplishments
- Rebased `feature/aap-direct-provider-everywhere` branch (7 commits) onto updated main (30+ upstream commits), resolving conflicts in 5 of 7 commits across `cmd/main.go`, controller tests, EDA provider, and factory
- Simplified EDA provider from 6-URL to 2-URL constructor; extracted `createProviderFromEnv` and generic `setupWebhookController` helper to eliminate dupl linter violations from upstream restructuring
- Passed full validation pipeline: build, fmt, lint (0 issues), 122 tests, manifests, code generation, and container image build
- Completed code review of PR osac-operator#110 — identified webhook fallback dual-firing issues in ClusterOrder and HostPool controllers that need fixing
- Analyzed review comments from Adrien on [MGMT-23348](https://issues.redhat.com/browse/MGMT-23348) (PR osac-operator#131) — need to add API server read-through to config version mismatch branch and consider merging nil-job and version-mismatch code paths

## Risks & Challenges
- ClusterOrder and HostPool controllers have a webhook/provider dual-firing issue: webhooks fire regardless of whether the AAP provider already handled provisioning/deprovisioning — needs fix before merge
- No controller-level tests exist for ClusterOrder/HostPool provisioning (only provider-level tests); ComputeInstance has these but equivalents are missing for the other two resource types
- PR #131 review comments still need implementation (API server read-through for config version mismatch, potential branch consolidation)

## Key Effort
Rebasing and validating the AAP direct provider feature branch after significant upstream restructuring, and reviewing open PR feedback for duplicate provision job prevention.
