---
layout: default
title: Daily Status - 2026-03-09
date: 2026-03-09
---

# Daily Status - 2026-03-09

## Accomplishments
- **ClusterOrder AAP Direct Ready**: Rebased feature branch (7 commits) on updated main, resolving conflicts in 5 of 7 commits across `cmd/main.go`, controller tests, EDA provider, and factory
- Simplified EDA provider from 6-URL to 2-URL constructor; extracted `createProviderFromEnv` and `setupWebhookController` generic helper to eliminate duplication
- Full validation passed: build, fmt, lint (0 issues), 122 tests, manifests, code generation, and container image build
- Completed thorough code review identifying 3 planned fix commits: remove webhook dual-firing from ClusterOrder/HostPool reconcilers, rename env vars to align with upstream naming, and refactor `createProviderFromEnv` to builder pattern
- **PR #131 — Prevent duplicate provision job triggers** ([MGMT-23348](https://issues.redhat.com/browse/MGMT-23348)): Addressed three rounds of review from Adrien
  - Unified nil-job and version-mismatch paths under `DesiredConfigVersion != ReconciledConfigVersion`
  - Extracted `checkAPIServerForNonTerminalJob` helper with API server read-through for both branches
  - Refactored switch to single-line cases with `triggerProvisionJob`/`pollProvisionJob` helpers
  - Removed jobs list mutation from `checkAPIServerForNonTerminalJob`; added stale-cache requeue guard in `pollProvisionJob`
  - Achieved 100% test coverage on all changed functions (129 tests pass, 0 lint issues)
  - Deployed and passed E2E tests three times across iterations (commits 8f74fed, ac32861, 6145060)
  - Cleaned up leftover CIs (vm-4q6h7, vm-klfxp) from failed earlier E2E runs

## Risks & Challenges
- ClusterOrder/HostPool controllers have no controller-level tests (only provider-level) — equivalent tests exist for ComputeInstance but not the other two
- Webhook fallback still fires when provider is configured in ClusterOrder and HostPool `handleUpdate`/`handleDelete` — planned fix commit pending
- Deployment to vmaas-dev and test flow for cluster order not yet started for the AAP Direct branch

## Key Effort
Rebased and validated the ClusterOrder AAP Direct feature branch against significant upstream changes, while completing three rounds of review feedback on the duplicate provision job prevention PR.
