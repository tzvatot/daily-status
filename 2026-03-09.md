---
layout: default
title: Daily Status - 2026-03-09
date: 2026-03-09
---

# Daily Status - 2026-03-09

## Accomplishments
- **ClusterOrder AAP Direct Ready**: Rebased feature branch (7 commits) on updated main, resolving conflicts across 5 commits (`cmd/main.go`, controller tests, EDA provider, factory)
- Simplified EDA provider from 6-URL to 2-URL constructor; each controller now creates its own provider instance via `createProviderFromEnv`
- Extracted `setupWebhookController` generic helper to eliminate duplication between cluster and host pool controller setup
- Full validation passed: build, fmt, lint (0 issues), 122 tests, manifests, code generation, and container image build all green
- Completed code review identifying implemented features (Jobs array in ClusterOrder/HostPool status, AAP provider type switches, per-resource-type finalizer constants) and gaps
- **PR #131 review comments ([MGMT-23348](https://issues.redhat.com/browse/MGMT-23348))**: Addressed Adrien's feedback on duplicate provision job prevention
- Unified nil-job and version-mismatch branches under `DesiredConfigVersion != ReconciledConfigVersion` check
- Added API server read-through to config version mismatch branch to catch already-triggered jobs
- Introduced `provisionAction` enum and extracted `checkAPIServerForNonTerminalJob` helper
- All 125 tests pass, 0 lint issues, pushed to fork (8f74fed)

## Risks & Challenges
- **Webhook fallback dual-firing**: In ClusterOrder controller, `CreateClusterWebhook` and `DeleteClusterWebhook` fire regardless of whether the provider handled provisioning — needs conditional skip when provider is set. Same issue exists in HostPool
- **Missing controller-level tests**: No provisioning or integration tests for ClusterOrder/HostPool controllers (only provider-level tests exist)
- **Deploy and E2E testing still pending** for both the AAP direct branch and PR #131

## Key Effort
Rebasing and stabilizing the AAP direct provider feature branch after significant upstream restructuring, while simultaneously addressing PR review feedback on duplicate provision job prevention.
