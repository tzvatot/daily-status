---
layout: default
title: Daily Status - 2026-03-09
date: 2026-03-09
---

# Daily Status - 2026-03-09

## Accomplishments
- **ClusterOrder AAP Direct Ready**: Rebased feature branch (7 commits) on updated main, resolving conflicts in 5 of 7 commits across `cmd/main.go`, controller tests, EDA provider, and factory
- Simplified EDA provider from 6-URL to 2-URL constructor; extracted `createProviderFromEnv` and `setupWebhookController` generic helper to eliminate duplication
- Full validation passed: build, fmt, lint (0 issues), 122 tests, manifests, code generation, and container image build
- Completed thorough code review identifying 3 issues: webhook fallback firing when provider is configured (ClusterOrder and HostPool), and missing controller-level tests
- Planned 3 fix commits: remove webhook fields from reconcilers, rename env vars to align with upstream naming, and refactor `createProviderFromEnv` to builder pattern
- **PR [MGMT-23348](https://issues.redhat.com/browse/MGMT-23348) — Prevent duplicate provision job triggers**: Addressed 3 rounds of review feedback from Adrien
  - Added API server read-through to config version mismatch branch; merged nil-job and version-mismatch paths
  - Extracted `checkAPIServerForNonTerminalJob` helper and `triggerProvisionJob`/`pollProvisionJob` helpers for readability
  - Removed jobs list mutation from API server check; added stale-cache requeue guard in `pollProvisionJob`
  - Added tests for 100% coverage on all changed functions (129 tests pass, 0 lint issues)
  - Deployed and ran E2E tests after each review round — all passed
  - Cleaned up leftover CIs (vm-4q6h7, vm-klfxp) from earlier failed E2E runs

## Risks & Challenges
- ClusterOrder/HostPool controllers have no controller-level tests (only provider-level); ComputeInstance has both
- Webhook/provider dual-firing issue in ClusterOrder and HostPool `handleUpdate`/`handleDelete` needs fixing before merge
- Deployment to vmaas-dev and test flow for cluster order still pending

## Key Effort
Rebased and reviewed the ClusterOrder AAP Direct feature branch while completing three rounds of review feedback on the duplicate provision job prevention PR.
