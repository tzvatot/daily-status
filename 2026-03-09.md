---
layout: default
title: Daily Status - 2026-03-09
date: 2026-03-09
---

# Daily Status - 2026-03-09

## Accomplishments
- Rebased `feature/aap-direct-provider-everywhere` branch (7 commits) onto updated main, resolving conflicts across 5 commits (`cmd/main.go`, controller tests, EDA provider, factory)
- Simplified EDA provider from 6-URL constructor to 2-URL constructor (provision + deprovision), with each controller creating its own provider instance via `createProviderFromEnv`
- Extracted `setupWebhookController` generic helper to eliminate dupl linter violations between cluster and host pool controller setup
- Full validation passed: build, fmt, lint (0 issues), 122 tests, manifests, code generation, and container image build (osac-operator#110)
- Addressed PR review comments from Adrien on osac-operator#131 ([MGMT-23348](https://issues.redhat.com/browse/MGMT-23348)) — unified nil-job and version-mismatch branches, added API server read-through to config version mismatch path, introduced `provisionAction` enum
- All 125 tests pass with 0 lint issues after PR #131 rework; pushed commit 8f74fed

## Risks & Challenges
- No controller-level tests exist for ClusterOrder/HostPool provisioning (only provider-level tests); ComputeInstance has equivalents but the other two do not
- Webhook fallback still fires even when provider is configured in ClusterOrder `handleUpdate` and `handleDelete` — needs conditional logic to skip when provider handles provisioning
- HostPool likely has the same webhook/provider dual-firing issue — needs verification
- E2E deployment and testing still pending for both PRs

## Key Effort
Rebasing and adapting the AAP direct provider feature branch to significant upstream restructuring, and addressing PR review feedback on duplicate provision job prevention.
