---
layout: default
title: Daily Status - 2026-03-09
date: 2026-03-09
---

# Daily Status - 2026-03-09

## Accomplishments
- Rebased `feature/aap-direct-provider-everywhere` branch (7 commits) onto updated main, resolving conflicts in 5 commits across `cmd/main.go`, controller tests, EDA provider, and factory
- Simplified EDA provider from 6-URL to 2-URL constructor; each controller now creates its own provider instance via extracted `createProviderFromEnv`
- Extracted `setupWebhookController` generic helper to eliminate dupl linter violations between cluster and host pool controller setup
- Full validation passed: build, fmt, lint (0 issues), 122 tests, manifests, codegen, and container image build
- Completed code review identifying implemented features (Jobs array in ClusterOrder/HostPool status, AAP provider type switches, controller provisioning/deprovisioning handlers) and gaps (missing controller-level tests, webhook fallback dual-firing when provider is configured)
- Addressed PR #131 review comments from Adrien ([MGMT-23348](https://issues.redhat.com/browse/MGMT-23348)): unified nil-job and version-mismatch branches, added API server read-through to config version mismatch path, extracted `checkAPIServerForNonTerminalJob` helper, introduced `provisionAction` enum
- All 125 tests pass with 0 lint issues on PR #131; pushed updated branch (8f74fed)

## Risks & Challenges
- ClusterOrder and HostPool controllers lack controller-level tests (only provider-level tests exist)
- Webhook fallback still fires when AAP provider is configured in both `handleUpdate` and `handleDelete` for ClusterOrder and HostPool controllers — needs fix before merge
- Deployment to vmaas-dev and E2E testing still pending for both PRs

## Key Effort
Rebasing and validating the AAP direct provider branch across ClusterOrder and HostPool controllers, plus addressing PR review feedback on duplicate provision job prevention.
