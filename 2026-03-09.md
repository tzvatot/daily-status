---
layout: default
title: Daily Status - 2026-03-09
date: 2026-03-09
---

# Daily Status - 2026-03-09

## Accomplishments
- Rebased `feature/aap-direct-provider-everywhere` branch (7 commits) onto updated main, resolving conflicts in 5 commits across `cmd/main.go`, controller tests, EDA provider, and factory
- Simplified EDA provider from 6-URL to 2-URL constructor; each controller now creates its own provider instance via extracted `createProviderFromEnv`
- Extracted `setupWebhookController` generic helper to eliminate dupl linter violations between cluster and host pool controller setup
- Full validation passed: build, fmt, lint (0 issues), 122 tests, manifests, code generation, and container image build
- Completed code review identifying webhook fallback dual-firing issues in ClusterOrder and HostPool controllers that need fixing
- Addressed PR review comments from Adrien on [MGMT-23348](https://issues.redhat.com/browse/MGMT-23348) (osac-operator#131): unified nil-job and version-mismatch paths, added API server read-through to config version mismatch branch, extracted `checkAPIServerForNonTerminalJob` helper
- Introduced `provisionAction` enum (`provisionSkip`/`provisionTrigger`/`provisionPoll`) and restructured `shouldTriggerProvision` logic
- All 125 tests pass with 0 lint issues; pushed updated branch (8f74fed)

## Risks & Challenges
- Webhook fallback still fires when provider is configured in ClusterOrder `handleUpdate` and `handleDelete` — needs to be skipped when provider handles provisioning
- HostPool likely has the same webhook/provider dual-firing issue
- No controller-level tests exist for ClusterOrder/HostPool provisioning (only provider-level tests); ComputeInstance has equivalents but the other two do not
- E2E testing and vmaas-dev deployment still pending for both PRs

## Key Effort
Rebasing and validating the AAP direct provider branch after significant upstream restructuring, and addressing PR review comments to prevent duplicate provision job triggers.
