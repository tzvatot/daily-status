---
layout: default
title: Daily Status - 2026-03-09
date: 2026-03-09
---

# Daily Status - 2026-03-09

## Accomplishments
- **ClusterOrder AAP Direct Ready**: Rebased feature branch (7 commits) on updated main, resolving conflicts in 5 of 7 commits across `cmd/main.go`, controller tests, EDA provider, and factory
- Simplified EDA provider from 6-URL to 2-URL constructor; extracted `createProviderFromEnv` and `setupWebhookController` generic helper to eliminate duplication
- Full validation passed: build, fmt, lint (0 issues), 122 tests, manifests, code generation, and container image build
- Completed thorough code review identifying 3 planned fix commits: remove webhook dual-firing from ClusterOrder/HostPool reconcilers, rename env vars to align with upstream naming, and refactor `createProviderFromEnv` to builder pattern
- **PR [MGMT-23348](https://issues.redhat.com/browse/MGMT-23348) review comments** (osac-operator#131): Addressed three rounds of review feedback from Adrien
  - Unified nil-job and version-mismatch paths in `shouldTriggerProvision`; added API server read-through to config version mismatch branch
  - Extracted `checkAPIServerForNonTerminalJob` helper and `triggerProvisionJob`/`pollProvisionJob` helpers for switch readability
  - Removed jobs list mutation from `checkAPIServerForNonTerminalJob`; added stale-cache requeue guard in `pollProvisionJob`
  - Achieved 100% test coverage on all changed functions (129 tests pass, 0 lint issues)
  - Deployed and ran E2E tests after each review round — all passed (commits 8f74fed, ac32861, 6145060)
  - Cleaned up leftover ComputeInstances (vm-4q6h7, vm-klfxp) from failed earlier E2E runs

## Risks & Challenges
- ClusterOrder/HostPool controllers have webhook/provider dual-firing issue — webhooks fire regardless of whether provider handled provisioning; fix commits planned but not yet applied
- No controller-level tests exist for ClusterOrder/HostPool provisioning (only provider-level tests)
- Deploy to vmaas-dev and test flow for cluster order still pending

## Key Effort
Rebased and validated the AAP Direct Provider feature branch for ClusterOrder support, and iterated through three rounds of PR review on the duplicate provision job fix with full E2E validation after each round.
