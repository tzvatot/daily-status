---
layout: default
title: Daily Status - 2026-03-09
date: 2026-03-09
---

# Daily Status - 2026-03-09

## Accomplishments

- **ClusterOrder AAP Direct Ready**: Rebased feature branch (7 commits) on updated main, resolving conflicts in 5 of 7 commits across `cmd/main.go`, controller tests, EDA provider, and factory
- Simplified EDA provider from 6-URL to 2-URL constructor; extracted `createProviderFromEnv` and `setupWebhookController` generic helper to eliminate duplication
- Full validation passed: build, fmt, lint (0 issues), 122 tests, manifests, code generation, and container image build
- Completed thorough code review identifying 3 planned fix commits: remove webhook dual-firing from ClusterOrder/HostPool reconcilers, rename env vars to align with upstream naming, and refactor `createProviderFromEnv` to builder pattern
- **PR #131 — Prevent duplicate provision job triggers** ([MGMT-23348](https://issues.redhat.com/browse/MGMT-23348)): Addressed all three rounds of review from Adrien
  - Unified nil-job and version-mismatch paths under `DesiredConfigVersion != ReconciledConfigVersion`
  - Extracted `checkAPIServerForNonTerminalJob` helper with API server read-through for both branches
  - Refactored switch to single-line cases with `triggerProvisionJob`/`pollProvisionJob` helpers
  - Removed jobs list mutation from `checkAPIServerForNonTerminalJob`, added stale-cache requeue guard
  - Added tests for 100% coverage on all changed functions (129 tests pass, 0 lint issues)
  - Deployed and verified via E2E tests across 3 iterations (commits 8f74fed, ac32861, 6145060)
  - Cleaned up leftover CIs (vm-4q6h7, vm-klfxp)

## Risks & Challenges

- ClusterOrder/HostPool controllers still have webhook/provider dual-firing issue — webhooks fire regardless of whether provider handled provisioning. Fix commits planned but not yet implemented.
- No controller-level tests exist for ClusterOrder/HostPool provisioning (only provider-level tests)
- Deploy to vmaas-dev and test flow for cluster order still pending

## Key Effort

Rebased and validated the AAP Direct Provider feature branch for ClusterOrder support, and completed all three review rounds on PR #131 for preventing duplicate provision job triggers.
