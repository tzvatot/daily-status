---
layout: default
title: Daily Status - 2026-03-09
date: 2026-03-09
---

# Daily Status - 2026-03-09

## Accomplishments
- Rebased `feature/aap-direct-provider-everywhere` branch (7 commits) onto updated main, resolving conflicts in 5 commits across `cmd/main.go`, controller tests, EDA provider, and factory
- Simplified EDA provider from 6-URL to 2-URL constructor; extracted `createProviderFromEnv` and `setupWebhookController` generic helper to eliminate duplication
- Passed full validation pipeline: build, fmt, lint, 122 tests, manifests, code generation, and container image build
- Completed code review of osac-operator#110 — identified webhook fallback dual-firing issues in ClusterOrder and HostPool controllers that need fixing
- Analyzed review comments on osac-operator#131 ([MGMT-23348](https://issues.redhat.com/browse/MGMT-23348)) — Adrien flagged need for API server read-through on config version mismatch branch and potential to merge nil-job and version-mismatch code paths

## Risks & Challenges
- No controller-level tests exist for ClusterOrder/HostPool provisioning (only provider-level tests); ComputeInstance has these but equivalents are missing for the other two resource types
- Webhook fallback fires even when provider is configured in ClusterOrder's `handleUpdate` and `handleDelete`, causing dual-firing — HostPool likely has the same issue
- PR #131 review changes not yet implemented; need to add API server read-through for the config version mismatch branch

## Key Effort
Rebasing and validating the AAP direct provider feature branch after significant upstream restructuring, and analyzing review feedback on the duplicate provision job prevention PR.
