---
layout: default
title: Daily Status - 2026-09-06
date: 2026-09-06
---

# Daily Status - 2026-09-06

## Accomplishments
- [OSAC-4441](https://redhat.atlassian.net/browse/OSAC-4441): PR [osac#646](https://github.com/osac-project/osac/pull/646) (order-independent ClusterOrder feedback condition mapping) received LGTM and approval, all CI checks are passing, and hold was removed — waiting for auto-merge to trigger.
- [OSAC-4441](https://redhat.atlassian.net/browse/OSAC-4441): Validated the PROGRESSING feedback behavior on a real CaaS-CI deployment (host karim04). Two test scenarios (T1 and T2) passed, confirming the operator correctly derives the furthest provisioning stage from ClusterOrder conditions and syncs the expected feedback state on a live OSAC stack.
- Set up the full CaaS-CI test environment: deployed OSAC via Helm, installed the CLI, registered a bare-metal worker agent, and created host types and templates — all to support end-to-end lifecycle validation.
- Resolved three infrastructure blockers during environment setup:
  - Created a missing Provisioning CR so that Metal3/Ironic would deploy on the platform-none SNO cluster, unblocking BareMetalHost processing.
  - Fixed the BMH BMC address to use HTTP instead of HTTPS, since the virtual BMC (sushy-emulator) does not serve TLS.
  - Created the missing pull-secret Secret and added RBAC for the AAP service account to read it, unblocking AAP job execution.
- Observed that AAP cluster creation progresses through 64 tasks (including HostedCluster and NodePool creation) but fails at the network step because the environment lacks OpenStack and the template defaults to the ESI network backend instead of the CI-compatible one. Identified three potential fixes but did not yet apply one.

## Risks & Challenges
- PR [osac#646](https://github.com/osac-project/osac/pull/646) merge depends on CI completing cleanly after the hold was removed — currently all green and expected to auto-merge.
- AAP network backend misconfiguration (Blocker 11) blocks full end-to-end cluster lifecycle validation. A workaround (switching to the CI-compatible network backend) has been identified but not yet applied or tested.
- SNO memory is at ~80% with HyperShift control plane pods running, which may cause pod evictions if additional clusters are created.

## Key Effort
Validating the ClusterOrder feedback condition mapping ([OSAC-4441](https://redhat.atlassian.net/browse/OSAC-4441)) on a real CaaS-CI deployment while driving PR [osac#646](https://github.com/osac-project/osac/pull/646) toward merge.
