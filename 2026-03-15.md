---
layout: default
title: Daily Status - 2026-03-15
date: 2026-03-15
---

# Daily Status - 2026-03-15

## Accomplishments
- Continued work on ClusterOrder AAP Direct Ready ([osac-operator#110](https://github.com/osac-project/osac-operator/pull/110)):
  - Rebased on upstream main
  - Fixed block-deletion-on-failure behavior and UTC timestamp handling
  - Implemented config version tracking and duplicate trigger guard for ClusterOrder and HostPool, ensuring spec changes trigger re-provisioning while preventing duplicate triggers from stale cache
  - Added comprehensive test coverage for config version and provision trigger logic
  - Pushed all changes to [osac-operator#110](https://github.com/osac-project/osac-operator/pull/110)
- Performed E2E testing on vmaas-dev: validated ClusterOrder and HostPool provisioning with config version tracking working correctly
- Discovered a critical bug: infinite provision retry loop when a job fails — affects all three resource types (ClusterOrder, HostPool, ComputeInstance). Drafted a Slack message to Adrien proposing a fix as a separate ticket/PR

## Risks & Challenges
- Ongoing missile attacks in Israel — sleep-deprived, frequent shelter time, helping kids with remote schooling, cooking and childcare throughout the day. Reduced availability and focus expected.
- Critical infinite retry bug discovered during E2E testing — when a provision job fails, the system retries indefinitely because the reconciled config version is never set. Proposed fix will be handled in a separate PR affecting all three resource types.

## Key Effort
Implementing config version tracking and duplicate trigger guards for ClusterOrder and HostPool, and validating through E2E testing which uncovered a critical infinite retry bug.
