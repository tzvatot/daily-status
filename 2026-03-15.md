---
layout: default
title: Daily Status - 2026-03-15
date: 2026-03-15
---

# Daily Status - 2026-03-15

## Accomplishments
- Continued work on ClusterOrder AAP Direct Ready ([osac-operator#110](https://github.com/osac-project/osac-operator/pull/110)):
  - Fixed deletion blocking behavior on failure and corrected timestamp handling
  - Implemented config version tracking and duplicate trigger guards for ClusterOrder and HostPool, ensuring spec updates correctly trigger re-provisioning while preventing spurious duplicate triggers from stale cache
  - Added comprehensive test coverage for the new config version and provision trigger logic
  - All validations passing (compile, formatting, lint, unit tests, CRD generation, image build)
- Completed end-to-end testing on vmaas-dev — verified ClusterOrder and HostPool both trigger jobs, poll status, and track config versions correctly
- Discovered a critical bug during E2E: failed provision jobs cause an infinite retry loop because the reconciled config version is never set on failure, so the system perpetually sees a mismatch and keeps launching new jobs — this affects all three resource types, not just the ones changed in this PR
- Drafted a proposed fix for the infinite retry bug as a separate PR, with multiple approach options to discuss with the team

## Risks & Challenges
- Ongoing missile attacks in Israel — sleep-deprived, frequent shelter time, helping kids with remote schooling, cooking and childcare throughout the day. Reduced availability and focus expected.
- Critical infinite retry bug discovered — needs alignment with Adrien on fix approach before the PR can be finalized

## Key Effort
Implementing config version tracking and duplicate trigger prevention for ClusterOrder and HostPool, with E2E validation and discovery of a critical retry bug requiring a separate fix.
