---
layout: default
title: Daily Status - 2026-03-15
date: 2026-03-15
---

# Daily Status - 2026-03-15

## Accomplishments
- Continued work on [OSAC ClusterOrder AAP Direct Ready](https://github.com/osac-project/osac-operator/pull/110) — rebased on upstream main and pushed multiple improvements
- Fixed deletion blocking behavior on failure and corrected timestamp handling to use UTC
- Implemented config version tracking and duplicate trigger guard for ClusterOrder and HostPool, ensuring spec changes are detected and re-provisioned while preventing spurious duplicate triggers from stale cache
- Added comprehensive unit tests covering provision trigger logic, config version hashing, and annotation reconciliation
- Performed end-to-end testing on vmaas-dev — verified ClusterOrder and HostPool provisioning, polling, and config version behavior all working correctly
- Discovered a critical bug during E2E: failed provision jobs cause an infinite retry loop because the reconciled config version is never set on failure — this affects all three resource types (ClusterOrder, HostPool, ComputeInstance)
- Drafted a proposed fix with multiple approach options and prepared a Slack message to discuss with Adrien
- Added integration tests for ClusterOrder (10 tests) and HostPool (8 tests) covering the full provision lifecycle, failure handling, stale cache guard, deprovisioning, and deletion blocking — documented the infinite retry bug with pending test placeholders

## Risks & Challenges
- Ongoing missile attacks in Israel — sleep-deprived, frequent shelter time, helping kids with remote schooling, cooking and childcare throughout the day. Reduced availability and focus expected.
- Critical infinite retry bug discovered — affects all resource types when a provision job fails. Fix planned as a separate PR pending discussion with Adrien.

## Key Effort
Implementing config version tracking and duplicate trigger guards for ClusterOrder and HostPool in [osac-operator#110](https://github.com/osac-project/osac-operator/pull/110), including E2E validation and comprehensive test coverage.
