---
layout: default
title: Daily Status - 2026-03-15
date: 2026-03-15
---

# Daily Status - 2026-03-15

## Accomplishments
- Continued work on ClusterOrder AAP Direct Ready feature ([osac-operator#110](https://github.com/osac-project/osac-operator/pull/110))
  - Rebased on upstream main
  - Fixed block-deletion-on-failure behavior and UTC timestamp handling
  - Implemented config version tracking and duplicate trigger guard for ClusterOrder and HostPool, following the existing ComputeInstance pattern — enables detecting spec changes and re-provisioning accordingly while preventing spurious duplicate triggers from stale cache
  - Added comprehensive unit tests for config version and provision trigger logic
  - Pushed all changes to [osac-operator#110](https://github.com/osac-project/osac-operator/pull/110)
- Ran end-to-end testing on vmaas-dev environment — verified ClusterOrder and HostPool provisioning, job polling, and config version tracking all work correctly
- Discovered a critical bug during E2E: infinite provision retry loop when a job fails — affects all three resource types (ClusterOrder, HostPool, ComputeInstance). Root cause is that a failed job never marks itself as reconciled, so the controller keeps triggering new jobs indefinitely. Drafted a proposal to Adrien with fix options for a separate PR

## Risks & Challenges
- Ongoing missile attacks in Israel — sleep-deprived, frequent shelter time, helping kids with remote schooling, cooking and childcare throughout the day. Reduced availability and focus expected
- Critical infinite retry bug discovered — needs alignment with Adrien on fix approach before [osac-operator#110](https://github.com/osac-project/osac-operator/pull/110) can be marked ready for review

## Key Effort
Implementing config version tracking and duplicate trigger guards for ClusterOrder and HostPool to support spec-change-driven re-provisioning, and validating the feature end-to-end.
