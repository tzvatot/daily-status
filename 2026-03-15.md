---
layout: default
title: Daily Status - 2026-03-15
date: 2026-03-15
---

# Daily Status - 2026-03-15

## Accomplishments
- Continued work on [OSAC ClusterOrder AAP Direct Ready](https://github.com/osac-project/osac-operator/pull/110) — rebased on upstream main and committed several improvements:
  - Fixed deletion blocking behavior on failure and corrected timestamp handling
  - Implemented config version tracking and duplicate trigger guards for ClusterOrder and HostPool, ensuring spec changes are detected and re-provisioned correctly while preventing duplicate triggers from stale cache
  - Added comprehensive unit tests covering provision trigger logic, config version handling, and reconciliation behavior
  - Added integration tests for both ClusterOrder (10 tests) and HostPool (8 tests) covering the full provision lifecycle, failure scenarios, stale cache guards, and deprovisioning
- Deployed and ran end-to-end testing on vmaas-dev — verified ClusterOrder and HostPool provisioning, job polling, and config version tracking all work correctly
- Discovered a critical bug during E2E: infinite provision retry loop when a job fails — this affects all three resource types but was previously unnoticed since CI jobs rarely fail. Drafted a Slack message to Adrien proposing a fix as a separate ticket/PR with multiple approach options

## Risks & Challenges
- Ongoing missile attacks in Israel — sleep-deprived, frequent shelter time, helping kids with remote schooling, cooking and childcare throughout the day. Reduced availability and focus expected.
- Critical infinite retry bug discovered — failed jobs never mark their config version as reconciled, causing endless re-provisioning. Fix planned as a separate PR affecting all three resource types; need alignment with Adrien on approach.

## Key Effort
Implementing config version tracking and duplicate trigger prevention for ClusterOrder and HostPool in [osac-operator#110](https://github.com/osac-project/osac-operator/pull/110), with full test coverage and E2E validation.
