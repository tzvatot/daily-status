---
layout: default
title: Daily Status - 2026-03-15
date: 2026-03-15
---

# Daily Status - 2026-03-15

## Accomplishments
- Continued work on [OSAC ClusterOrder AAP Direct Ready](https://github.com/osac-project/osac-operator/pull/110) — rebased on upstream main and committed several improvements:
  - Fixed deletion blocking behavior on failure and corrected timestamp handling to use UTC
  - Implemented config version tracking and duplicate trigger prevention for ClusterOrder and HostPool, ensuring spec changes are detected and re-provisioned while preventing spurious duplicate triggers from stale cache
  - Added comprehensive unit tests covering provision trigger logic, config version hashing, and annotation reconciliation
- Deployed and ran end-to-end validation on vmaas-dev — confirmed ClusterOrder and HostPool provision lifecycle works correctly with config version tracking
- Discovered a critical bug during E2E testing: failed provision jobs cause an infinite retry loop because the reconciled config version is never set on failure, causing perpetual re-triggering. This bug also affects ComputeInstance but had gone unnoticed. Drafted a Slack message to Adrien proposing a fix as a separate PR
- Added integration tests for ClusterOrder (10 tests) and HostPool (8 tests) covering the full provision lifecycle, failure handling, stale cache guards, deprovisioning, and deletion blocking. Documented the infinite retry bug with pending test cases

## Risks & Challenges
- Ongoing missile attacks in Israel — sleep-deprived, frequent shelter time, helping kids with remote schooling, cooking and childcare throughout the day. Reduced availability and focus expected
- Discovered infinite retry bug on provision failure affects all three resource types — needs discussion with Adrien on preferred fix approach before [osac-operator#110](https://github.com/osac-project/osac-operator/pull/110) can be finalized

## Key Effort
Implementing config version tracking and duplicate trigger prevention for ClusterOrder and HostPool in [osac-operator#110](https://github.com/osac-project/osac-operator/pull/110), with full test coverage and E2E validation.
