---
layout: default
title: Daily Status - 2026-03-15
date: 2026-03-15
---

# Daily Status - 2026-03-15

## Accomplishments
- Continued work on [OSAC ClusterOrder AAP Direct Ready](https://github.com/osac-project/osac-operator/pull/110) — rebased on upstream main and committed fixes for block-deletion-on-failure behavior and UTC timestamp handling
- Implemented config version tracking and duplicate trigger guard for ClusterOrder and HostPool resources, ensuring spec changes properly trigger re-provisioning while preventing duplicate triggers from stale cache during back-to-back reconciles
- Added comprehensive unit tests covering provision trigger logic, config version hashing, and reconciled config version handling
- Deployed and ran end-to-end testing on vmaas-dev — verified ClusterOrder and HostPool provision lifecycle, job triggering, status polling, and config version tracking all working correctly
- Discovered a critical bug during E2E: infinite provision retry loop when a job fails, affecting all three resource types (ClusterOrder, HostPool, ComputeInstance) — drafted a proposed fix and Slack message to Adrien for discussion
- Added integration tests for ClusterOrder (10 tests) and HostPool (8 tests) covering provision lifecycle, failure handling, stale cache guard, deprovision, and config version behavior — including pending tests documenting the infinite retry bug

## Risks & Challenges
- Ongoing missile attacks in Israel — sleep-deprived, frequent shelter time, helping kids with remote schooling, cooking and childcare throughout the day. Reduced availability and focus expected.
- Critical infinite retry bug found: when a provision job fails, the system retries indefinitely because the reconciled config version is never set. This affects all three resource types and needs a coordinated fix in a separate PR.

## Key Effort
Implementing config version tracking and duplicate trigger prevention for ClusterOrder and HostPool, validating via E2E testing, and discovering a critical infinite retry bug to be addressed separately.
