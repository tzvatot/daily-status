---
layout: default
title: Daily Status - 2026-03-15
date: 2026-03-15
---

# Daily Status - 2026-03-15

## Accomplishments
- Continued work on [OSAC ClusterOrder AAP Direct Ready](https://github.com/osac-project/osac-operator/pull/110) — rebased on upstream main and committed several fixes:
  - Fixed block-deletion-on-failure behavior and UTC timestamp handling
  - Implemented config version tracking and duplicate trigger guard for ClusterOrder and HostPool, ensuring spec updates trigger re-provisioning and back-to-back reconciles don't cause duplicate jobs
  - Added unit tests covering provision trigger logic, config version hashing, and reconciled version handling
  - Added integration tests for ClusterOrder (10 tests) and HostPool (8 tests) covering provision lifecycle, failure handling, stale cache guard, deprovisioning, and config version behavior
- Performed end-to-end testing on vmaas-dev — verified ClusterOrder and HostPool provision jobs trigger correctly with config version tracking
- Discovered a critical bug during E2E: infinite provision retry loop when a job fails (affects all three resource types, not just ClusterOrder/HostPool) — drafted a Slack message to Adrien with a proposed fix as a separate PR
- Documented the infinite retry bug with pending integration tests so the known issue is captured in the test suite

## Risks & Challenges
- Ongoing missile attacks in Israel — sleep-deprived, frequent shelter time, helping kids with remote schooling, cooking and childcare throughout the day. Reduced availability and focus expected.
- Critical infinite retry bug discovered — failed provision jobs never stop retrying because the reconciled config version is never set on failure. Fix proposed as a separate PR pending discussion with Adrien.

## Key Effort
Implementing config version tracking and duplicate trigger guards for ClusterOrder and HostPool in [osac-operator#110](https://github.com/osac-project/osac-operator/pull/110), with full test coverage and E2E validation.
