---
layout: default
title: Daily Status - 2026-03-15
date: 2026-03-15
---

# Daily Status - 2026-03-15

## Accomplishments
- Continued work on ClusterOrder AAP Direct Ready feature ([osac-operator#110](https://github.com/osac-project/osac-operator/pull/110)):
  - Rebased feature branch on upstream main
  - Fixed deletion blocking behavior on failure and corrected timestamp formatting to UTC
  - Implemented config version tracking and duplicate trigger guard for ClusterOrder and HostPool, ensuring spec changes trigger re-provisioning while preventing duplicate triggers from stale cache
  - Added comprehensive unit tests covering provision trigger logic, config version handling, and reconciliation behavior
  - Pushed all changes to [osac-operator#110](https://github.com/osac-project/osac-operator/pull/110)
- Performed end-to-end testing on vmaas-dev environment:
  - Verified ClusterOrder and HostPool provisioning flows work correctly with config version tracking
  - Discovered a critical bug: infinite provision retry loop when a job fails, affecting all three resource types (ClusterOrder, HostPool, ComputeInstance)
  - Drafted a Slack message to Adrien proposing the fix as a separate ticket/PR with multiple approach options
- Added integration tests for ClusterOrder (10 tests) and HostPool (8 tests) covering the full provision lifecycle, failure handling, stale cache guard, deprovisioning, and deletion blocking
  - Documented the infinite retry bug with pending tests to be enabled once the fix lands

## Risks & Challenges
- Ongoing missile attacks in Israel — sleep-deprived, frequent shelter time, helping kids with remote schooling, cooking and childcare throughout the day. Reduced availability and focus expected.
- Discovered critical infinite retry bug on job failure that affects all resource types — existing in production but previously unnoticed. Proposed fix requires alignment with Adrien before proceeding.

## Key Effort
Implementing config version tracking and duplicate trigger prevention for ClusterOrder and HostPool, validating via E2E testing, and uncovering a critical infinite retry bug on job failure.
