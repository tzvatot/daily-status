---
layout: default
title: Daily Status - 2026-03-15
date: 2026-03-15
---

# Daily Status - 2026-03-15

## Accomplishments

- Continued work on ClusterOrder AAP Direct Ready ([osac-operator#110](https://github.com/osac-project/osac-operator/pull/110)):
  - Rebased on upstream main and committed fixes for block-deletion-on-failure behavior and UTC timestamp handling
  - Implemented config version tracking and duplicate trigger guard for ClusterOrder and HostPool, ensuring spec changes are detected and re-provisioned while preventing duplicate triggers from stale cache
  - Added unit tests covering provision trigger logic, config version hashing, and reconciled config version handling
  - Added integration tests for ClusterOrder (10 tests) and HostPool (8 tests) covering the full provision lifecycle, failure handling, stale cache guard, deprovisioning, and config version behavior
- Deployed and validated end-to-end on vmaas-dev — ClusterOrder and HostPool both working correctly with config version tracking
- Discovered a critical bug: infinite provision retry loop when a job fails — affects all three resource types (ClusterOrder, HostPool, ComputeInstance). Drafted a proposed fix with multiple approaches for discussion with Adrien

## Risks & Challenges

- Ongoing missile attacks in Israel — sleep-deprived, frequent shelter time, helping kids with remote schooling, cooking and childcare throughout the day. Reduced availability and focus expected.
- Critical infinite retry bug discovered during E2E testing — when a provision job fails, it retriggers indefinitely because the reconciled config version is never set. Fix planned as a separate PR pending discussion with Adrien.

## Key Effort

Implementing config version tracking and duplicate trigger guards for ClusterOrder and HostPool in [osac-operator#110](https://github.com/osac-project/osac-operator/pull/110), including unit/integration tests and E2E validation.
