---
layout: default
title: Daily Status - 2026-03-15
date: 2026-03-15
---

# Daily Status - 2026-03-15

## Accomplishments
- Continued work on ClusterOrder AAP Direct Ready ([osac-operator#110](https://github.com/osac-project/osac-operator/pull/110)):
  - Fixed deletion blocking behavior on failure and corrected timestamp handling
  - Implemented config version tracking and duplicate trigger prevention for ClusterOrder and HostPool, ensuring spec changes are detected and re-provisioned while preventing spurious duplicate triggers from stale cache
  - Added comprehensive unit tests covering provision trigger logic, config version hashing, and annotation reconciliation
  - Deployed and validated end-to-end on vmaas-dev — both ClusterOrder and HostPool provision, poll, and track config versions correctly
  - Discovered a critical bug: failed provision jobs cause an infinite retry loop because the reconciled config version is never set on failure, affecting all three resource types (ClusterOrder, HostPool, ComputeInstance)
  - Drafted a proposed fix with multiple approaches and prepared a Slack message to discuss with Adrien as a separate ticket/PR
  - Added integration tests for ClusterOrder (10 tests) and HostPool (8 tests) covering full provision lifecycle, failure handling, stale cache guard, deprovisioning, and deletion blocking — including pending tests that document the known infinite retry bug

## Risks & Challenges
- Ongoing missile attacks in Israel — sleep-deprived, frequent shelter time, helping kids with remote schooling, cooking and childcare throughout the day. Reduced availability and focus expected.
- Critical infinite retry bug discovered during E2E testing — failed provision jobs trigger endless new jobs when config version tracking is enabled. Fix planned as a separate PR affecting all three resource types.

## Key Effort
Implementing config version tracking and duplicate trigger prevention for ClusterOrder and HostPool, with end-to-end validation and comprehensive test coverage.
