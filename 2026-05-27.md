---
layout: default
title: Daily Status - 2026-05-27
date: 2026-05-27
---

# Daily Status - 2026-05-27

## Accomplishments

- **Reviewed [osac-workspace#21](https://github.com/osac-project/osac-workspace/pull/21)** — re-reviewed multiple revisions of the fork-based PR workflow skill, resolved all findings, and approved
- **Updated [osac-workspace#23](https://github.com/osac-project/osac-workspace/pull/23)** — fixed placeholder values in example config and committed a dashboard refactor (static HTML + client-side data with PR-based publish flow); all 51 tests passing, pushed to fork
- **Split pr-notify into a GitHub Action + Slack service** ([osac-workspace#23](https://github.com/osac-project/osac-workspace/pull/23)) — separated PR data collection (scheduled GitHub Action) from Slack notification (local systemd service) to decouple concerns; all 44 tests passing, pushed to fork, pinged Eran for review
- **Continued multi-repo architecture discussion with David Eads** — read his response on payload model and version skew, sent follow-up on cross-repo coordination ownership, and discussed with Eran why `go.work` helps local dev but doesn't solve runtime skew or release coordination; waiting for David's reply
- **Triaged [OSAC-38](https://redhat.atlassian.net/browse/OSAC-38) rename scope** — reviewed current status of the fulfillment-to-osac renaming epic and replied to Alona confirming it's still relevant with a list of remaining items
- **Fixed CodeRabbit findings on [osac-aap#318](https://github.com/osac-project/osac-aap/pull/318)** — corrected wrong role references and added missing metadata; replied to all CodeRabbit comments and converted PR to draft since it's demo-only
- **Advanced E2E test coverage for [OSAC-706](https://redhat.atlassian.net/browse/OSAC-706)** ([osac-test-infra#44](https://github.com/osac-project/osac-test-infra/pull/44), draft) — rebased onto upstream, ran E2E tests (2 pass, 3 fail due to server bug and missing CLI flag), implemented all 6 field-definition E2E tests (defaults, non-editable, editable override, schema validation, node sets, mutual exclusivity); all 11 tests collect and lint passes; pushed to fork
- **Discovered server-side bug**: public API List does not filter unpublished catalog items — started a fix on a separate branch to filter unpublished items from public List/Get responses
- **Posted gap analysis for [OSAC-997](https://redhat.atlassian.net/browse/OSAC-997)** — reviewed tenant user stories against current implementation and identified gaps including missing CLI scale command, missing E2E scale test, stale API paths, and unpublished API docs
- **Recorded final polished demo** for catalog items

## Risks & Challenges

- **Innabox CA certificate expires 2026-05-28 (tomorrow)** — all TLS connections using this CA will break; needs immediate rotation or renewal
- **E2E test failures on [OSAC-706](https://redhat.atlassian.net/browse/OSAC-706)** — CLI tests fail because the deployed `osac` binary is missing the `--catalog-item` flag, and the unpublished-visibility test needs a server-side fix before it can pass
- Open design question: should catalog item Create/Update/Delete be removed from the public API proto (currently exposed but OPA-gated)?
- No E2E test coverage for ComputeInstance catalog items (all tests are cluster-only)
- AAP project source still needs restoring to upstream/main

## Key Effort

Advancing E2E test coverage for catalog item field definitions ([OSAC-706](https://redhat.atlassian.net/browse/OSAC-706)) and splitting the PR notification tooling into a GitHub Action + Slack service architecture.
